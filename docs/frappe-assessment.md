# Frappe Assessment And cf-frappe Parity Map

## Assessment

Frappe's core productivity loop is metadata-driven application assembly:

- A DocType is the main unit of model definition and captures fields, naming, list/form behavior, permissions, and persistence intent.
- Document APIs make each DocType available through uniform read and write operations.
- Hooks and controller methods let apps add behavior around lifecycle boundaries.
- Role-based permissions are model-level configuration rather than scattered handler code.
- REST resources expose documents through predictable `/api/resource/...` endpoints.

That model is powerful, but it is not event-sourcing first. cf-frappe keeps the metadata center and shifts the persistence contract:

- commands decide whether a change is allowed
- command decisions fold current state from the event stream
- accepted changes are stored as immutable domain events
- current document state is a projection
- Durable Objects are the intended command serialization boundary
- D1 stores event streams and queryable projections in a single command commit batch

## Current Equivalence

| Capability | Frappe | cf-frappe current state |
| --- | --- | --- |
| Metadata model | DocTypes | `defineDocType` |
| Custom fields | Custom Field / Customize Form | event-sourced tenant custom field overlays folded into base DocType metadata by `CustomFieldService`, with generated HTTP/Desk/CLI administration and runtime application to command validation, metadata APIs, form/list views, form placeholders, filters, saved filters, and Worker query/command services |
| Property setters | Property Setter | event-sourced tenant field property overrides folded after custom fields by `FieldPropertyService`, with generated HTTP/Desk/CLI administration and runtime application to labels, descriptions, placeholders, no-copy and allow-on-submit behavior, command validation, metadata APIs, form/list/search flags, filters, and Worker query/command services |
| Naming series | autoname and naming_series | field/provided/uuid strategies plus event-stream-backed series counters |
| Field validation | DocField rules | built-in field types, description/help and placeholder metadata, print-hide and empty-value-print-hide metadata, no-copy duplication metadata, submitted-document allow-on-submit metadata, link-backed fetch-from metadata, conditional mandatory/read-only/hidden metadata, required, min/max, select options, and scalar `unique` fields enforced through event-stream-backed value reservations |
| Link fields | Link DocFields | registered DocType targets, event-stream existence checks, generated option lookup API, and Desk select controls |
| Child tables | Table DocFields and child DocTypes | `type: "table"` fields backed by child DocType row validation, nested link checks, and Desk row grids |
| Default values | DocField defaults | scalar and function defaults |
| Permissions | roles, permission rules, user permissions, and DocShare | role/action/predicate rules plus event-sourced linked-record user permissions and document-stream share grants |
| Roles | Role DocType | per-tenant event-sourced role catalog with generated HTTP, Desk, browser-helper, metadata-read, remote CLI, and optional account role-validation surfaces |
| Document sharing | DocShare | per-document share/revoke events folded into read/update/share overlays, HTTP routes, timelines, audit search, and user notifications |
| Document lifecycle | docstatus draft/submitted/cancelled plus Duplicate/Amend | first-class submit/cancel events with command-side status guards, allow-on-submit field updates for submitted documents, no-copy-aware duplication, and cancelled-document amendment through source-read/target-create `DocumentCreated` paths |
| Lifecycle behavior | hooks/controllers | registry hooks: `beforeValidate`, `validate`, `afterCommit` |
| REST resources | `/api/resource/:doctype` | generated Hono routes with metadata-configured list views, system-field, pattern, presence, range, membership, scalar, and nested compound expression filters, metadata-aware list ordering, bounded CSV exports, metadata-driven CSV import templates, command-bound CSV imports, resolved filter-builder metadata, duplicate/amend actions, and bounded selected-document bulk delete/lifecycle/workflow actions |
| Desk views | list/form views from metadata | generated server-rendered `/desk` list/forms with model-defined form sections, columns, list columns, saved filters, system-field, pattern, presence, range, membership, scalar, and nested compound expression filters, generated top-level compound condition builders, order controls, CSV export actions, filter-builder metadata plus a bounded advanced expression editor/preview, duplicate/amend actions, permission-aware selected-row bulk delete/lifecycle/workflow actions, and page size |
| Global search | Desk/global search | metadata-driven permissioned search over readable projections with `inGlobalSearch` fields, HTTP route, generated Desk page, Desk browser helpers, and remote CLI reads |
| Print formats | printable document views | metadata-defined printable document pages, letterheads, escaped templates that respect DocField print-hide and empty-value-print-hide metadata, HTTP/browser/CLI metadata inspection, remote CLI rendered HTML/PDF downloads, event-sourced tenant print defaults with Desk/browser/CLI administration, and an injected Cloudflare Browser Rendering PDF adapter |
| Reports | Report DocType/query reports | metadata-defined document reports plus custom row-provider reports, typed filters, row ordering, arithmetic formula columns with numeric field/literal/nested-expression operands, summaries, drilldown charts, saved definitions, CSV/PDF exports, HTTP/Desk/browser/CLI report-builder APIs, Desk pages, and remote CLI metadata/run/CSV export commands |
| Dashboards | Dashboard / Number Card | metadata-defined number and chart cards backed by permissioned flat-filtered and compound-filtered document counts, document aggregates, report summaries, or report charts, with value-driven indicators, metric/chart drilldowns, HTTP routes, generated Desk pages, browser helpers, and remote CLI metadata/run reads over the same `DashboardService` boundary |
| Calendar views | Calendar View | metadata-defined read-only Calendar views over DocType date/datetime fields through `QueryService`, with metadata-validated title/date/all-day/color fields plus flat and compound filters, HTTP routes, generated Desk pages, browser helpers, workspace shortcuts, and remote CLI metadata/run reads |
| Kanban boards | Kanban Board | metadata-defined read-only Kanban boards grouped from permissioned DocType projections through `QueryService`, with metadata-validated columns plus flat and compound filters, HTTP routes, generated Desk pages, browser helpers, workspace shortcuts, and remote CLI metadata/run reads |
| Web Forms | Web Form | metadata-defined themed submission forms with DocField placeholder rendering, published and login-required filtering plus optional safe public routes and success URLs, validated against DocType metadata, restricted to declared writable scalar fields, permissioned by login state, form roles, and DocType create permissions, and submitted through `DocumentService.create` from name-based HTTP, route-or-name public HTML, route-aware Desk browser helpers, and remote CLI commands |
| Website Generator / Web View Pages | Website Generator and Web View Page | metadata-defined themed public flat-filtered and compound-filtered, ordered, paginated list/detail pages over permissioned DocType projections, with metadata-validated title/published/display fields, canonical nested route values, generated JSON and HTML routes, Desk browser helpers, and remote CLI reads |
| Web Page | Web Page | metadata-defined static public pages stored in app manifests, with safe relative route validation, role/published filtering, escaped HTML rendering, JSON metadata routes, Desk browser helpers, and remote CLI reads without adding a content persistence path |
| Website Settings | Website Settings / Navbar Settings | metadata-defined singleton site title, homepage target, and role-filtered navigation rendered across public Web Page/Web View/Web Form HTML, with Web Page route, Web Form, Web View, and safe-link reference validation, generated JSON metadata route, Desk browser helpers, `/` homepage redirect, and remote CLI reads |
| Website Theme | Website Theme | metadata-defined safe design tokens and font family, referenced by Website Settings, exposed through generated JSON metadata routes, Desk browser helpers, and remote CLI reads, and applied to generated Web Page, Web View, and Web Form rendering without raw CSS injection |
| Workspaces | Workspace | metadata-defined role-filtered workspace sections and shortcuts to DocType lists, new-document forms, reports, dashboards, Calendar views, Kanban boards, files, notifications, admin tools, safe links, and remote CLI metadata reads |
| Audit trail | document versioning/activity | permissioned timelines with field diffs, comments, activity feed entries, assignments, tags, followers, admin audit search, deleted-document recovery, and remote audit CLI reads projected from append-only events plus model-declared domain commands |
| Assignment rules | Assignment Rule | source metadata plus tenant-scoped event-sourced runtime assignment rules with generated HTTP, Desk, browser-helper, and remote CLI administration, Cloudflare Durable Object hook execution by default, post-commit snapshot conditions, static/field assignees, temporal rule evaluation, and normal `DocumentAssigned` events through the application command boundary |
| Current reads | SQL document tables | D1/in-memory projections plus metadata-planned D1 indexes and shared D1 event insert serialization across generic event-store and document-store commits |
| Migrations | patches and schema migrations | D1 migration plans, rendered SQL bundles, applied checksum journal, app-declared data patches with applied and rollback journals, event-level field unsets for rename/remove backfills, API/Desk/CLI status, plan, rollback-plan, apply, rollback, failed-patch retry, and failed-rollback retry controls, plus API/Desk/browser/CLI enqueue, rollback-enqueue, and rollback-retry-enqueue controls with optional bounds |
| Workflow | Workflow DocType | source metadata transitions plus tenant-scoped event-sourced runtime workflow definition overrides, HTTP/Desk/CLI administration, effective metadata composition, and transition events |
| Background jobs | scheduler and queue workers | `JobRegistry`, queue dispatch/consume, Cron mapping, tenant-scoped event-sourced runtime schedule definitions with explicit Worker trigger catalogs plus enable/disable/pause-until/reset overrides, D1-backed execution history, failed-job retry admin, filter-preserving Desk schedule operator actions, and remote CLI job/schedule operations |
| File attachments | File DocType plus file store | `File` metadata DocType plus R2/in-memory `FileStorage`, direct upload reservation/finalization APIs, event-sourced multipart upload orchestration for R2-backed large objects, browser-side chunked upload helpers, file scan hooks, permissioned inline previews, injected image transform adapters with bounded watermark and overlay-source options, event-sourced persisted image renditions, generated record attachment panels, bounded bulk delete/metadata workflows, filtered Desk file manager, and remote file operator CLI |
| Notification logs | Notification Log | event-sourced per-user inbox streams with generated HTTP, Desk, browser-helper, and remote CLI list/read/dismiss workflows |
| Notification rules | Notification | tenant-scoped event-sourced notification rules that react to document commit events, evaluate metadata-validated list-filter conditions against post-commit snapshots, resolve static/document-owner/field recipients, record durable per-user inbox entries, and optionally send transactional email through an EmailSender port and Cloudflare Email Sending adapter |
| Realtime notifications | `publish_realtime`/Socket.IO | document commit events over Durable Object topics with tenant, DocType, document, and redacted user-topic fan-out plus permissioned presence snapshots, transient document field-edit intent, field-level merge planning, server-side deterministic merge apply, generated realtime-updated Desk presence panels with stale-document warnings and live field-edit indicators, and bounded durable replay |
| Session auth | sessions | signed cookie, Cloudflare Access, provider-neutral OIDC/JWKS actor/account sync resolvers, Okta/Auth0/Google Workspace claim presets, optional event-sourced account/profile/login/recovery routes, env-backed Worker composition, and CLI-planned/applied Cloudflare Access application/policy setup |
| Cloudflare runtime | not native | Worker, D1, Durable Object command routing, and Browser Rendering PDF output through an adapter port |
| Apps | installed apps and hooks | `defineApp` manifests composed into one registry with dependency-ordered hooks, workspace metadata, dashboards, Calendar views, Kanban boards, Web Forms, Web Views, Web Pages, Website Settings, and Website Themes |
| Client scripts | form/list scripts | same-origin `defineClientScript` bundles plus a generated Desk browser API with DocType/workspace/report/dashboard/calendar/Kanban/Web View/Web Page/Website Settings/Website Theme/print/link-option/role/profile/workflow metadata aliases and auth/accounts/audit/roles/custom-fields/field-properties/workflows/notification-rules/assignment-rules/user-permissions/data-patches/jobs/print/report-builder/resource/history/search/dashboard/calendar/file/notification/report/collaboration/realtime helpers, Desk document/workspace/report/dashboard/calendar/Kanban/Web Form/Web View/Web Page/print/file-manager/file-download/file-preview/notification/admin navigation URL helpers including route-aware Web Form public URLs from metadata, selected-document bulk actions, basic form event hooks, field controls, field-level merge preflight, resource merge apply, generated stale-form `frm.merge_save()`, explicit shared-draft patch helpers, presence-panel merge and shared-draft apply actions including child-table patches, and feedback helpers |
| App starter | bench/new app setup | initial `cf-frappe init` scaffold with Worker, D1, Durable Object, Queue binding/consumer, R2 file bucket binding, D1 job execution history, a Task DocType/report/print/workspace/dashboard/Calendar/Kanban/Web Form/Web View/Web Page/Website Settings/Website Theme app, R2-backed file manager, idempotent rollbackable seed data patch for sample records and a starter owner-notification rule, signed-session, Cloudflare Access, or OIDC account-sync wiring, `cf-frappe access` Cloudflare Access setup automation, `cf-frappe install` package-manager/dependency/app-registry wiring, and remote DocType/custom-field/field-property/workflow/dashboard/calendar/Kanban/Web Form/Web View/Web Page/Website Settings/Website Theme/workspace/link-option/print-formats/print-settings/role/user-account/resource collaboration/history/notification-inbox/notification-rule/assignment-rule/user-permission/data-patch/job/file operator commands |

## Current Gaps

- App manifests now also compose Calendar, Kanban, Web Form, Web View, Web Page, Website Settings, and Website Theme metadata. Remote operators can use `cf-frappe calendars list|get|run`, `cf-frappe kanbans list|get|run`, `cf-frappe web-forms list|get|submit`, `cf-frappe web-views list|get|items|item`, `cf-frappe web-pages list|get`, `cf-frappe website-settings get`, and `cf-frappe website-themes list|get`, while HTTP, Desk, browser helpers, and public HTML routes read the same registry-owned definitions through `CalendarService`, `KanbanService`, `WebFormService`, `WebViewService`, `WebPageService`, `WebsiteSettingsService`, `WebsiteThemeService`, `DocumentService`, and `QueryService`. Dashboard, Calendar, Kanban, and Web View metadata now share the same bounded compound filter-expression validator/freezer, then run those expressions through `QueryService` rather than a second read model. Web Form JSON metadata and submit APIs remain name-based, unpublished Web Forms are hidden from non-admin metadata/list/submission flows, login-required Web Forms are hidden from Guest actors across metadata, public HTML, and submission flows while reusing the configured actor resolver for identity, public Web Form HTML resolves optional safe routes first, falls back to encoded names for compatibility, exposes the same route-first URL generation through metadata-aware Desk browser helpers, and renders safe success URL Continue links after submission. Web View item route values are folded from document projections only when they are safe canonical relative routes, support nested public detail URLs such as `docs/getting-started`, stay hidden when unsafe, and paginate JSON plus public HTML list pages with metadata-defined filters, compound filter expressions, and ordering plus bounded `limit`, `offset`, `hasMore`, `nextOffset`, and `totalIsExact` metadata over the safe visible item set. Public Web Page, Web View, and Web Form HTML rendering shares the same actor-aware Website Settings presentation resolution for safe theme tokens and role-filtered navigation, including Web Form and Web View navigation items plus homepage targets that reuse their application services' access checks and URL generation, and falls back to safe default CSS with no navigation when the settings are unpublished or role-restricted. Desk client scripts can now call `window.cfFrappe.webView`, `webPage`, `websiteSettings`, `websiteTheme`, and matching `meta` aliases instead of hand-building website metadata URLs.
- Generated Desk UI now covers basic list/form/report/print/workspace/dashboard/Kanban pages, generated Admin navigation, metadata-defined workspace sections and shortcuts including dashboard and Kanban shortcuts, model-defined form sections and field order, tenant custom fields, field property overrides, and runtime workflow definitions applied to generated form/list/saved-filter/global-search/metadata surfaces, parent and child table custom fields plus nested custom table fields inside child table DocTypes applied to command validation, generated metadata, JSON routes, field-property routes, workflow-admin routes, notification-rule routes, assignment-rule routes, child-table form rendering, and recursive Desk form parsing, list columns, selected-row bulk document delete/lifecycle/workflow actions, default list filters, metadata-aware list ordering, bounded resource CSV exports, metadata-driven CSV import templates, command-bound resource CSV imports, generated Desk document/workspace/report/dashboard/Kanban/print/file-manager/file-download/file-preview/notification/admin navigation URL helpers, CSV import, and bulk action forms plus Desk client helpers that preserve filtered/ordered list context, saved user filters, metadata-driven global search, system-field, pattern, presence, range, membership, scalar, and nested compound expression list filters, generated arbitrary-depth compound condition builders, resolved filter-builder metadata with a bounded advanced expression editor/preview, link-field select controls, child table row grids, submit/cancel lifecycle actions, metadata workflow transition actions, Frappe-style duplicate and amend actions, document timelines with field diffs/comments/assignments/tags/followers/share events, document share controls, role administration, custom-field administration, field-property administration, workflow administration, notification-rule administration APIs, assignment-rule administration APIs, user-permission administration, user-account administration, durable notification inbox read/dismiss workflows with event fold/ordering/invariant policy, typed report filters including equals/not-equals/contains/threshold plus between/not-between range operators, bounded report filter expression trees across HTTP, saved report definitions, custom row-provider contexts, Desk browser helpers, and visual Desk report-builder controls, report row ordering, custom row-provider reports, event-sourced saved report definitions with HTTP and Desk report-builder APIs, Desk saved report builder filter operator/default/required/numeric/date/datetime range presets, formula-column field/literal/arbitrary-depth nested formula operand and chart sort/limit/palette/value-label/axis-label controls, report formula numeric field/literal/nested-expression operands, report summaries/charts/exports/printouts/PDFs with extracted filter-expression, filter-matching, filter-coercion, filter-control, order-control, row-projection, and chart-result policy and metadata-driven ordering/palette/value-label/axis-label/drilldown controls and tenant print defaults, an optional Browser Rendering PDF adapter for Cloudflare Worker deployments, metadata-defined dashboard cards backed by permissioned document counts, count/sum/avg/min/max document aggregates, report summaries, or report charts plus ordered numeric indicator rules and metric/chart drilldowns, metadata-defined read-only Kanban boards grouped through permissioned document list projections, custom print templates, reusable letterheads, same-origin client script injection with a built-in browser API, workspace metadata client helpers, dashboard metadata aliases/card execution helpers, Kanban metadata aliases/board execution helpers, link-option metadata aliases, role metadata aliases, profile metadata alias, workflow metadata alias, auth/session/recovery/account-management/provider/profile client helpers, audit search/deleted-recovery client helpers plus remote CLI reads, role catalog client helpers, custom-field client helpers, field-property client helpers, workflow-definition client helpers, notification-rule client helpers, assignment-rule client helpers, user-permission client helpers, data-patch client helpers, job history/schedule client helpers, print metadata/document client helpers, report-builder client helpers, selected-document bulk resource client helpers, document-history read client helpers, search client helpers, file metadata/upload/direct-upload client helpers, notification inbox/read-state client helpers, document collaboration and saved-filter client helpers, basic form event hooks, field controls, field-level merge preflight, resource merge apply, generated stale-form merge save, presence-panel merge and shared-draft apply actions including child-table patches, user feedback helpers, parsed realtime subscriptions, snapshot-backed and realtime-updated document presence panels with stale-document warnings, live field-edit indicators, and page size. Broader visual admin tools and broader browser-side client APIs beyond the current metadata/auth/accounts/audit/roles/custom-fields/field-properties/workflows/notification-rules/assignment-rules/user-permissions/data-patches/jobs/print/report-builder/resource/history/desk/search/dashboard/Kanban/file/notification/report/collaboration/realtime surface are not implemented.
- DocType metadata now plans and applies D1 projection-index migrations with retired-index drops and a checksum journal, app manifests compose DocTypes/hooks/reports/print formats/workspaces/data patches, and the CLI can save app dependency metadata, run detected package managers for lockfile/node_modules updates, wire app modules into generated registries, generate reviewable D1 migration files after versioned DocType index changes, scaffold signed-session, Cloudflare Access, or OIDC account-sync starter auth, plan/apply Cloudflare Access application and policy setup with env-backed API tokens, and drive remote DocType metadata/list-view reads, remote custom-field list/save/disable commands, remote field-property list/save/clear commands, remote workflow get/save/clear commands, remote report list/get/run/export commands, remote report-builder list/get/create/update/delete/run/export commands, remote dashboard list/get/run commands, remote workspace list/get commands, remote link-option reads, remote print-formats list/get/letterheads/letterhead reads plus rendered document HTML/PDF downloads, remote print-settings get/update commands, remote role list/get/create/describe/enable/disable commands, remote user account get/create/password/roles/provider-sync/enable/disable commands, remote user profile get/update commands, remote audit search/deleted-recovery commands, remote notification inbox list/read/dismiss commands, remote resource list/get/create/update/delete/lifecycle/workflow/domain-command/duplicate/amend/bulk/timeline/comment/activity/assignment/tag/follower/share/saved-filter/CSV commands, data-patch status/plan/rollback-plan/apply/rollback/enqueue/rollback-enqueue/retry/rollback-retry/rollback-retry-enqueue, plus job history/retry/schedule operations through generated admin/resource APIs. Basic app-declared data patches/backfills have D1 and in-memory applied plus rollback journals with admin API/Desk/browser/CLI status, dry-run apply and rollback plans, apply, rollback execution, failed-patch retry, and failed-rollback retry routes with bounded and single-patch execution. Apply retry clears only a failed apply journal entry with a matching registered checksum and re-enters the normal apply runner; rollback retry atomically claims only a matching `rollback_failed` journal entry as `rollback_pending`, checks successor rollback safety, and re-enters the normal rollback runner. Event-level document field unsets let rename/remove backfills stay append-only, while queue-backed apply, rollback, and rollback-retry jobs let admins enqueue validated patch plans through Cloudflare Queues via API/Desk/browser/CLI controls; Desk batch and single-patch enqueue controls now carry optional idempotency keys and delay seconds. Richer cross-surface operator workflows are still future work.
- Background jobs now have Queue/Cron support, registry-declared worker pools with batch-lane concurrency and pool retry defaults, schedule admin, tenant-scoped event-sourced runtime schedule definitions with bounded Queue delays, explicit Worker trigger catalogs, enable/disable/pause-until/reset overrides, filter-preserving Desk schedule run/enable/disable/pause/reset/delete/save operator actions, D1-backed execution history with API/Desk/browser/CLI admin dashboards, manual schedule dispatch, failed-execution retry actions, and remote schedule save/delete/toggle/pause/run commands.
- Document history now exposes permissioned timeline entries with bounded event-sourced field-level old/new diffs, comment events, activity feed entries, assignment events, tag events, follower events, share events, admin-only audit search, deleted-document audit recovery from the event stream, and audit browser/CLI helpers over the same admin routes.
- Document command field mutation, domain-command execution, workflow-transition planning, create/update lifecycle planning, submit/cancel status-change planning, delete lifecycle planning, and duplicate/amend copy planning are now pure application boundaries separate from the main command service: readonly and conditional-readonly checks, submitted-document field mutation policy, unset safety, child-row origin validation, read-only child-table preservation, internal row-origin stripping, duplicate/amend copy shaping, compact duplicate/amend overrides, provenance metadata, compact domain-command input, field-picked versus `buildPatch` patches, role eligibility, permission-action defaults, read-only override defaults, current workflow-state resolution, allowed-transition selection, custom state-field patches, custom/default transition event types, denied-transition errors, create/update event names and payloads, status-change allowed source states, delete allowed source states, target docstatus, custom/default status/delete event types, and status/delete payload kinds are covered by focused unit tests while `DocumentService` keeps orchestration, permissions, hooks, and event construction.
- Document collaboration and share command normalization plus event planning are also pure application boundaries: comment text, activity metadata, assignees, tags, followers, share grants, share users, delegated-share permission checks, comment/activity event names and payloads, assignment/tag/follower event names, share/revoke event names and payloads, collection payloads, and noop detection now live outside `DocumentService` with direct unit coverage, leaving the service to orchestrate event-stream reads, permission providers, idempotency folds, and commits.
- Document create/update commands now commit unique-value reservation streams and the document stream through one `DocumentStore.commitBatch` boundary, with unique-value reserve/release event planning, write decision, owner lookup decision, release decision, reservation projection, release projection, naming-series start/advance event planning, committed-document snapshot projection, saved-event id/first-event lookup, after-commit completion, live-document fold guards, and before/after document hook context/data shaping isolated as pure policies/helpers. D1 uses one `db.batch` for all event inserts plus reservation/document projections, and the in-memory adapter preflights versions before mutating state, so a later unique conflict no longer leaves reserve/release compensation events from a failed command.
- Document delivery intents for notification, realtime, and email after-commit effects can now be appended to an event-sourced per-tenant outbox with idempotent source-event/target keys, claim/fail/retry/deliver transitions, stale-claim protection, a composed delivery hook, a platform-neutral drain consumer, a built-in drain job wrapper, Worker queue notification/realtime/email drain and retry coverage, default Worker/Queue plus starter wiring for generated apps, and generated Wrangler binding-type smoke coverage for D1, R2, Queue, Durable Object, and session-secret bindings. Broader live deployment coverage remains future work.
- DocType metadata and tenant-scoped runtime state can now declare assignment rules that run after selected document commit events when the assignment-rule hook is installed, reuse bounded list-filter expressions as conditions, resolve static and field-based assignees, and route matches through `DocumentService.assign` so duplicate suppression, permissions, timelines, notifications, and realtime delivery stay on the existing event-sourced assignment path. Runtime assignment rules have admin JSON routes, Desk list/edit/toggle/clear management, Desk browser helpers, remote CLI list/get/save/enable/disable/clear commands, expected-version concurrency, and temporal provider reads for deterministic delivery; Cloudflare aggregate coordinators install the hook by default with a tenantless System Manager automation actor while allowing explicit actor overrides or opt-out, and the hook rejects actors bound to a different tenant.
- Realtime notifications now have Durable Object tenant, DocType, document, and user topics plus redacted user-recipient realtime notifications, while durable notification logs are folded from separate per-user event streams with HTTP, Desk, browser-helper, and remote CLI read/dismiss events. Tenant-scoped notification rules can also create durable inbox entries from document commit events through the same after-commit hook and Worker/Durable Object composition, reuse metadata-validated list-filter expressions as post-commit conditions, and opt into transactional email delivery through the same metadata rule stream, an EmailSender port, and the Cloudflare Email Sending adapter without coupling provider code to the rule evaluator. Permissioned presence snapshots through HTTP, Desk client helpers, generated realtime-updated Desk document presence panels with stale-document warnings, transient field-edit intent, live field-edit indicators, field-level merge preflight, server-side deterministic merge apply, generated stale-form merge save, presence-panel merge actions, explicit shared-draft patch transport, generated shared-draft proposal apply actions with stale-version guards, and bounded durable replay exist, but conflict-free collaborative editing is not implemented.
- Auth providers remain adapter seams; signed cookie, Cloudflare Access, and provider-neutral OIDC/JWKS actor resolution exist, Cloudflare Access and generic OIDC accounts can auto-sync into event-sourced provider links, packaged Okta/Auth0/Google Workspace OIDC claim presets map provider groups/roles/permissions/hosted domains without SDK coupling, starter scaffolding for Access-protected and OIDC-protected Workers, CLI Access application/policy setup planning and apply automation, event-sourced role catalogs, optional catalog-backed account role validation, event-sourced account/profile/login/recovery routes, generic auth-provider account sync with HTTP/Desk/browser helpers, generic password reset and email verification token flows, self/admin profile updates, profile preference fields, browser client helpers for session/recovery/account-management/provider/profile routes, and basic Desk account/profile administration plus remote user-account/profile CLI operations exist, but setup automation beyond Cloudflare Access plus generic OIDC configuration is still future work.
- User permissions now restrict reads, link options, link validation, and existing-document commands from event-sourced per-user grants, with admin API, Desk management, and browser client helpers over the same event stream plus model-backed grant validation. Document shares now add per-record read/update/share overlays from the document stream, with HTTP and Desk management routes, while DocType action gates, document action gates, shared document command access, user-permission command access, share-management access, projected-document reads, link-option candidate selection, global-search candidate selection, projection page scans, and shared-permission provider reads are planned through pure access/query-policy decisions reused by query and document command access checks. Job schedule, history, and retry administration now plan admin access, tenant scoping, schedule visibility, manual-dispatch eligibility, override eligibility, override write/no-op decisions, summary shaping, event projection, event creation, runtime definition command shaping, runtime definition selectors, runtime definition save/delete eligibility, failed-execution eligibility, missing-snapshot rejection, dashboard query normalization, list options, job summaries, and retry metadata as pure policy before services perform execution-log, runner, event-store, or queue I/O.
- File storage now has basic R2-backed attachments plus permission-aware HTTP metadata listing/update, direct browser upload reservation/finalization APIs behind the `FileStorage` signer boundary, storage-level multipart upload primitives for R2/in-memory adapters, browser client helpers for the existing file routes, application-level direct and chunked browser upload orchestration with safe signed-storage boundaries, generated Desk file-manager and attachment forms that direct-upload only when the storage port exposes signed direct-upload targets while retaining multipart form fallbacks, event-sourced file scan hooks, generated record attachment panels, bounded selected-file metadata/delete outcomes, permissioned browser-safe inline previews, bounded image transforms plus styled text-watermark and permissioned image-overlay source options through an injected `FileTransformer` port, a Cloudflare Images adapter for resize/output transforms that rejects unsupported watermark/overlay intent explicitly, event-sourced persisted image renditions with permissioned content serving, Desk upload/list/filter/bulk-delete/bulk-metadata/metadata/download/preview/delete workflows, and remote CLI file get/list/download/preview-download/transform-download/rendition-download/update/bulk-update/bulk-delete/rendition/delete operations. Video/audio processing, provider-backed overlay pixel composition adapters, and richer media workflows remain future adapters.
- The CLI now creates a Cloudflare-ready starter app with signed-session, Cloudflare Access, or OIDC account-sync auth modes plus D1, aggregate Durable Object, realtime Durable Object, Queue, Cron, and R2 bindings, built-in data-patch jobs, D1-backed job execution history, realtime document update/presence wiring, scheduled document-delivery outbox draining, and a Task DocType/report/print/workspace/dashboard app with an R2-backed file manager and idempotent rollbackable starter seed data patch for sample records plus a starter owner-notification rule, can dry-run or apply Cloudflare Access application/policy setup with environment-backed API tokens, saves app package metadata, runs detected package managers to update lockfiles and `node_modules`, wires app modules into the generated registry, generates reviewable D1 migration files after versioned DocType index changes, can list/get deployed effective DocType metadata and list-view metadata, can list/save/disable event-sourced tenant custom fields, can list/save/clear event-sourced tenant field property overrides, can get/save/clear event-sourced tenant workflow definitions, can list/get/run/export deployed report metadata and results, can list/get/create/update/delete/run/export per-user saved report definitions, can list/get deployed workspace metadata, can list/get/run deployed dashboard metadata and card values, can read deployed link-field options through the generated resource query API, can download rendered print HTML/PDF through deployed print routes, can list/get/create/describe/enable/disable event-sourced tenant role catalog entries, can get/create/change-password/change-roles/provider-sync/enable/disable event-sourced user accounts, can get/update separate event-sourced user profile streams, can search immutable audit events and recover deleted document audit data, can list/read/dismiss durable notification inbox entries, can list/get/create/update/delete, lifecycle-transition, execute public domain commands, duplicate, amend, bulk-run, read timelines, add comments, record activity feed entries, list/assign/unassign assignees, list/tag/untag tags, list/follow/unfollow followers, share/list/revoke document shares, saved-filter list/save/delete, CSV export, CSV import-template download, and CSV import deployed DocType resources through the generated resource API, can list/get/save/enable/disable/clear event-sourced document notification rules, can list/get/save/enable/disable/clear event-sourced document assignment rules, can list/allow/revoke event-sourced linked-record user permissions, can inspect, plan, rollback-plan, apply, rollback, enqueue, rollback-enqueue, retry, rollback-retry, or rollback-retry-enqueue deployed app patches, can inspect/retry job executions plus list/run/enable/disable/pause/reset/save/delete deployed job schedules, and can inspect single-file metadata, upload local files, download original content, download browser-safe preview content, download temporary transform content, download generated rendition content, and list/update/bulk-update/bulk-delete/generate persisted renditions/delete remote File records through remote admin API routes with literal or environment-backed headers. Richer cross-surface operator workflows remain future work.
- The starter Task app now includes metadata-defined `Task Calendar`, `Task Board`, `Task Intake`, `Task Updates`, `About` Web Page, `Tasks Starter` Website Settings, and `Starter Theme` Website Theme shortcuts/homepage/theme tokens and registers them in the generated app manifest beside the Task report, print format, workspace, and dashboard.
- Test volume is intentionally focused on the new kernel and is not close to Frappe's project-wide test count: `2378` current Vitest cases versus the `2784` static-marker Frappe reference.

## Design Direction

The next stage should preserve the current dependency direction:

1. Keep domain decisions and event creation in pure TypeScript.
2. Add platform integrations only through ports and adapters.
3. Grow metadata into UI/report/job/migration generators without coupling those surfaces to D1 directly.
4. Treat Durable Objects as command routers, not as the source of all model logic.
5. Add contract tests for every new adapter before adding more generated features.

## Completion Bar For The Original Goal

The original goal asks for Frappe-like ease, a public GitHub repository, an independent architecture review, and a test suite no smaller than the old framework. This repository is not at that bar yet. The standalone review in `docs/architecture-review.md` currently fails the architecture-quality bar, and current evidence proves the first architectural slice is working, not that the full goal is complete.
