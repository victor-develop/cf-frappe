// DO NOT EDIT — generated by scripts/build-desk-client.mjs (npm run build:client).
// Source: src/adapters/desk/client-src/. CI guards drift via npm run check:client-fresh.
/* eslint-disable */

export const DESK_CLIENT_BUNDLE_HASH = "417e5653dd85a161";

export const DESK_CLIENT_BUNDLE: string = "\"use strict\";\n(() => {\n  // src/adapters/desk/client-src/context.ts\n  function runtimeScript() {\n    return document.querySelector('script[data-cf-frappe-runtime=\"desk\"]');\n  }\n  function pageContext(script) {\n    const source = script || document.currentScript || runtimeScript();\n    const dataset = source && source.dataset ? source.dataset : {};\n    const documentVersion = Number(dataset.documentVersion);\n    const context = {\n      doctype: dataset.doctype,\n      documentName: dataset.documentName,\n      realtimeRoute: dataset.realtimeRoute,\n      script: dataset.cfFrappeScript,\n      scope: dataset.scope,\n      tenantId: dataset.tenantId\n    };\n    if (dataset.documentStatus !== void 0) {\n      context.documentStatus = dataset.documentStatus;\n    }\n    if (Number.isInteger(documentVersion) && documentVersion >= 0) {\n      context.documentVersion = documentVersion;\n    }\n    return context;\n  }\n  function ready(callback) {\n    if (document.readyState === \"loading\") {\n      document.addEventListener(\"DOMContentLoaded\", callback, { once: true });\n      return;\n    }\n    callback();\n  }\n\n  // src/adapters/desk/client-src/url.ts\n  function encodePart(value) {\n    return encodeURIComponent(String(value));\n  }\n  function encodePath(value) {\n    return String(value).split(\"/\").map(encodePart).join(\"/\");\n  }\n  function withQuery(path, params) {\n    const query = new URLSearchParams();\n    Object.entries(params ?? {}).forEach(([key, value]) => {\n      if (Array.isArray(value)) {\n        value.forEach((item) => {\n          if (item !== void 0 && item !== null) {\n            query.append(key, String(item));\n          }\n        });\n      } else if (value !== void 0 && value !== null) {\n        query.set(key, String(value));\n      }\n    });\n    const suffix = query.toString();\n    return suffix ? `${path}?${suffix}` : path;\n  }\n  function setParam(params, key, value) {\n    if (value !== void 0 && value !== null) {\n      params[key] = value;\n    }\n  }\n  function appendParam(params, key, value) {\n    const current = params[key];\n    if (current === void 0) {\n      params[key] = value;\n    } else if (Array.isArray(current)) {\n      current.push(value);\n    } else {\n      params[key] = [current, value];\n    }\n  }\n  function setFormParam(params, key, value) {\n    if (value !== void 0 && value !== null) {\n      params.set(key, String(value));\n    }\n  }\n  function isPlainObject(value) {\n    return Object.prototype.toString.call(value) === \"[object Object]\";\n  }\n  function resourcePath(doctype, name) {\n    return `/api/resource/${encodePart(doctype)}${name === void 0 ? \"\" : `/${encodePart(name)}`}`;\n  }\n  function deskPath(doctype) {\n    return `/desk/${encodePart(doctype)}`;\n  }\n  function filePath(name, action) {\n    return `/api/files/${encodePart(name)}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n\n  // src/adapters/desk/client-src/bodies.ts\n  function versionBody(options) {\n    return options && options.expectedVersion !== void 0 ? { expectedVersion: options.expectedVersion } : {};\n  }\n  function withoutKeys(input, keys) {\n    const excluded = new Set(keys);\n    const body = {};\n    Object.entries(input ?? {}).forEach(([key, value]) => {\n      if (!excluded.has(key)) {\n        body[key] = value;\n      }\n    });\n    return body;\n  }\n  function commandBody(input, options) {\n    return Object.assign(withoutKeys(input, [\"expectedVersion\"]), versionBody(options));\n  }\n  function commentBody(input, options) {\n    return commandBody(typeof input === \"string\" ? { text: input } : input, options);\n  }\n  function descriptionBody(input, options) {\n    return commandBody(typeof input === \"string\" ? { description: input } : input, options);\n  }\n  function passwordBody(input, options) {\n    return commandBody(typeof input === \"string\" ? { password: input } : input, options);\n  }\n  function rolesBody(input, options) {\n    return commandBody(Array.isArray(input) ? { roles: input } : input, options);\n  }\n  function customFieldBody(field, options) {\n    const bodyField = isPlainObject(field) ? withoutKeys(field, [\"expectedVersion\"]) : field;\n    return Object.assign({ field: bodyField }, versionBody(options));\n  }\n  function notificationRuleBody(rule, options) {\n    const bodyRule = isPlainObject(rule) ? withoutKeys(rule, [\"name\", \"expectedVersion\"]) : rule;\n    return Object.assign({ rule: bodyRule }, versionBody(options));\n  }\n  function assignmentRuleBody(rule, options) {\n    const bodyRule = isPlainObject(rule) ? withoutKeys(rule, [\"name\", \"expectedVersion\"]) : rule;\n    return Object.assign({ rule: bodyRule }, versionBody(options));\n  }\n  function requiredNotificationRuleEvents(rule, ruleName) {\n    if (!Array.isArray(rule.events) || rule.events.length === 0) {\n      throw new Error(`Notification rule '${ruleName}' cannot be toggled because it has no events`);\n    }\n    return rule.events;\n  }\n  function requiredNotificationRuleRecipients(rule, ruleName) {\n    if (!Array.isArray(rule.recipients) || rule.recipients.length === 0) {\n      throw new Error(`Notification rule '${ruleName}' cannot be toggled because it has no recipients`);\n    }\n    return rule.recipients;\n  }\n  function notificationRuleToggleBody(ruleName, state, enabled, options) {\n    const expectedVersion = options?.expectedVersion;\n    if (expectedVersion !== void 0 && state && state.version !== void 0 && state.version !== expectedVersion) {\n      throw new Error(`Expected notification rules at version ${String(expectedVersion)}, found ${String(state.version)}`);\n    }\n    const entry = notificationRuleEntry(ruleName, state);\n    const rule = entry.rule;\n    const bodyRule = {\n      events: requiredNotificationRuleEvents(rule, ruleName).slice(),\n      recipients: requiredNotificationRuleRecipients(rule, ruleName).slice()\n    };\n    if (Array.isArray(rule.channels) && rule.channels.length > 0) {\n      bodyRule.channels = rule.channels.slice();\n    }\n    if (rule.condition !== void 0) {\n      bodyRule.condition = rule.condition;\n    }\n    bodyRule.enabled = enabled;\n    if (rule.subject !== void 0) {\n      bodyRule.subject = rule.subject;\n    }\n    if (rule.excludeActor !== void 0) {\n      bodyRule.excludeActor = rule.excludeActor;\n    }\n    return {\n      rule: bodyRule,\n      expectedVersion: expectedVersion !== void 0 ? expectedVersion : state && state.version !== void 0 ? state.version : 0\n    };\n  }\n  function notificationRuleEntry(ruleName, state) {\n    const entry = (state?.rules ?? []).find((item) => item && item.rule && item.rule.name === ruleName);\n    if (entry === void 0) {\n      throw new Error(`Notification rule '${ruleName}' was not found in remote state`);\n    }\n    return entry;\n  }\n  function assignmentRuleEntry(ruleName, state) {\n    const entry = (state?.rules ?? []).find((item) => item && item.rule && item.rule.name === ruleName);\n    if (entry === void 0) {\n      throw new Error(`Assignment rule '${ruleName}' was not found in remote state`);\n    }\n    return entry;\n  }\n  function fieldPropertyBody(overrides, options) {\n    const bodyOverrides = isPlainObject(overrides) ? withoutKeys(overrides, [\"expectedVersion\"]) : overrides;\n    return Object.assign({ overrides: bodyOverrides }, versionBody(options));\n  }\n  function workflowBody(workflow, options) {\n    const bodyWorkflow = isPlainObject(workflow) ? withoutKeys(workflow, [\"expectedVersion\"]) : workflow;\n    return Object.assign({ workflow: bodyWorkflow }, versionBody(options));\n  }\n  function userPermissionBody(grant, options) {\n    return commandBody(grant ?? {}, options);\n  }\n  function dataPatchBody(options, includePatchIds) {\n    return includePatchIds === false ? withoutKeys(options ?? {}, [\"patchIds\"]) : Object.assign({}, options ?? {});\n  }\n  function savedFilterBody(input) {\n    return withoutKeys(input, [\"id\"]);\n  }\n  function bulkDocumentsBody(documents) {\n    return { documents };\n  }\n  function bulkFilesBody(files, input) {\n    return Object.assign({}, input ?? {}, { files });\n  }\n  function opt(options, camel, snake) {\n    if (!options) {\n      return void 0;\n    }\n    const camelValue = options[camel];\n    if (camelValue !== void 0 || snake === void 0) {\n      return camelValue;\n    }\n    return options[snake];\n  }\n  function tenantParams(options) {\n    const params = {};\n    setParam(params, \"tenant\", options?.tenant);\n    return params;\n  }\n  function notificationInboxParams(options) {\n    const params = {};\n    setParam(params, \"user\", options?.user);\n    setParam(params, \"limit\", options?.limit);\n    setParam(params, \"unread\", options?.unread);\n    setParam(params, \"include_dismissed\", opt(options, \"includeDismissed\", \"include_dismissed\"));\n    return params;\n  }\n  function notificationCommandParams(options) {\n    const params = {};\n    setParam(params, \"user\", options?.user);\n    return params;\n  }\n  function deskNotificationInboxParams(options) {\n    const params = {};\n    setParam(params, \"limit\", options?.limit);\n    setParam(params, \"unread\", options?.unread);\n    setParam(params, \"include_dismissed\", opt(options, \"includeDismissed\", \"include_dismissed\"));\n    return params;\n  }\n  function fileAttachmentParams(params, options) {\n    const attachedTo = options?.attachedTo;\n    if (attachedTo) {\n      setParam(params, \"attached_to_doctype\", attachedTo.doctype);\n      setParam(params, \"attached_to_name\", attachedTo.name);\n      return;\n    }\n    if (options && (options.attached_to_doctype !== void 0 || options.attached_to_name !== void 0)) {\n      setParam(params, \"attached_to_doctype\", options.attached_to_doctype);\n      setParam(params, \"attached_to_name\", options.attached_to_name);\n    }\n  }\n  function fileListParams(options) {\n    const params = {};\n    fileAttachmentParams(params, options ?? {});\n    setParam(params, \"content_type\", opt(options, \"contentType\", \"content_type\"));\n    setParam(params, \"filename\", options?.filename);\n    setParam(params, \"is_private\", opt(options, \"isPrivate\", \"is_private\"));\n    setParam(params, \"limit\", options?.limit);\n    setParam(params, \"scan_status\", opt(options, \"scanStatus\", \"scan_status\"));\n    setParam(params, \"storage_state\", opt(options, \"storageState\", \"storage_state\"));\n    setParam(params, \"uploaded_by\", opt(options, \"uploadedBy\", \"uploaded_by\"));\n    return params;\n  }\n  function fileWatermarkText(value) {\n    if (value && typeof value === \"object\") {\n      return value.text;\n    }\n    return value;\n  }\n  function fileWatermarkField(value, field) {\n    if (value && typeof value === \"object\") {\n      return value[field];\n    }\n    return void 0;\n  }\n  function fileOverlayFile(value) {\n    if (value && typeof value === \"object\") {\n      return value.file;\n    }\n    return value;\n  }\n  function fileOverlayField(value, field) {\n    if (value && typeof value === \"object\") {\n      return value[field];\n    }\n    return void 0;\n  }\n  function fileTransformParams(options) {\n    const params = {};\n    setParam(params, \"width\", options?.width);\n    setParam(params, \"height\", options?.height);\n    setParam(params, \"fit\", options?.fit);\n    setParam(params, \"format\", options?.format);\n    setParam(params, \"quality\", options?.quality);\n    setParam(params, \"watermark\", fileWatermarkText(options?.watermark));\n    setParam(params, \"watermarkPlacement\", fileWatermarkField(options?.watermark, \"placement\"));\n    setParam(params, \"watermarkOpacity\", fileWatermarkField(options?.watermark, \"opacity\"));\n    setParam(params, \"watermarkColor\", fileWatermarkField(options?.watermark, \"color\"));\n    setParam(params, \"watermarkFontSize\", fileWatermarkField(options?.watermark, \"fontSize\"));\n    setParam(params, \"overlay\", fileOverlayFile(options?.overlay));\n    setParam(params, \"overlayPlacement\", fileOverlayField(options?.overlay, \"placement\"));\n    setParam(params, \"overlayOpacity\", fileOverlayField(options?.overlay, \"opacity\"));\n    setParam(params, \"overlayWidth\", fileOverlayField(options?.overlay, \"width\"));\n    setParam(params, \"overlayHeight\", fileOverlayField(options?.overlay, \"height\"));\n    return params;\n  }\n  function auditEventParams(options) {\n    const params = {};\n    setParam(params, \"tenant\", options?.tenant);\n    setParam(params, \"doctype\", options?.doctype);\n    setParam(params, \"name\", options?.name);\n    setParam(params, \"actor_id\", opt(options, \"actorId\", \"actor_id\"));\n    setParam(params, \"kind\", options?.kind);\n    setParam(params, \"since\", options?.since);\n    setParam(params, \"until\", options?.until);\n    setParam(params, \"limit\", options?.limit);\n    return params;\n  }\n  function printFormatParams(options) {\n    const params = {};\n    setParam(params, \"doctype\", options?.doctype);\n    return params;\n  }\n  function setFilterParam(params, key, value) {\n    params[key] = value;\n    if (value === \"\" || Array.isArray(value) && value.some((item) => item === \"\")) {\n      appendParam(params, \"empty_filter\", key);\n    }\n  }\n  function setFilterExpressionParam(params, value) {\n    if (value === void 0 || value === null || value === \"\") {\n      return;\n    }\n    params.filter_expression = typeof value === \"string\" ? value : JSON.stringify(value);\n  }\n  function appendFilterParams(params, field, value) {\n    if (value === void 0 || value === null) {\n      return;\n    }\n    if (isPlainObject(value)) {\n      Object.entries(value).forEach(([operator, operand]) => {\n        if (operand !== void 0 && operand !== null) {\n          setFilterParam(params, `filter_${field}${operator === \"eq\" ? \"\" : `__${operator}`}`, operand);\n        }\n      });\n      return;\n    }\n    setFilterParam(params, `filter_${field}`, value);\n  }\n  function resourceListParams(options) {\n    const params = {};\n    Object.entries(options ?? {}).forEach(([key, value]) => {\n      if (key !== \"filters\" && key !== \"filterExpression\" && key !== \"filter_expression\" && key !== \"orderBy\" && key !== \"order_by\" && value !== void 0 && value !== null) {\n        params[key] = value;\n      }\n    });\n    setParam(params, \"order_by\", opt(options, \"orderBy\", \"order_by\"));\n    setParam(params, \"order\", options?.order);\n    setFilterExpressionParam(params, opt(options, \"filterExpression\", \"filter_expression\"));\n    Object.entries(options?.filters ?? {}).forEach(([field, value]) => {\n      appendFilterParams(params, field, value);\n    });\n    return params;\n  }\n  function resourceExportParams(options) {\n    const params = resourceListParams(options ?? {});\n    delete params.offset;\n    return params;\n  }\n  function reportRunParams(options) {\n    const params = {};\n    Object.entries(options?.filters ?? {}).forEach(([field, value]) => {\n      appendFilterParams(params, field, value);\n    });\n    setFilterExpressionParam(params, opt(options, \"filterExpression\", \"filter_expression\"));\n    setParam(params, \"order_by\", opt(options, \"orderBy\", \"order_by\"));\n    setParam(params, \"order\", options?.order);\n    setParam(params, \"limit\", options?.limit);\n    setParam(params, \"offset\", options?.offset);\n    return params;\n  }\n  function reportExportParams(options) {\n    const params = reportRunParams(options);\n    delete params.offset;\n    return params;\n  }\n  function calendarParams(options) {\n    const params = {};\n    setParam(params, \"from\", options?.from);\n    setParam(params, \"to\", options?.to);\n    setParam(params, \"limit\", options?.limit);\n    return params;\n  }\n  function webViewParams(options) {\n    const params = {};\n    setParam(params, \"limit\", options?.limit);\n    setParam(params, \"offset\", options?.offset);\n    return params;\n  }\n  function searchParams(q, options) {\n    const params = {};\n    setParam(params, \"q\", q);\n    setParam(params, \"limit\", options?.limit);\n    setParam(params, \"tenant\", options?.tenant);\n    return params;\n  }\n  function jobDashboardParams(options) {\n    const params = {};\n    setParam(params, \"job\", opt(options, \"jobName\", \"job\"));\n    setParam(params, \"run_id\", opt(options, \"runId\", \"run_id\"));\n    setParam(params, \"status\", options?.status);\n    setParam(params, \"limit\", options?.limit);\n    return params;\n  }\n  function jobScheduleParams(options) {\n    const params = {};\n    setParam(params, \"cron\", options?.cron);\n    setParam(params, \"job\", opt(options, \"jobName\", \"job\"));\n    return params;\n  }\n  function timelineParams(options) {\n    const params = {};\n    if (options && options.limit !== void 0 && options.limit !== null) {\n      params.limit = options.limit;\n    }\n    if (options && options.beforeSequence !== void 0 && options.beforeSequence !== null) {\n      params.before_sequence = options.beforeSequence;\n    } else if (options && options.before_sequence !== void 0 && options.before_sequence !== null) {\n      params.before_sequence = options.before_sequence;\n    }\n    return params;\n  }\n  function currentDeskListReturnTo(doctype) {\n    try {\n      const current = new URL(window.location.href);\n      return current.pathname === deskPath(doctype) ? current.pathname + current.search : void 0;\n    } catch {\n      return void 0;\n    }\n  }\n  function deskImportBody(doctype, csv, options) {\n    const body = new URLSearchParams();\n    const returnTo = options && options.returnTo !== void 0 ? options.returnTo : currentDeskListReturnTo(doctype);\n    setFormParam(body, \"mode\", options?.mode);\n    setFormParam(body, \"returnTo\", returnTo);\n    body.set(\"csv\", csv ?? \"\");\n    return body;\n  }\n  function deskBulkDocumentsBody(doctype, documents, options) {\n    const body = new URLSearchParams();\n    const returnTo = options && options.returnTo !== void 0 ? options.returnTo : currentDeskListReturnTo(doctype);\n    setFormParam(body, \"returnTo\", returnTo);\n    (documents ?? []).forEach((document2) => {\n      const name = typeof document2 === \"string\" ? document2 : document2?.name;\n      if (name === void 0 || name === null) {\n        return;\n      }\n      body.append(\"document\", String(name));\n      const expectedVersion = typeof document2 === \"string\" ? void 0 : document2.expectedVersion;\n      setFormParam(body, `expectedVersion:${String(name)}`, expectedVersion);\n    });\n    return body;\n  }\n\n  // src/adapters/desk/client-src/constants.ts\n  var CHILD_TABLE_ROW_INDEX_FIELD = \"__cf_frappe_row_index\";\n  var MIN_MULTIPART_FILE_PART_BYTES = 5 * 1024 * 1024;\n  var MAX_MULTIPART_FILE_PARTS = 1e4;\n  var LOCKED_VALUE_PROPERTY = \"__cfFrappeLockedValue\";\n  var READ_ONLY_PROPERTY = \"__cfFrappeReadOnly\";\n  var SOFT_DISABLED_PROPERTY = \"__cfFrappeSoftDisabled\";\n  var REALTIME_COLLABORATION_MESSAGE_TYPE = \"cf-frappe.realtime.collaboration\";\n  var FIELD_EDIT_MESSAGE_TYPE = \"cf-frappe.collaboration.field_edit\";\n  var SHARED_DRAFT_MESSAGE_TYPE = \"cf-frappe.collaboration.shared_draft\";\n\n  // src/adapters/desk/client-src/http.ts\n  var HttpRequestError = class extends Error {\n    status;\n    payload;\n    constructor(message, status, payload) {\n      super(message);\n      this.status = status;\n      this.payload = payload;\n    }\n  };\n  function isJsonBody(value) {\n    return value !== void 0 && typeof value !== \"string\" && !(value instanceof FormData) && !(value instanceof URLSearchParams) && !(value instanceof Blob);\n  }\n  function unwrapData(payload) {\n    return payload && Object.prototype.hasOwnProperty.call(payload, \"data\") ? payload.data : payload;\n  }\n  function requestInit(options) {\n    const init = options ?? {};\n    const headers = new Headers(init.headers ?? {});\n    let body = init.body;\n    if (isJsonBody(body)) {\n      headers.set(\"content-type\", \"application/json\");\n      body = JSON.stringify(body);\n    }\n    const result = Object.assign({}, init, {\n      credentials: init.credentials ?? \"same-origin\",\n      headers\n    });\n    if (body === void 0) {\n      delete result.body;\n    } else {\n      result.body = body;\n    }\n    return result;\n  }\n  async function readResponsePayload(response) {\n    const contentType = response.headers.get(\"content-type\") ?? \"\";\n    return contentType.indexOf(\"application/json\") >= 0 ? await response.json() : await response.text();\n  }\n  function throwResponseError(response, payload) {\n    const message = payload?.error?.message || response.statusText;\n    throw new HttpRequestError(message, response.status, payload);\n  }\n  async function request(path, options) {\n    const response = await fetch(path, requestInit(options));\n    const payload = await readResponsePayload(response);\n    if (!response.ok) {\n      throwResponseError(response, payload);\n    }\n    return payload;\n  }\n  async function requestBinary(path, options) {\n    const response = await fetch(path, requestInit(options));\n    if (!response.ok) {\n      throwResponseError(response, await readResponsePayload(response));\n    }\n    return response.arrayBuffer();\n  }\n  function deskDashboardPath(dashboard) {\n    return `/desk/dashboards/${encodePart(dashboard)}`;\n  }\n  function deskKanbanPath(kanban) {\n    return `/desk/kanbans/${encodePart(kanban)}`;\n  }\n  function deskCalendarPath(calendar, options) {\n    return withQuery(`/desk/calendars/${encodePart(calendar)}`, calendarParams(options ?? {}));\n  }\n  function webFormPublicPath(input) {\n    if (input && typeof input === \"object\") {\n      const form = input.form && typeof input.form === \"object\" ? input.form : input;\n      if (form.route !== void 0 && form.route !== null && String(form.route).trim()) {\n        return encodePath(form.route);\n      }\n      if (form.name !== void 0 && form.name !== null) {\n        return encodePart(form.name);\n      }\n    }\n    return encodePart(input);\n  }\n  function webFormPagePath(webForm) {\n    return `/web-forms/${webFormPublicPath(webForm)}`;\n  }\n  function webViewPagePath(webView) {\n    return `/web/${encodePart(webView)}`;\n  }\n  function webViewItemPagePath(webView, route) {\n    return `${webViewPagePath(webView)}/${encodePath(route)}`;\n  }\n  function webPagePath(route) {\n    return `/page/${encodePath(route)}`;\n  }\n  function deskAdminUsersPath(options) {\n    const params = {};\n    setParam(params, \"user\", options && (options.userId !== void 0 ? options.userId : options.user));\n    return withQuery(\"/desk/admin/users\", params);\n  }\n  function deskAdminCustomFieldsPath(doctype) {\n    const params = {};\n    setParam(params, \"doctype\", doctype);\n    return withQuery(\"/desk/admin/custom-fields\", params);\n  }\n  function deskAdminFieldPropertiesPath(doctype, field) {\n    const params = {};\n    setParam(params, \"doctype\", doctype);\n    setParam(params, \"field\", field);\n    return withQuery(\"/desk/admin/field-properties\", params);\n  }\n  function deskAdminUserPermissionsPath(options) {\n    const params = {};\n    setParam(params, \"user\", options && (options.userId !== void 0 ? options.userId : options.user));\n    return withQuery(\"/desk/admin/user-permissions\", params);\n  }\n  function deskAdminWorkflowsPath(doctype) {\n    const params = {};\n    setParam(params, \"doctype\", doctype);\n    return withQuery(\"/desk/admin/workflows\", params);\n  }\n  function deskAdminAssignmentRulesPath(doctype, rule) {\n    const params = {};\n    setParam(params, \"doctype\", doctype);\n    setParam(params, \"rule\", rule);\n    return withQuery(\"/desk/admin/assignment-rules\", params);\n  }\n  function deskFilesPath(options) {\n    return withQuery(\"/desk/files\", fileListParams(options ?? {}));\n  }\n  function deskFilePath(name, action) {\n    return `/desk/files/${encodePart(name)}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function deskNotificationsPath(options) {\n    return withQuery(\"/desk/notifications\", deskNotificationInboxParams(options ?? {}));\n  }\n  function deskSearchPath(q, options) {\n    return withQuery(\"/desk/search\", searchParams(q, options ?? {}));\n  }\n  function deskPrintPath(format, name) {\n    return `/desk/print/${encodePart(format)}/${encodePart(name)}`;\n  }\n  function deskPrintPdfPath(format, name) {\n    return `${deskPrintPath(format, name)}/pdf`;\n  }\n  function deskReportBuilderPath(doctype, id, options) {\n    const path = `/desk/report-builder/${encodePart(doctype)}${id === void 0 ? \"\" : `/${encodePart(id)}`}`;\n    return withQuery(path, reportRunParams(options ?? {}));\n  }\n  function deskReportBuilderPdfPath(doctype, id, options) {\n    return withQuery(`/desk/report-builder/${encodePart(doctype)}/${encodePart(id)}/pdf`, reportRunParams(options ?? {}));\n  }\n  function deskReportPath(report, options) {\n    return withQuery(`/desk/reports/${encodePart(report)}`, reportRunParams(options ?? {}));\n  }\n  function deskReportPdfPath(report, options) {\n    return withQuery(`/desk/reports/${encodePart(report)}/pdf`, reportRunParams(options ?? {}));\n  }\n  function deskWorkspacePath(workspace) {\n    return `/desk/workspaces/${encodePart(workspace)}`;\n  }\n  function resourceActionPath(doctype, name, action) {\n    return `${resourcePath(doctype, name)}/${action}`;\n  }\n  function resourceMemberPath(doctype, name, action, member) {\n    return `${resourceActionPath(doctype, name, action)}/${encodePart(member)}`;\n  }\n  function profilePath(userId, options) {\n    return withQuery(`/api/users/${encodePart(userId)}/profile`, tenantParams(options ?? {}));\n  }\n  function accountPath(userId, action, options) {\n    return withQuery(`/api/users/${encodePart(userId)}${action === void 0 ? \"\" : `/${action}`}`, tenantParams(options ?? {}));\n  }\n  function notificationActionPath(notificationId, action, options) {\n    return withQuery(`/api/notifications/${encodePart(notificationId)}/${action}`, notificationCommandParams(options ?? {}));\n  }\n  function notificationRulePath(doctype, rule, options) {\n    return withQuery(\n      `/api/notification-rules/${encodePart(doctype)}${rule === void 0 ? \"\" : `/${encodePart(rule)}`}`,\n      tenantParams(options ?? {})\n    );\n  }\n  function assignmentRulePath(doctype, rule, options) {\n    return withQuery(\n      `/api/assignment-rules/${encodePart(doctype)}${rule === void 0 ? \"\" : `/${encodePart(rule)}`}`,\n      tenantParams(options ?? {})\n    );\n  }\n  function assignmentRuleActionPath(doctype, rule, action, options) {\n    return withQuery(`/api/assignment-rules/${encodePart(doctype)}/${encodePart(rule)}/${action}`, tenantParams(options ?? {}));\n  }\n  function rolesPath(options) {\n    return withQuery(\"/api/roles\", tenantParams(options ?? {}));\n  }\n  function rolePath(role, options) {\n    return withQuery(`/api/roles/${encodePart(role)}`, tenantParams(options ?? {}));\n  }\n  function roleActionPath(role, action, options) {\n    return withQuery(`/api/roles/${encodePart(role)}/${action}`, tenantParams(options ?? {}));\n  }\n  function customFieldPath(doctype, field, options) {\n    return withQuery(\n      `/api/custom-fields/${encodePart(doctype)}${field === void 0 ? \"\" : `/${encodePart(field)}`}`,\n      tenantParams(options ?? {})\n    );\n  }\n  function fieldPropertyPath(doctype, field, options) {\n    return withQuery(\n      `/api/field-properties/${encodePart(doctype)}${field === void 0 ? \"\" : `/${encodePart(field)}`}`,\n      tenantParams(options ?? {})\n    );\n  }\n  function workflowPath(doctype, workflow, options) {\n    return withQuery(\n      `/api/workflows/${encodePart(doctype)}${workflow === void 0 ? \"\" : `/${encodePart(workflow)}`}`,\n      tenantParams(options ?? {})\n    );\n  }\n  function userPermissionPath(userId, options) {\n    return withQuery(`/api/user-permissions/${encodePart(userId)}`, tenantParams(options ?? {}));\n  }\n  function dataPatchPath(patchId, action) {\n    return `/api/data-patches${patchId === void 0 ? \"\" : `/${encodePart(patchId)}`}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function dashboardPath(dashboard, action) {\n    return `${dashboard === void 0 ? \"/api/meta/dashboards\" : `/api/dashboard/${encodePart(dashboard)}`}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function dashboardMetaPath(dashboard) {\n    return `/api/meta/dashboards${dashboard === void 0 ? \"\" : `/${encodePart(dashboard)}`}`;\n  }\n  function kanbanPath(kanban, action) {\n    return `${kanban === void 0 ? \"/api/meta/kanbans\" : `/api/kanban/${encodePart(kanban)}`}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function kanbanMetaPath(kanban) {\n    return `/api/meta/kanbans${kanban === void 0 ? \"\" : `/${encodePart(kanban)}`}`;\n  }\n  function calendarPath(calendar, action, options) {\n    return withQuery(\n      `${calendar === void 0 ? \"/api/meta/calendars\" : `/api/calendar/${encodePart(calendar)}`}${action === void 0 ? \"\" : `/${action}`}`,\n      calendarParams(options ?? {})\n    );\n  }\n  function calendarMetaPath(calendar) {\n    return `/api/meta/calendars${calendar === void 0 ? \"\" : `/${encodePart(calendar)}`}`;\n  }\n  function webFormPath(webForm, action) {\n    return `${webForm === void 0 ? \"/api/meta/web-forms\" : `/api/web-form/${encodePart(webForm)}`}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function webFormMetaPath(webForm) {\n    return `/api/meta/web-forms${webForm === void 0 ? \"\" : `/${encodePart(webForm)}`}`;\n  }\n  function webViewMetaPath(webView) {\n    return `/api/meta/web-views${webView === void 0 ? \"\" : `/${encodePart(webView)}`}`;\n  }\n  function webViewPath(webView, route, options) {\n    const path = `/api/web-view/${encodePart(webView)}${route === void 0 ? \"\" : `/${encodePath(route)}`}`;\n    return withQuery(path, webViewParams(options ?? {}));\n  }\n  function webPageMetaPath(webPage) {\n    return `/api/meta/web-pages${webPage === void 0 ? \"\" : `/${encodePart(webPage)}`}`;\n  }\n  function websiteThemeMetaPath(theme) {\n    return `/api/meta/website-themes${theme === void 0 ? \"\" : `/${encodePart(theme)}`}`;\n  }\n  function reportBuilderPath(doctype, id, action) {\n    return `/api/report-builder/${encodePart(doctype)}${id === void 0 ? \"\" : `/${encodePart(id)}`}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function reportPath(report, action) {\n    return `/api/report/${encodePart(report)}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function reportPdfPath(report, options) {\n    return withQuery(reportPath(report, \"pdf\"), reportRunParams(options ?? {}));\n  }\n  function reportBuilderPdfPath(doctype, id, options) {\n    return withQuery(reportBuilderPath(doctype, id, \"pdf\"), reportRunParams(options ?? {}));\n  }\n  function auditDeletedPath(doctype, name, options) {\n    return withQuery(`/api/audit/deleted/${encodePart(doctype)}/${encodePart(name)}`, tenantParams(options ?? {}));\n  }\n  function linkOptionsPath(doctype, field, params) {\n    return withQuery(`/api/link-options/${encodePart(doctype)}/${encodePart(field)}`, params ?? {});\n  }\n  function printDocumentPath(format, name) {\n    return `/api/print/${encodePart(format)}/${encodePart(name)}`;\n  }\n  function printPdfDocumentPath(format, name) {\n    return `${printDocumentPath(format, name)}/pdf`;\n  }\n  function printFormatPath(format) {\n    return `/api/meta/print-formats${format === void 0 ? \"\" : `/${encodePart(format)}`}`;\n  }\n  function printLetterheadPath(letterhead) {\n    return `/api/meta/print-letterheads${letterhead === void 0 ? \"\" : `/${encodePart(letterhead)}`}`;\n  }\n  function printSettingsPath(options) {\n    return withQuery(\"/api/print-settings\", tenantParams(options ?? {}));\n  }\n  function jobExecutionPath(idempotencyKey, action) {\n    return `/api/jobs/executions/${encodePart(idempotencyKey)}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n  function jobSchedulePath(scheduleId, action) {\n    return `/api/jobs/schedules${scheduleId === void 0 ? \"\" : `/${encodePart(scheduleId)}`}${action === void 0 ? \"\" : `/${action}`}`;\n  }\n\n  // src/adapters/desk/client-src/topics.ts\n  function documentTopic(tenantId, doctype, name) {\n    return `document:${encodePart(tenantId)}:${encodePart(doctype)}:${encodePart(name)}`;\n  }\n  function doctypeTopic(tenantId, doctype) {\n    return `doctype:${encodePart(tenantId)}:${encodePart(doctype)}`;\n  }\n  function tenantTopic(tenantId) {\n    return `tenant:${encodePart(tenantId)}`;\n  }\n  function userTopic(tenantId, userId) {\n    return `user:${encodePart(tenantId)}:${encodePart(userId)}`;\n  }\n  function tenantIdFromOptions(options, label) {\n    const tenantId = options && (options.tenantId || options.document && options.document.tenantId);\n    if (!tenantId) {\n      throw new Error(`tenantId is required for ${label} realtime subscriptions`);\n    }\n    return tenantId;\n  }\n  function doctypeTopicFromOptions(doctype, options) {\n    return doctypeTopic(tenantIdFromOptions(options, \"doctype\"), doctype);\n  }\n  function documentTopicFromOptions(doctype, name, options) {\n    return documentTopic(tenantIdFromOptions(options, \"document\"), doctype, name);\n  }\n  function tenantTopicFromOptions(options) {\n    return tenantTopic(tenantIdFromOptions(options, \"tenant\"));\n  }\n  function userTopicFromOptions(userId, options) {\n    const resolvedUserId = userId || options?.userId;\n    if (!resolvedUserId) {\n      throw new Error(\"userId is required for user realtime subscriptions\");\n    }\n    return userTopic(tenantIdFromOptions(options, \"user\"), resolvedUserId);\n  }\n\n  // src/adapters/desk/client-src/namespace.ts\n  function msgprint(message) {\n    const text = message == null ? \"\" : String(message);\n    if (typeof window.alert === \"function\") {\n      window.alert(text);\n    }\n    return text;\n  }\n  function throwMessage(message) {\n    const text = msgprint(message);\n    throw new Error(text);\n  }\n  async function getNotificationRule(doctype, rule, options) {\n    const state = unwrapData(await request(notificationRulePath(doctype, rule, options ?? {})));\n    return notificationRuleEntry(rule, state);\n  }\n  async function getAssignmentRule(doctype, rule, options) {\n    const state = unwrapData(await request(assignmentRulePath(doctype, rule, options ?? {})));\n    return assignmentRuleEntry(rule, state);\n  }\n  async function toggleNotificationRule(doctype, rule, enabled, options) {\n    const commandOptions = options ?? {};\n    const state = unwrapData(await request(notificationRulePath(doctype, void 0, commandOptions)));\n    return request(notificationRulePath(doctype, rule, commandOptions), {\n      method: \"PUT\",\n      body: notificationRuleToggleBody(rule, state, enabled, commandOptions)\n    }).then(unwrapData);\n  }\n  async function toggleAssignmentRule(doctype, rule, enabled, options) {\n    const commandOptions = options ?? {};\n    return request(assignmentRuleActionPath(doctype, rule, enabled ? \"enable\" : \"disable\", commandOptions), {\n      method: \"POST\",\n      body: versionBody(commandOptions)\n    }).then(unwrapData);\n  }\n  var FORM_URLENCODED_HEADERS = { \"content-type\": \"application/x-www-form-urlencoded; charset=utf-8\" };\n  function coreFilesApi() {\n    return {\n      bulkDelete: (files) => request(\"/api/files/delete\", { method: \"POST\", body: bulkFilesBody(files) }).then(unwrapData),\n      bulkUpdateMetadata: (files, input) => request(\"/api/files/bulk-metadata\", { method: \"POST\", body: bulkFilesBody(files, input) }).then(unwrapData),\n      contentUrl: (name) => filePath(name, \"content\"),\n      delete: (name, options) => request(withQuery(filePath(name), versionBody(options)), { method: \"DELETE\" }).then(unwrapData),\n      generateRendition: (name, options) => request(filePath(name, \"renditions\"), { method: \"POST\", body: options ?? {} }),\n      list: (options) => request(withQuery(\"/api/files\", fileListParams(options ?? {}))).then(unwrapData),\n      previewUrl: (name) => filePath(name, \"preview\"),\n      renditionContentUrl: (name, renditionId) => filePath(name, `renditions/${encodePart(renditionId)}/content`),\n      transformUrl: (name, options) => withQuery(filePath(name, \"transform\"), fileTransformParams(options ?? {})),\n      updateMetadata: (name, input, options) => request(filePath(name), { method: \"PATCH\", body: commandBody(input, options) }).then(unwrapData)\n    };\n  }\n  function buildNamespace(extensions) {\n    const ext = extensions ?? {};\n    const namespace = {\n      context: pageContext,\n      audit: Object.freeze({\n        deleted: (doctype, name, options) => request(auditDeletedPath(doctype, name, options ?? {})).then(unwrapData),\n        events: (options) => request(withQuery(\"/api/audit/events\", auditEventParams(options ?? {}))).then(unwrapData)\n      }),\n      auth: Object.freeze({\n        completeEmailVerification: (input) => request(\"/api/auth/email-verification/complete\", { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        completePasswordReset: (input) => request(\"/api/auth/password-reset/complete\", { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        login: (input) => request(\"/api/auth/login\", { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        logout: () => request(\"/api/auth/logout\", { method: \"POST\" }).then(unwrapData),\n        me: () => request(\"/api/auth/me\").then(unwrapData),\n        requestEmailVerification: (input) => request(\"/api/auth/email-verification/request\", { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        requestPasswordReset: (input) => request(\"/api/auth/password-reset/request\", { method: \"POST\", body: input ?? {} }).then(unwrapData)\n      }),\n      accounts: Object.freeze({\n        changePassword: (userId, input, options) => request(accountPath(userId, \"password\", options ?? {}), { method: \"PUT\", body: passwordBody(input, options) }).then(unwrapData),\n        changeRoles: (userId, input, options) => request(accountPath(userId, \"roles\", options ?? {}), { method: \"PUT\", body: rolesBody(input, options) }).then(unwrapData),\n        create: (userId, input, options) => request(accountPath(userId, void 0, options ?? {}), { method: \"POST\", body: commandBody(input ?? {}, options) }).then(unwrapData),\n        disable: (userId, options) => request(accountPath(userId, \"disable\", options ?? {}), { method: \"POST\", body: versionBody(options) }).then(unwrapData),\n        enable: (userId, options) => request(accountPath(userId, \"enable\", options ?? {}), { method: \"POST\", body: versionBody(options) }).then(unwrapData),\n        get: (userId, options) => request(accountPath(userId, void 0, options ?? {})).then(unwrapData),\n        syncProvider: (userId, input, options) => request(accountPath(userId, \"provider-sync\", options ?? {}), { method: \"POST\", body: commandBody(input, options) }).then(unwrapData)\n      }),\n      linkOptions: (doctype, field, params) => request(linkOptionsPath(doctype, field, params)).then(unwrapData),\n      search: (q, options) => request(withQuery(\"/api/search\", searchParams(q, options ?? {}))).then(unwrapData),\n      customFields: Object.freeze({\n        disable: (doctype, field, options) => request(customFieldPath(doctype, field, options ?? {}), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        list: (doctype, options) => request(customFieldPath(doctype, void 0, options ?? {})).then(unwrapData),\n        save: (doctype, field, options) => request(customFieldPath(doctype, void 0, options ?? {}), { method: \"POST\", body: customFieldBody(field, options) }).then(unwrapData)\n      }),\n      fieldProperties: Object.freeze({\n        clear: (doctype, field, options) => request(fieldPropertyPath(doctype, field, options ?? {}), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        list: (doctype, options) => request(fieldPropertyPath(doctype, void 0, options ?? {})).then(unwrapData),\n        save: (doctype, field, overrides, options) => request(fieldPropertyPath(doctype, field, options ?? {}), { method: \"PUT\", body: fieldPropertyBody(overrides, options) }).then(unwrapData)\n      }),\n      workflows: Object.freeze({\n        clear: (doctype, workflow, options) => request(workflowPath(doctype, workflow, options ?? {}), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        get: (doctype, workflow, options) => request(workflowPath(doctype, workflow, options ?? {})).then(unwrapData),\n        list: (doctype, options) => request(workflowPath(doctype, void 0, options ?? {})).then(unwrapData),\n        save: (doctype, workflow, options) => request(workflowPath(doctype, workflow && workflow.name, options ?? {}), {\n          method: \"PUT\",\n          body: workflowBody(workflow, options)\n        }).then(unwrapData)\n      }),\n      userPermissions: Object.freeze({\n        allow: (userId, grant, options) => request(userPermissionPath(userId, options ?? {}), { method: \"POST\", body: userPermissionBody(grant, options) }).then(unwrapData),\n        get: (userId, options) => request(userPermissionPath(userId, options ?? {})).then(unwrapData),\n        revoke: (userId, grant, options) => request(userPermissionPath(userId, options ?? {}), { method: \"DELETE\", body: userPermissionBody(grant, options) }).then(unwrapData)\n      }),\n      dataPatches: Object.freeze({\n        apply: (options) => request(dataPatchPath(void 0, \"apply\"), { method: \"POST\", body: dataPatchBody(options) }).then(unwrapData),\n        applyOne: (patchId) => request(dataPatchPath(patchId, \"apply\"), { method: \"POST\" }).then(unwrapData),\n        enqueue: (options) => request(dataPatchPath(void 0, \"enqueue\"), { method: \"POST\", body: dataPatchBody(options) }).then(unwrapData),\n        enqueueOne: (patchId, options) => request(dataPatchPath(patchId, \"enqueue\"), { method: \"POST\", body: dataPatchBody(options, false) }).then(unwrapData),\n        plan: (options) => request(dataPatchPath(void 0, \"plan\"), { method: \"POST\", body: dataPatchBody(options) }).then(unwrapData),\n        planOne: (patchId) => request(dataPatchPath(patchId, \"plan\"), { method: \"POST\" }).then(unwrapData),\n        rollbackPlan: (options) => request(dataPatchPath(void 0, \"rollback-plan\"), { method: \"POST\", body: dataPatchBody(options) }).then(unwrapData),\n        rollbackPlanOne: (patchId) => request(dataPatchPath(patchId, \"rollback-plan\"), { method: \"POST\" }).then(unwrapData),\n        rollback: (options) => request(dataPatchPath(void 0, \"rollback\"), { method: \"POST\", body: dataPatchBody(options) }).then(unwrapData),\n        rollbackOne: (patchId) => request(dataPatchPath(patchId, \"rollback\"), { method: \"POST\" }).then(unwrapData),\n        rollbackEnqueue: (options) => request(dataPatchPath(void 0, \"rollback-enqueue\"), { method: \"POST\", body: dataPatchBody(options) }).then(unwrapData),\n        rollbackEnqueueOne: (patchId, options) => request(dataPatchPath(patchId, \"rollback-enqueue\"), { method: \"POST\", body: dataPatchBody(options, false) }).then(unwrapData),\n        rollbackRetry: (patchId) => request(dataPatchPath(patchId, \"rollback-retry\"), { method: \"POST\" }).then(unwrapData),\n        rollbackRetryEnqueue: (patchId, options) => request(dataPatchPath(patchId, \"rollback-retry-enqueue\"), { method: \"POST\", body: dataPatchBody(options, false) }).then(unwrapData),\n        retry: (patchId) => request(dataPatchPath(patchId, \"retry\"), { method: \"POST\" }).then(unwrapData),\n        status: () => request(dataPatchPath()).then(unwrapData)\n      }),\n      dashboard: Object.freeze({\n        get: (dashboard) => request(dashboardMetaPath(dashboard)).then(unwrapData),\n        list: () => request(dashboardMetaPath()).then(unwrapData),\n        run: (dashboard) => request(dashboardPath(dashboard, \"run\")).then(unwrapData)\n      }),\n      kanban: Object.freeze({\n        get: (kanban) => request(kanbanMetaPath(kanban)).then(unwrapData),\n        list: () => request(kanbanMetaPath()).then(unwrapData),\n        run: (kanban) => request(kanbanPath(kanban, \"run\")).then(unwrapData)\n      }),\n      calendar: Object.freeze({\n        get: (calendar) => request(calendarMetaPath(calendar)).then(unwrapData),\n        list: () => request(calendarMetaPath()).then(unwrapData),\n        run: (calendar, options) => request(calendarPath(calendar, \"run\", options ?? {})).then(unwrapData)\n      }),\n      webForm: Object.freeze({\n        get: (webForm) => request(webFormMetaPath(webForm)).then(unwrapData),\n        list: () => request(webFormMetaPath()).then(unwrapData),\n        submit: (webForm, data) => request(webFormPath(webForm, \"submit\"), { method: \"POST\", body: { data: data ?? {} } }).then(unwrapData),\n        url: webFormPagePath\n      }),\n      webView: Object.freeze({\n        get: (webView) => request(webViewMetaPath(webView)).then(unwrapData),\n        item: (webView, route) => request(webViewPath(webView, route)).then(unwrapData),\n        itemUrl: webViewItemPagePath,\n        items: (webView, options) => request(webViewPath(webView, void 0, options ?? {})).then(unwrapData),\n        list: () => request(webViewMetaPath()).then(unwrapData),\n        url: webViewPagePath\n      }),\n      webPage: Object.freeze({\n        get: (webPage) => request(webPageMetaPath(webPage)).then(unwrapData),\n        list: () => request(webPageMetaPath()).then(unwrapData),\n        url: webPagePath\n      }),\n      websiteSettings: Object.freeze({\n        get: () => request(\"/api/meta/website-settings\").then(unwrapData)\n      }),\n      websiteTheme: Object.freeze({\n        get: (theme) => request(websiteThemeMetaPath(theme)).then(unwrapData),\n        list: () => request(websiteThemeMetaPath()).then(unwrapData)\n      }),\n      jobs: Object.freeze({\n        createSchedule: (input) => request(jobSchedulePath(), { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        dashboard: (options) => request(withQuery(\"/api/jobs\", jobDashboardParams(options ?? {}))).then(unwrapData),\n        deleteSchedule: (scheduleId) => request(jobSchedulePath(scheduleId), { method: \"DELETE\" }).then(unwrapData),\n        disableSchedule: (scheduleId) => request(jobSchedulePath(scheduleId, \"disable\"), { method: \"POST\" }).then(unwrapData),\n        enableSchedule: (scheduleId) => request(jobSchedulePath(scheduleId, \"enable\"), { method: \"POST\" }).then(unwrapData),\n        execution: (idempotencyKey) => request(jobExecutionPath(idempotencyKey)).then(unwrapData),\n        pauseSchedule: (scheduleId, pausedUntil) => request(jobSchedulePath(scheduleId, \"pause\"), { method: \"POST\", body: { pauseUntil: pausedUntil } }).then(unwrapData),\n        resetSchedule: (scheduleId) => request(jobSchedulePath(scheduleId, \"reset\"), { method: \"POST\" }).then(unwrapData),\n        retry: (idempotencyKey) => request(jobExecutionPath(idempotencyKey, \"retry\"), { method: \"POST\" }).then(unwrapData),\n        runSchedule: (scheduleId) => request(jobSchedulePath(scheduleId, \"run\"), { method: \"POST\" }).then(unwrapData),\n        schedules: (options) => request(withQuery(jobSchedulePath(), jobScheduleParams(options ?? {}))).then(unwrapData),\n        updateSchedule: (scheduleId, input) => request(jobSchedulePath(scheduleId), { method: \"PUT\", body: input ?? {} }).then(unwrapData)\n      }),\n      history: Object.freeze({\n        assignments: (doctype, name) => request(resourceActionPath(doctype, name, \"assignments\")).then(unwrapData),\n        followers: (doctype, name) => request(resourceActionPath(doctype, name, \"followers\")).then(unwrapData),\n        shares: (doctype, name) => request(resourceActionPath(doctype, name, \"shares\")).then(unwrapData),\n        tags: (doctype, name) => request(resourceActionPath(doctype, name, \"tags\")).then(unwrapData),\n        timeline: (doctype, name, options) => request(withQuery(resourceActionPath(doctype, name, \"timeline\"), timelineParams(options ?? {}))).then(unwrapData)\n      }),\n      files: Object.freeze(Object.assign(coreFilesApi(), ext.files ?? {})),\n      meta: Object.freeze({\n        customFields: (doctype, options) => request(customFieldPath(doctype, void 0, options ?? {})).then(unwrapData),\n        dashboard: (dashboard) => request(dashboardMetaPath(dashboard)).then(unwrapData),\n        dashboards: () => request(dashboardMetaPath()).then(unwrapData),\n        kanban: (kanban) => request(kanbanMetaPath(kanban)).then(unwrapData),\n        kanbans: () => request(kanbanMetaPath()).then(unwrapData),\n        calendar: (calendar) => request(calendarMetaPath(calendar)).then(unwrapData),\n        calendars: () => request(calendarMetaPath()).then(unwrapData),\n        webForm: (webForm) => request(webFormMetaPath(webForm)).then(unwrapData),\n        webForms: () => request(webFormMetaPath()).then(unwrapData),\n        webView: (webView) => request(webViewMetaPath(webView)).then(unwrapData),\n        webViews: () => request(webViewMetaPath()).then(unwrapData),\n        webPage: (webPage) => request(webPageMetaPath(webPage)).then(unwrapData),\n        webPages: () => request(webPageMetaPath()).then(unwrapData),\n        websiteSettings: () => request(\"/api/meta/website-settings\").then(unwrapData),\n        websiteTheme: (theme) => request(websiteThemeMetaPath(theme)).then(unwrapData),\n        websiteThemes: () => request(websiteThemeMetaPath()).then(unwrapData),\n        doctype: (doctype) => request(`/api/meta/doctypes/${encodePart(doctype)}`).then(unwrapData),\n        doctypes: () => request(\"/api/meta/doctypes\").then(unwrapData),\n        fieldProperties: (doctype, options) => request(fieldPropertyPath(doctype, void 0, options ?? {})).then(unwrapData),\n        listView: (doctype) => request(`/api/meta/doctypes/${encodePart(doctype)}/list-view`).then(unwrapData),\n        linkOptions: (doctype, field, params) => request(linkOptionsPath(doctype, field, params)).then(unwrapData),\n        notificationRules: (doctype, options) => request(notificationRulePath(doctype, void 0, options ?? {})).then(unwrapData),\n        assignmentRules: (doctype, options) => request(assignmentRulePath(doctype, void 0, options ?? {})).then(unwrapData),\n        profile: (userId, options) => request(profilePath(userId, options ?? {})).then(unwrapData),\n        printFormat: (format) => request(printFormatPath(format)).then(unwrapData),\n        printFormats: (options) => request(withQuery(printFormatPath(), printFormatParams(options ?? {}))).then(unwrapData),\n        printLetterhead: (letterhead) => request(printLetterheadPath(letterhead)).then(unwrapData),\n        printLetterheads: () => request(printLetterheadPath()).then(unwrapData),\n        report: (report) => request(`/api/meta/reports/${encodePart(report)}`).then(unwrapData),\n        reports: () => request(\"/api/meta/reports\").then(unwrapData),\n        role: (role, options) => request(rolePath(role, options ?? {})).then(unwrapData),\n        roles: (options) => request(rolesPath(options ?? {})).then(unwrapData),\n        userPermissions: (userId, options) => request(userPermissionPath(userId, options ?? {})).then(unwrapData),\n        workflow: (doctype, workflow, options) => request(workflowPath(doctype, workflow, options ?? {})).then(unwrapData),\n        workflows: (doctype, options) => request(workflowPath(doctype, void 0, options ?? {})).then(unwrapData),\n        workspace: (workspace) => request(`/api/meta/workspaces/${encodePart(workspace)}`).then(unwrapData),\n        workspaces: () => request(\"/api/meta/workspaces\").then(unwrapData)\n      }),\n      notifications: Object.freeze({\n        dismiss: (notificationId, options) => request(notificationActionPath(notificationId, \"dismiss\", options ?? {}), { method: \"POST\" }).then(unwrapData),\n        inbox: (options) => request(withQuery(\"/api/notifications\", notificationInboxParams(options ?? {}))).then(unwrapData),\n        markRead: (notificationId, options) => request(notificationActionPath(notificationId, \"read\", options ?? {}), { method: \"POST\" }).then(unwrapData)\n      }),\n      notificationRules: Object.freeze({\n        clear: (doctype, rule, options) => request(notificationRulePath(doctype, rule, options ?? {}), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        disable: (doctype, rule, options) => toggleNotificationRule(doctype, rule, false, options),\n        enable: (doctype, rule, options) => toggleNotificationRule(doctype, rule, true, options),\n        get: (doctype, rule, options) => getNotificationRule(doctype, rule, options),\n        list: (doctype, options) => request(notificationRulePath(doctype, void 0, options ?? {})).then(unwrapData),\n        save: (doctype, rule, options) => request(notificationRulePath(doctype, rule.name, options ?? {}), {\n          method: \"PUT\",\n          body: notificationRuleBody(rule, options)\n        }).then(unwrapData)\n      }),\n      assignmentRules: Object.freeze({\n        clear: (doctype, rule, options) => request(assignmentRulePath(doctype, rule, options ?? {}), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        disable: (doctype, rule, options) => toggleAssignmentRule(doctype, rule, false, options),\n        enable: (doctype, rule, options) => toggleAssignmentRule(doctype, rule, true, options),\n        get: (doctype, rule, options) => getAssignmentRule(doctype, rule, options),\n        list: (doctype, options) => request(assignmentRulePath(doctype, void 0, options ?? {})).then(unwrapData),\n        save: (doctype, rule, options) => request(assignmentRulePath(doctype, rule.name, options ?? {}), {\n          method: \"PUT\",\n          body: assignmentRuleBody(rule, options)\n        }).then(unwrapData)\n      }),\n      profiles: Object.freeze({\n        get: (userId, options) => request(profilePath(userId, options ?? {})).then(unwrapData),\n        update: (userId, input, options) => request(profilePath(userId, options ?? {}), { method: \"PUT\", body: commandBody(input, options) }).then(unwrapData)\n      }),\n      print: Object.freeze({\n        format: (format) => request(printFormatPath(format)).then(unwrapData),\n        formats: (options) => request(withQuery(printFormatPath(), printFormatParams(options ?? {}))).then(unwrapData),\n        letterhead: (letterhead) => request(printLetterheadPath(letterhead)).then(unwrapData),\n        letterheads: () => request(printLetterheadPath()).then(unwrapData),\n        html: (format, name) => request(printDocumentPath(format, name)),\n        pdf: (format, name) => requestBinary(printPdfDocumentPath(format, name)),\n        pdfUrl: printPdfDocumentPath,\n        settings: (options) => request(printSettingsPath(options ?? {})).then(unwrapData),\n        updateSettings: (input, options) => request(printSettingsPath(options ?? {}), {\n          method: \"PUT\",\n          body: Object.assign({}, input ?? {}, versionBody(options ?? {}))\n        }).then(unwrapData),\n        url: printDocumentPath\n      }),\n      report: Object.freeze({\n        csvUrl: (report, options) => withQuery(reportPath(report, \"export.csv\"), reportExportParams(options ?? {})),\n        get: (report) => request(`/api/meta/reports/${encodePart(report)}`).then(unwrapData),\n        list: () => request(\"/api/meta/reports\").then(unwrapData),\n        pdf: (report, options) => requestBinary(reportPdfPath(report, options ?? {})),\n        pdfUrl: reportPdfPath,\n        run: (report, options) => request(withQuery(reportPath(report, \"run\"), reportRunParams(options ?? {})))\n      }),\n      reportBuilder: Object.freeze({\n        create: (doctype, input) => request(reportBuilderPath(doctype), { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        csvUrl: (doctype, id, options) => withQuery(reportBuilderPath(doctype, id, \"export.csv\"), reportExportParams(options ?? {})),\n        delete: (doctype, id) => request(reportBuilderPath(doctype, id), { method: \"DELETE\" }).then(unwrapData),\n        get: (doctype, id) => request(reportBuilderPath(doctype, id)).then(unwrapData),\n        list: (doctype) => request(reportBuilderPath(doctype)).then(unwrapData),\n        pdf: (doctype, id, options) => requestBinary(reportBuilderPdfPath(doctype, id, options ?? {})),\n        pdfUrl: reportBuilderPdfPath,\n        run: (doctype, id, options) => request(withQuery(reportBuilderPath(doctype, id, \"run\"), reportRunParams(options ?? {}))),\n        update: (doctype, id, input) => request(reportBuilderPath(doctype, id), { method: \"PUT\", body: input ?? {} }).then(unwrapData)\n      }),\n      roles: Object.freeze({\n        changeDescription: (role, input, options) => request(roleActionPath(role, \"description\", options ?? {}), { method: \"PUT\", body: descriptionBody(input, options) }).then(unwrapData),\n        create: (role, input, options) => request(rolePath(role, options ?? {}), { method: \"POST\", body: commandBody(input ?? {}, options) }).then(unwrapData),\n        disable: (role, options) => request(roleActionPath(role, \"disable\", options ?? {}), { method: \"POST\", body: versionBody(options) }).then(unwrapData),\n        enable: (role, options) => request(roleActionPath(role, \"enable\", options ?? {}), { method: \"POST\", body: versionBody(options) }).then(unwrapData),\n        get: (role, options) => request(rolePath(role, options ?? {})).then(unwrapData),\n        list: (options) => request(rolesPath(options ?? {})).then(unwrapData)\n      }),\n      request,\n      msgprint,\n      throw: throwMessage,\n      ui: Object.freeze({\n        msgprint\n      }),\n      desk: Object.freeze({\n        adminAssignmentRulesUrl: deskAdminAssignmentRulesPath,\n        adminCustomFieldsUrl: deskAdminCustomFieldsPath,\n        adminDataPatchesUrl: () => \"/desk/admin/data-patches\",\n        adminFieldPropertiesUrl: deskAdminFieldPropertiesPath,\n        adminJobsUrl: (options) => withQuery(\"/desk/admin/jobs\", jobDashboardParams(options ?? {})),\n        adminJobSchedulesUrl: (options) => withQuery(\"/desk/admin/jobs/schedules\", jobScheduleParams(options ?? {})),\n        adminPrintSettingsUrl: () => \"/desk/admin/print-settings\",\n        adminRolesUrl: () => \"/desk/admin/roles\",\n        adminUserPermissionsUrl: deskAdminUserPermissionsPath,\n        adminUsersUrl: deskAdminUsersPath,\n        adminWorkflowsUrl: deskAdminWorkflowsPath,\n        dashboardUrl: deskDashboardPath,\n        kanbanUrl: deskKanbanPath,\n        calendarUrl: deskCalendarPath,\n        webFormUrl: webFormPagePath,\n        webViewItemUrl: webViewItemPagePath,\n        webViewUrl: webViewPagePath,\n        webPageUrl: webPagePath,\n        fileContentUrl: (name) => deskFilePath(name, \"content\"),\n        filesUrl: deskFilesPath,\n        filePreviewUrl: (name) => deskFilePath(name, \"preview\"),\n        listUrl: (doctype, options) => withQuery(deskPath(doctype), resourceListParams(options ?? {})),\n        csvUrl: (doctype, options) => withQuery(`${deskPath(doctype)}/export.csv`, resourceExportParams(options ?? {})),\n        formUrl: (doctype, name) => `${deskPath(doctype)}/${encodePart(name)}`,\n        importTemplateCsvUrl: (doctype) => `${deskPath(doctype)}/import-template.csv`,\n        notificationsUrl: deskNotificationsPath,\n        printingUrl: () => \"/desk/printing\",\n        printingFormatUrl: (format) => `/desk/printing/formats/${encodePart(format)}`,\n        printingLetterheadUrl: (letterhead) => `/desk/printing/letterheads/${encodePart(letterhead)}`,\n        printPdfUrl: deskPrintPdfPath,\n        printUrl: deskPrintPath,\n        reportBuilderUrl: deskReportBuilderPath,\n        reportBuilderPdfUrl: deskReportBuilderPdfPath,\n        reportPdfUrl: deskReportPdfPath,\n        reportUrl: deskReportPath,\n        searchUrl: deskSearchPath,\n        workspaceUrl: deskWorkspacePath,\n        importCsv: (doctype, csv, options) => request(`${deskPath(doctype)}/import.csv`, {\n          method: \"POST\",\n          headers: FORM_URLENCODED_HEADERS,\n          body: deskImportBody(doctype, csv, options ?? {})\n        }),\n        bulkDelete: (doctype, documents, options) => request(`${deskPath(doctype)}/bulk-delete`, {\n          method: \"POST\",\n          headers: FORM_URLENCODED_HEADERS,\n          body: deskBulkDocumentsBody(doctype, documents, options ?? {})\n        }),\n        bulkSubmit: (doctype, documents, options) => request(`${deskPath(doctype)}/bulk-submit`, {\n          method: \"POST\",\n          headers: FORM_URLENCODED_HEADERS,\n          body: deskBulkDocumentsBody(doctype, documents, options ?? {})\n        }),\n        bulkCancel: (doctype, documents, options) => request(`${deskPath(doctype)}/bulk-cancel`, {\n          method: \"POST\",\n          headers: FORM_URLENCODED_HEADERS,\n          body: deskBulkDocumentsBody(doctype, documents, options ?? {})\n        }),\n        bulkTransition: (doctype, workflow, action, documents, options) => request(`${deskPath(doctype)}/workflows/${encodePart(workflow)}/bulk-transition/${encodePart(action)}`, {\n          method: \"POST\",\n          headers: FORM_URLENCODED_HEADERS,\n          body: deskBulkDocumentsBody(doctype, documents, options ?? {})\n        }),\n        newUrl: (doctype) => `${deskPath(doctype)}/new`\n      }),\n      resource: Object.freeze({\n        activity: (doctype, name, input, options) => request(resourceActionPath(doctype, name, \"activities\"), { method: \"POST\", body: commandBody(input, options) }).then(unwrapData),\n        assign: (doctype, name, assignee, options) => request(resourceActionPath(doctype, name, \"assignments\"), {\n          method: \"POST\",\n          body: Object.assign({ assignee }, versionBody(options))\n        }).then(unwrapData),\n        assignments: (doctype, name) => request(resourceActionPath(doctype, name, \"assignments\")).then(unwrapData),\n        bulkCancel: (doctype, documents) => request(`${resourcePath(doctype)}/bulk-cancel`, { method: \"POST\", body: bulkDocumentsBody(documents) }).then(unwrapData),\n        bulkDelete: (doctype, documents) => request(`${resourcePath(doctype)}/delete`, { method: \"POST\", body: bulkDocumentsBody(documents) }).then(unwrapData),\n        bulkSubmit: (doctype, documents) => request(`${resourcePath(doctype)}/bulk-submit`, { method: \"POST\", body: bulkDocumentsBody(documents) }).then(unwrapData),\n        bulkTransition: (doctype, workflow, action, documents) => request(`${resourcePath(doctype)}/workflows/${encodePart(workflow)}/bulk-transition/${encodePart(action)}`, {\n          method: \"POST\",\n          body: bulkDocumentsBody(documents)\n        }).then(unwrapData),\n        amend: (doctype, name, input, options) => request(`${resourcePath(doctype, name)}/amend`, { method: \"POST\", body: commandBody(input ?? {}, options) }).then(unwrapData),\n        cancel: (doctype, name, options) => request(`${resourcePath(doctype, name)}/cancel`, { method: \"POST\", body: versionBody(options) }).then(unwrapData),\n        command: (doctype, name, command, input, options) => request(`${resourcePath(doctype, name)}/command/${encodePart(command)}`, {\n          method: \"POST\",\n          body: commandBody(input, options)\n        }).then(unwrapData),\n        comment: (doctype, name, input, options) => request(resourceActionPath(doctype, name, \"comments\"), { method: \"POST\", body: commentBody(input, options) }).then(unwrapData),\n        create: (doctype, data) => request(resourcePath(doctype), { method: \"POST\", body: data ?? {} }).then(unwrapData),\n        csvUrl: (doctype, options) => withQuery(`${resourcePath(doctype)}/export.csv`, resourceExportParams(options ?? {})),\n        importTemplateCsvUrl: (doctype) => `${resourcePath(doctype)}/import-template.csv`,\n        importCsv: (doctype, csv, options) => {\n          const params = {};\n          setParam(params, \"mode\", options?.mode);\n          setParam(\n            params,\n            \"max_rows\",\n            options && (options.maxRows !== void 0 ? options.maxRows : options.max_rows)\n          );\n          return request(withQuery(`${resourcePath(doctype)}/import.csv`, params), {\n            method: \"POST\",\n            headers: { \"content-type\": \"text/csv; charset=utf-8\" },\n            body: csv ?? \"\"\n          }).then(unwrapData);\n        },\n        delete: (doctype, name, options) => request(resourcePath(doctype, name), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        deleteSavedFilter: (doctype, filterId) => request(`${resourcePath(doctype)}/saved-filters/${encodePart(filterId)}`, { method: \"DELETE\" }).then(unwrapData),\n        duplicate: (doctype, name, input, options) => request(`${resourcePath(doctype, name)}/duplicate`, { method: \"POST\", body: commandBody(input ?? {}, options) }).then(unwrapData),\n        follow: (doctype, name, options) => request(resourceActionPath(doctype, name, \"followers\"), { method: \"POST\", body: commandBody(options ?? {}, options) }).then(unwrapData),\n        followers: (doctype, name) => request(resourceActionPath(doctype, name, \"followers\")).then(unwrapData),\n        get: (doctype, name) => request(resourcePath(doctype, name)).then(unwrapData),\n        list: (doctype, options) => request(withQuery(resourcePath(doctype), resourceListParams(options ?? {}))),\n        listSavedFilters: (doctype) => request(`${resourcePath(doctype)}/saved-filters`).then(unwrapData),\n        saveFilter: (doctype, input) => request(`${resourcePath(doctype)}/saved-filters`, { method: \"POST\", body: savedFilterBody(input ?? {}) }).then(unwrapData),\n        share: (doctype, name, userId, permissions, options) => request(resourceActionPath(doctype, name, \"shares\"), {\n          method: \"POST\",\n          body: Object.assign({ userId, permissions: permissions ?? [\"read\"] }, versionBody(options))\n        }).then(unwrapData),\n        shares: (doctype, name) => request(resourceActionPath(doctype, name, \"shares\")).then(unwrapData),\n        submit: (doctype, name, options) => request(`${resourcePath(doctype, name)}/submit`, { method: \"POST\", body: versionBody(options) }).then(unwrapData),\n        tag: (doctype, name, tag, options) => request(resourceActionPath(doctype, name, \"tags\"), {\n          method: \"POST\",\n          body: Object.assign({ tag }, versionBody(options))\n        }).then(unwrapData),\n        tags: (doctype, name) => request(resourceActionPath(doctype, name, \"tags\")).then(unwrapData),\n        timeline: (doctype, name, options) => request(withQuery(resourceActionPath(doctype, name, \"timeline\"), timelineParams(options ?? {}))).then(unwrapData),\n        merge: (doctype, name, input) => request(`${resourcePath(doctype, name)}/merge`, { method: \"POST\", body: input ?? {} }).then(unwrapData),\n        transition: (doctype, name, workflow, action, options) => request(`${resourcePath(doctype, name)}/workflows/${encodePart(workflow)}/transition/${encodePart(action)}`, {\n          method: \"POST\",\n          body: versionBody(options)\n        }).then(unwrapData),\n        unassign: (doctype, name, assignee, options) => request(resourceMemberPath(doctype, name, \"assignments\", assignee), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        unfollow: (doctype, name, follower, options) => request(resourceMemberPath(doctype, name, \"followers\", follower), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        unshare: (doctype, name, userId, options) => request(resourceMemberPath(doctype, name, \"shares\", userId), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        untag: (doctype, name, tag, options) => request(resourceMemberPath(doctype, name, \"tags\", tag), { method: \"DELETE\", body: versionBody(options) }).then(unwrapData),\n        update: (doctype, name, data, options) => request(resourcePath(doctype, name), { method: \"PUT\", body: commandBody(data, options) }).then(unwrapData)\n      })\n    };\n    if (ext.form !== void 0) {\n      namespace.form = Object.freeze(ext.form);\n    }\n    if (ext.realtime !== void 0) {\n      namespace.realtime = Object.freeze(ext.realtime);\n    }\n    if (ext.collaboration !== void 0) {\n      namespace.collaboration = Object.freeze(ext.collaboration);\n    }\n    return namespace;\n  }\n  function installNamespace(extensions) {\n    const namespace = Object.freeze(\n      Object.assign({}, window.cfFrappe ?? {}, buildNamespace(extensions))\n    );\n    window.cfFrappe = namespace;\n    return namespace;\n  }\n  var coreSeam = {\n    childRowIndexField: CHILD_TABLE_ROW_INDEX_FIELD,\n    minMultipartChunkBytes: MIN_MULTIPART_FILE_PART_BYTES,\n    maxMultipartFileParts: MAX_MULTIPART_FILE_PARTS,\n    lockedValueProperty: LOCKED_VALUE_PROPERTY,\n    readOnlyProperty: READ_ONLY_PROPERTY,\n    softDisabledProperty: SOFT_DISABLED_PROPERTY,\n    realtimeCollaborationMessageType: REALTIME_COLLABORATION_MESSAGE_TYPE,\n    fieldEditMessageType: FIELD_EDIT_MESSAGE_TYPE,\n    sharedDraftMessageType: SHARED_DRAFT_MESSAGE_TYPE,\n    pageContext,\n    runtimeScript,\n    ready,\n    request,\n    requestBinary,\n    readResponsePayload,\n    throwResponseError,\n    unwrapData,\n    withQuery,\n    encodePart,\n    encodePath,\n    resourcePath,\n    resourceActionPath,\n    deskPath,\n    filePath,\n    versionBody,\n    commandBody,\n    fileAttachmentParams,\n    fileListParams,\n    resourceListParams,\n    reportRunParams,\n    documentTopic,\n    doctypeTopic,\n    tenantTopic,\n    userTopic,\n    doctypeTopicFromOptions,\n    documentTopicFromOptions,\n    tenantTopicFromOptions,\n    userTopicFromOptions,\n    msgprint\n  };\n\n  // src/adapters/desk/client-src/boot.ts\n  var registrations = [];\n  var contributions = [];\n  var hydratorRegistry = {\n    register(registration) {\n      registrations.push(registration);\n    },\n    list() {\n      return registrations.slice();\n    }\n  };\n  function registerHydrator(registration) {\n    hydratorRegistry.register(registration);\n  }\n  function registerNamespaceContribution(contribution) {\n    contributions.push(contribution);\n  }\n  function collectNamespaceExtensions() {\n    const collected = {};\n    for (const contribution of contributions) {\n      const extensions = contribution(coreSeam);\n      if (extensions.files !== void 0) {\n        collected.files = Object.assign({}, collected.files ?? {}, extensions.files);\n      }\n      if (extensions.form !== void 0) {\n        collected.form = extensions.form;\n      }\n      if (extensions.realtime !== void 0) {\n        collected.realtime = extensions.realtime;\n      }\n      if (extensions.collaboration !== void 0) {\n        collected.collaboration = extensions.collaboration;\n      }\n    }\n    return collected;\n  }\n  function boot() {\n    installNamespace(collectNamespaceExtensions());\n    for (const registration of hydratorRegistry.list()) {\n      ready(registration.hydrate);\n    }\n  }\n\n  // src/adapters/desk/client-src/merge.ts\n  function documentMergePlan(base, remote, draft, options) {\n    const baseSnapshot = mergeSnapshot(base, 0);\n    const remoteSnapshot = mergeSnapshot(remote, baseSnapshot.version);\n    const draftData = isPlainObject(draft) ? draft : {};\n    const fields = mergeFields(baseSnapshot.data, remoteSnapshot.data, draftData, options && options.fields);\n    const localChangedFields = [];\n    const remoteChangedFields = [];\n    const mergedFields = [];\n    const conflicts = [];\n    const patch = {};\n    const unset = [];\n    if (baseSnapshot.docstatus !== void 0 && remoteSnapshot.docstatus !== void 0 && baseSnapshot.docstatus !== remoteSnapshot.docstatus) {\n      conflicts.push(\n        mergeConflict(\"docstatus\", \"remote_status_changed\", {\n          basePresent: true,\n          localPresent: true,\n          remotePresent: true,\n          baseValue: baseSnapshot.docstatus,\n          localValue: baseSnapshot.docstatus,\n          remoteValue: remoteSnapshot.docstatus\n        })\n      );\n    }\n    fields.forEach((field) => {\n      const basePresent = Object.prototype.hasOwnProperty.call(baseSnapshot.data, field);\n      const localPresent = Object.prototype.hasOwnProperty.call(draftData, field);\n      const remotePresent = Object.prototype.hasOwnProperty.call(remoteSnapshot.data, field);\n      const baseValue = baseSnapshot.data[field];\n      const localValue = draftData[field];\n      const remoteValue = remoteSnapshot.data[field];\n      const localChanged = localPresent ? !mergeJsonEqual(localValue, baseValue) : basePresent;\n      const remoteChanged = !mergeJsonEqual(remoteValue, baseValue);\n      if (localChanged) {\n        localChangedFields.push(field);\n      }\n      if (remoteChanged) {\n        remoteChangedFields.push(field);\n      }\n      if (!localChanged) {\n        return;\n      }\n      if (remoteChanged && !mergeJsonEqual(localValue, remoteValue)) {\n        conflicts.push(\n          mergeConflict(field, \"remote_changed\", {\n            basePresent,\n            localPresent,\n            remotePresent,\n            baseValue,\n            localValue,\n            remoteValue\n          })\n        );\n        return;\n      }\n      if (mergeJsonEqual(localValue, remoteValue)) {\n        mergedFields.push(field);\n        return;\n      }\n      mergedFields.push(field);\n      if (localValue === void 0) {\n        unset.push(field);\n      } else {\n        patch[field] = cloneMergeValue(localValue);\n      }\n    });\n    return {\n      status: conflicts.length === 0 ? \"clean\" : \"conflict\",\n      baseVersion: baseSnapshot.version,\n      remoteVersion: remoteSnapshot.version,\n      localChangedFields,\n      remoteChangedFields,\n      mergedFields,\n      patch,\n      unset,\n      conflicts\n    };\n  }\n  function mergeSnapshot(value, fallbackVersion) {\n    const source = isPlainObject(value) ? value : {};\n    const hasData = isPlainObject(source.data);\n    const version = typeof source.version === \"number\" && Number.isFinite(source.version) ? source.version : fallbackVersion;\n    const snapshot = {\n      version,\n      data: cloneMergeValue(hasData ? source.data : source)\n    };\n    if (source.docstatus !== void 0) {\n      snapshot.docstatus = source.docstatus;\n    }\n    return snapshot;\n  }\n  function mergeFields(base, remote, draft, fields) {\n    const input = Array.isArray(fields) ? fields : Object.keys(base).concat(Object.keys(remote), Object.keys(draft));\n    const seen = {};\n    const result = [];\n    input.forEach((field) => {\n      const name = String(field || \"\").trim();\n      if (!name || seen[name]) {\n        return;\n      }\n      seen[name] = true;\n      result.push(name);\n    });\n    return result;\n  }\n  function mergeConflict(field, reason, values) {\n    const conflict = {\n      field,\n      reason,\n      basePresent: values.basePresent,\n      localPresent: values.localPresent,\n      remotePresent: values.remotePresent\n    };\n    if (values.baseValue !== void 0) {\n      conflict.baseValue = cloneMergeValue(values.baseValue);\n    }\n    if (values.localValue !== void 0) {\n      conflict.localValue = cloneMergeValue(values.localValue);\n    }\n    if (values.remoteValue !== void 0) {\n      conflict.remoteValue = cloneMergeValue(values.remoteValue);\n    }\n    return conflict;\n  }\n  function mergeJsonEqual(left, right) {\n    if (left === right) {\n      return true;\n    }\n    if (left === void 0 || right === void 0) {\n      return false;\n    }\n    if (left === null || right === null || typeof left !== \"object\" || typeof right !== \"object\") {\n      return false;\n    }\n    if (Array.isArray(left) || Array.isArray(right)) {\n      if (!Array.isArray(left) || !Array.isArray(right) || left.length !== right.length) {\n        return false;\n      }\n      return left.every((item, index) => mergeJsonEqual(item, right[index]));\n    }\n    const leftRecord = left;\n    const rightRecord = right;\n    const leftKeys = Object.keys(leftRecord).sort();\n    const rightKeys = Object.keys(rightRecord).sort();\n    if (leftKeys.length !== rightKeys.length) {\n      return false;\n    }\n    return leftKeys.every((key, index) => key === rightKeys[index] && mergeJsonEqual(leftRecord[key], rightRecord[key]));\n  }\n  function cloneMergeValue(value) {\n    if (value === null || typeof value !== \"object\") {\n      return value;\n    }\n    if (Array.isArray(value)) {\n      return value.map(cloneMergeValue);\n    }\n    const source = value;\n    const clone = {};\n    Object.keys(source).forEach((key) => {\n      clone[key] = cloneMergeValue(source[key]);\n    });\n    return clone;\n  }\n\n  // src/adapters/desk/client-src/forms.ts\n  var formHandlers = {};\n  var formBinding = null;\n  function expando(field) {\n    return field;\n  }\n  function registerFormHandlers(doctype, handlers) {\n    const registered = formHandlers[doctype] || [];\n    registered.push(handlers || {});\n    formHandlers[doctype] = registered;\n    ready(() => {\n      const binding = currentFormBinding();\n      if (binding && binding.context.doctype === doctype) {\n        triggerFormHandler(binding, handlers || {}, \"setup\");\n        triggerFormHandler(binding, handlers || {}, \"onload\");\n        triggerFormHandler(binding, handlers || {}, \"refresh\");\n      }\n    });\n  }\n  function currentFormBinding() {\n    const context = pageContext();\n    if (context.scope !== \"form\" || !context.doctype) {\n      return null;\n    }\n    const form = document.querySelector(\"form.form\");\n    if (!form) {\n      return null;\n    }\n    if (!formBinding || formBinding.form !== form) {\n      formBinding = createFormBinding(context, form);\n    }\n    return formBinding;\n  }\n  function createFormBinding(context, form) {\n    const baseDoc = readFormData(form);\n    const binding = {\n      baseDoc: cloneMergeValue(baseDoc),\n      baseDocstatus: context.documentStatus,\n      baseVersion: context.documentVersion === void 0 ? formExpectedVersion(form) : context.documentVersion,\n      context,\n      dirty: false,\n      doc: baseDoc,\n      form,\n      frm: void 0,\n      submitting: false,\n      validated: true\n    };\n    binding.frm = createFrm(binding);\n    attachFieldListeners(binding);\n    applyConditionalFieldVisibility(binding);\n    attachDocumentCollaboration(binding);\n    form.addEventListener(\"submit\", (event) => {\n      if (binding.submitting) {\n        return;\n      }\n      if (!isSaveSubmit(event)) {\n        return;\n      }\n      if (!validateFormForSave(binding)) {\n        event.preventDefault();\n      }\n    });\n    return binding;\n  }\n  function createFrm(binding) {\n    const frm = {\n      doc: binding.doc,\n      docname: binding.context.documentName,\n      doctype: binding.context.doctype,\n      validated: true,\n      dirty: () => {\n        binding.dirty = true;\n        binding.form.dataset.dirty = \"1\";\n      },\n      get_value: (fieldname) => {\n        syncFormData(binding);\n        return docValue(binding.doc, fieldname);\n      },\n      clear_value: (fieldname) => frm.set_value(fieldname, null),\n      get_field: (fieldname) => {\n        const fields = fieldsNamed(binding.form, fieldname);\n        return fields.length > 0 ? fields[0] : null;\n      },\n      is_dirty: () => binding.dirty,\n      is_new: () => !binding.context.documentName,\n      refresh: () => triggerFormEvent(binding, \"refresh\"),\n      refresh_field: (fieldname) => {\n        setFieldValue(binding.form, fieldname, docValue(binding.doc, fieldname));\n      },\n      save: (options) => {\n        if (options && options.merge) {\n          return mergeSaveForm(binding);\n        }\n        return submitNativeForm(binding);\n      },\n      set_value: (fieldname, value) => {\n        setFieldValue(binding.form, fieldname, value);\n        syncFormData(binding);\n        frm.dirty();\n        triggerFormEvent(binding, fieldname);\n        applyConditionalFieldVisibility(binding);\n        return Promise.resolve(value);\n      },\n      set_df_property: (fieldname, property, value) => {\n        setFieldProperty(binding.form, fieldname, property, value);\n        return frm;\n      },\n      toggle_display: (fieldname, show) => {\n        setFieldProperty(binding.form, fieldname, \"hidden\", !show);\n        return frm;\n      },\n      toggle_enable: (fieldname, enable) => {\n        setFieldProperty(binding.form, fieldname, \"disabled\", !enable);\n        return frm;\n      },\n      trigger: (eventName) => triggerFormEvent(binding, eventName),\n      mergePlan: (remote, draft) => currentFormMergePlan(binding, remote, draft),\n      merge_save: () => mergeSaveForm(binding),\n      share_draft: (input) => sendFormSharedDraft(binding, input)\n    };\n    return frm;\n  }\n  function validateFormForSave(binding) {\n    syncFormData(binding);\n    binding.validated = true;\n    binding.frm.validated = true;\n    const valid = triggerFormEvent(binding, \"validate\") !== false && binding.frm.validated !== false && binding.validated !== false;\n    const beforeSave = valid ? triggerFormEvent(binding, \"before_save\") !== false : false;\n    return valid && beforeSave && binding.frm.validated !== false && binding.validated !== false;\n  }\n  function submitNativeForm(binding) {\n    if (!validateFormForSave(binding)) {\n      return false;\n    }\n    binding.submitting = true;\n    if (typeof binding.form.requestSubmit === \"function\") {\n      binding.form.requestSubmit();\n    } else if (typeof binding.form.submit === \"function\") {\n      binding.form.submit();\n    }\n    binding.submitting = false;\n    return true;\n  }\n  function mergeSaveForm(binding) {\n    if (binding.submitting) {\n      return Promise.resolve(false);\n    }\n    if (!binding.context.doctype || !binding.context.documentName || binding.baseVersion === void 0) {\n      return Promise.reject(new Error(\"Merge save requires an existing document\"));\n    }\n    if (!validateFormForSave(binding)) {\n      return Promise.resolve(false);\n    }\n    const plan = currentFormLocalChangePlan(binding);\n    const input = {\n      baseVersion: binding.baseVersion,\n      patch: plan.patch\n    };\n    if (plan.unset.length > 0) {\n      input.unset = plan.unset;\n    }\n    binding.submitting = true;\n    return request(`${resourcePath(binding.context.doctype, binding.context.documentName)}/merge`, {\n      method: \"POST\",\n      body: input\n    }).then(unwrapData).then((result) => {\n      applyMergeSaveResult(binding, result);\n      return result;\n    }).finally(() => {\n      binding.submitting = false;\n    });\n  }\n  function attachFieldListeners(binding) {\n    binding.form.querySelectorAll(\"[name]\").forEach((element) => {\n      const field = element;\n      const fieldname = field.name;\n      field.addEventListener(\"focus\", () => {\n        sendFormFieldEdit(binding, field, true);\n      });\n      field.addEventListener(\"change\", () => {\n        if (restoreLockedFieldValue(field)) {\n          return;\n        }\n        syncFormData(binding);\n        binding.frm.dirty();\n        triggerFormEvent(binding, fieldname);\n        applyConditionalFieldVisibility(binding);\n        sendFormFieldEdit(binding, field, true);\n      });\n      field.addEventListener(\"input\", () => {\n        if (restoreLockedFieldValue(field)) {\n          return;\n        }\n        syncFormData(binding);\n        binding.frm.dirty();\n        applyConditionalFieldVisibility(binding);\n        sendFormFieldEdit(binding, field, true);\n      });\n      field.addEventListener(\"blur\", () => {\n        sendFormFieldEdit(binding, field, false);\n      });\n    });\n  }\n  function attachDocumentCollaboration(binding) {\n    const context = binding.context;\n    if (!context.documentName || !context.tenantId || !context.realtimeRoute) {\n      return;\n    }\n    try {\n      const namespace = window.cfFrappe;\n      const realtime = namespace && namespace.realtime;\n      const subscribe = realtime && realtime.subscribe;\n      const options = {\n        tenantId: context.tenantId,\n        realtimeRoute: context.realtimeRoute\n      };\n      binding.collaborationSubscription = subscribe(\n        documentTopicFromOptions(context.doctype, context.documentName, options),\n        {},\n        options\n      );\n    } catch (_error) {\n      binding.collaborationSubscription = void 0;\n    }\n  }\n  function sendFormFieldEdit(binding, field, editing) {\n    if (!binding.collaborationSubscription || isInternalFormField(field.name)) {\n      return;\n    }\n    binding.collaborationSubscription.sendFieldEdit(field.name, { editing });\n  }\n  function sendFormSharedDraft(binding, input) {\n    let messageInput = isPlainObject(input) ? input : {};\n    if (!Object.prototype.hasOwnProperty.call(messageInput, \"patch\")) {\n      const plan = currentFormLocalChangePlan(binding);\n      messageInput = Object.assign(\n        {\n          baseVersion: binding.baseVersion,\n          patch: plan.patch\n        },\n        plan.unset.length > 0 ? { unset: plan.unset } : {},\n        messageInput\n      );\n    }\n    messageInput = withoutUnsetPatchFields(messageInput);\n    if (!hasSharedDraftChanges(messageInput)) {\n      return sharedDraftMessage(messageInput);\n    }\n    if (!binding.collaborationSubscription || typeof binding.collaborationSubscription.sendSharedDraft !== \"function\") {\n      return sharedDraftMessage(messageInput);\n    }\n    return binding.collaborationSubscription.sendSharedDraft(messageInput);\n  }\n  function hasSharedDraftChanges(input) {\n    return isPlainObject(input.patch) && Object.keys(input.patch).length > 0 || Array.isArray(input.unset) && input.unset.length > 0;\n  }\n  function withoutUnsetPatchFields(input) {\n    if (!isPlainObject(input.patch) || !Array.isArray(input.unset)) {\n      return input;\n    }\n    const unset = input.unset.map((field) => String(field || \"\").trim());\n    const patch = Object.assign({}, input.patch);\n    unset.forEach((field) => {\n      delete patch[field];\n    });\n    return Object.assign({}, input, { patch });\n  }\n  function sharedDraftMessage(input) {\n    const options = isPlainObject(input) ? input : {};\n    const message = {\n      type: SHARED_DRAFT_MESSAGE_TYPE\n    };\n    if (Number.isInteger(options.baseVersion) && options.baseVersion >= 0) {\n      message.baseVersion = options.baseVersion;\n    }\n    if (isPlainObject(options.patch)) {\n      message.patch = options.patch;\n    }\n    if (Array.isArray(options.unset)) {\n      message.unset = options.unset;\n    }\n    return message;\n  }\n  function readFormData(form) {\n    const doc = {};\n    form.querySelectorAll(\"[name]\").forEach((element) => {\n      const field = element;\n      if (!isInternalFormField(field.name)) {\n        setDocValue(doc, field.name, fieldValue(field));\n      }\n    });\n    return doc;\n  }\n  function syncFormData(binding) {\n    binding.doc = readFormData(binding.form);\n    binding.frm.doc = binding.doc;\n  }\n  function formExpectedVersion(form) {\n    const fields = fieldsNamed(form, \"expectedVersion\");\n    if (fields.length === 0) {\n      return 0;\n    }\n    const value = Number(fields[0].value);\n    return Number.isInteger(value) && value >= 0 ? value : 0;\n  }\n  function fieldValue(field) {\n    const fieldType = field.dataset && field.dataset.cfFrappeFieldType;\n    if (field.type === \"checkbox\") {\n      return Boolean(field.checked);\n    }\n    if (fieldType && field.value === \"\" && !field.required) {\n      return void 0;\n    }\n    if (fieldType === \"integer\") {\n      const integerValue = Number(field.value);\n      return Number.isInteger(integerValue) ? integerValue : field.value;\n    }\n    if (fieldType === \"number\") {\n      const numberValue = Number(field.value);\n      return Number.isFinite(numberValue) ? numberValue : field.value;\n    }\n    if (fieldType === \"boolean\") {\n      return field.value === \"on\" || field.value === \"true\";\n    }\n    if (fieldType === \"json\") {\n      try {\n        return JSON.parse(field.value);\n      } catch (_error) {\n        return field.value;\n      }\n    }\n    return field.value;\n  }\n  function setFieldValue(form, fieldname, value) {\n    fieldsNamed(form, fieldname).forEach((field) => {\n      setControlValue(field, value);\n      rememberLockedFieldValue(field);\n    });\n  }\n  function fieldsNamed(form, fieldname) {\n    const fields = [];\n    form.querySelectorAll(\"[name]\").forEach((element) => {\n      const field = element;\n      if (field.name === fieldname) {\n        fields.push(field);\n      }\n    });\n    return fields;\n  }\n  function setFieldProperty(form, fieldname, property, value) {\n    fieldsNamed(form, fieldname).forEach((field) => {\n      if (property === \"hidden\") {\n        setFieldHidden(field, Boolean(value));\n        return;\n      }\n      if (property === \"display\") {\n        setFieldHidden(field, !value);\n        return;\n      }\n      if (property === \"read_only\" || property === \"readOnly\") {\n        setFieldReadOnly(field, Boolean(value));\n        return;\n      }\n      if (property === \"disabled\") {\n        setFieldSoftDisabled(field, Boolean(value));\n        return;\n      }\n      if (property === \"reqd\" || property === \"required\") {\n        field.required = Boolean(value);\n        return;\n      }\n      expando(field)[property] = value;\n    });\n  }\n  function setControlValue(field, value) {\n    if (field.type === \"checkbox\") {\n      field.checked = Boolean(value);\n    } else if (field.dataset && field.dataset.cfFrappeFieldType === \"json\" && value !== null && typeof value === \"object\") {\n      field.value = JSON.stringify(value);\n    } else {\n      field.value = value == null ? \"\" : String(value);\n    }\n  }\n  function fieldWrapper(field) {\n    if (typeof field.closest === \"function\") {\n      return field.closest(\".field\") || field;\n    }\n    return field;\n  }\n  function setFieldHidden(field, hidden) {\n    field.hidden = hidden;\n    fieldWrapper(field).hidden = hidden;\n  }\n  function setFieldReadOnly(field, readOnly) {\n    expando(field)[READ_ONLY_PROPERTY] = readOnly;\n    field.readOnly = readOnly;\n    setBooleanAttribute(field, \"aria-readonly\", readOnly);\n    if (readOnly) {\n      rememberLockedFieldValue(field, true);\n    } else {\n      delete expando(field)[READ_ONLY_PROPERTY];\n      clearLockedFieldValueIfUnlocked(field);\n    }\n  }\n  function setFieldSoftDisabled(field, disabled) {\n    expando(field)[SOFT_DISABLED_PROPERTY] = disabled;\n    setBooleanAttribute(field, \"aria-disabled\", disabled);\n    if (disabled) {\n      rememberLockedFieldValue(field, true);\n    } else {\n      delete expando(field)[SOFT_DISABLED_PROPERTY];\n      clearLockedFieldValueIfUnlocked(field);\n    }\n  }\n  function setBooleanAttribute(field, name, value) {\n    if (typeof field.setAttribute === \"function\" && typeof field.removeAttribute === \"function\") {\n      if (value) {\n        field.setAttribute(name, \"true\");\n      } else {\n        field.removeAttribute(name);\n      }\n    }\n  }\n  function fieldInteractionLocked(field) {\n    return Boolean(expando(field)[READ_ONLY_PROPERTY] || expando(field)[SOFT_DISABLED_PROPERTY]);\n  }\n  function rememberLockedFieldValue(field, force) {\n    if (force || fieldInteractionLocked(field)) {\n      expando(field)[LOCKED_VALUE_PROPERTY] = fieldValue(field);\n    }\n  }\n  function restoreLockedFieldValue(field) {\n    if (!fieldInteractionLocked(field)) {\n      return false;\n    }\n    if (Object.prototype.hasOwnProperty.call(field, LOCKED_VALUE_PROPERTY)) {\n      setControlValue(field, expando(field)[LOCKED_VALUE_PROPERTY]);\n    }\n    return true;\n  }\n  function clearLockedFieldValueIfUnlocked(field) {\n    if (!fieldInteractionLocked(field)) {\n      delete expando(field)[LOCKED_VALUE_PROPERTY];\n    }\n  }\n  function setDocValue(doc, fieldname, value) {\n    const child = childFieldPath(fieldname);\n    if (child) {\n      const rows = Array.isArray(doc[child.table]) ? doc[child.table] : [];\n      rows[child.index] = Object.assign({}, rows[child.index] || {}, {\n        [child.field]: value\n      });\n      doc[child.table] = rows;\n      return;\n    }\n    doc[fieldname] = value;\n  }\n  function docValue(doc, fieldname) {\n    if (Object.prototype.hasOwnProperty.call(doc, fieldname)) {\n      return doc[fieldname];\n    }\n    const child = childFieldPath(fieldname);\n    if (!child || !Array.isArray(doc[child.table])) {\n      return void 0;\n    }\n    const row = doc[child.table][child.index];\n    return row && row[child.field];\n  }\n  function childFieldPath(fieldname) {\n    const match = /^([^.[\\]]+)\\[(\\d+)\\]\\.(.+)$/.exec(fieldname);\n    if (!match) {\n      return null;\n    }\n    return {\n      field: match[3],\n      index: Number(match[2]),\n      table: match[1]\n    };\n  }\n  function isInternalFormField(fieldname) {\n    const child = childFieldPath(fieldname);\n    return fieldname === \"expectedVersion\" || fieldname === CHILD_TABLE_ROW_INDEX_FIELD || child !== null && child.field === CHILD_TABLE_ROW_INDEX_FIELD;\n  }\n  function formFieldNames(form) {\n    const names = [];\n    const seen = {};\n    form.querySelectorAll(\"[name]\").forEach((element) => {\n      const field = element;\n      const fieldname = String(field.name || \"\").trim();\n      const child = childFieldPath(fieldname);\n      const mergeField = child ? child.table : fieldname;\n      if (!mergeField || isInternalFormField(fieldname) || seen[mergeField]) {\n        return;\n      }\n      seen[mergeField] = true;\n      names.push(mergeField);\n    });\n    return names;\n  }\n  function currentFormMergePlan(binding, remote, draft) {\n    syncFormData(binding);\n    const remoteSnapshot = remote || binding.remoteSnapshot || {\n      version: binding.baseVersion,\n      data: binding.baseDoc\n    };\n    const baseSnapshot = formBaseSnapshot(binding);\n    return documentMergePlan(baseSnapshot, remoteSnapshot, draft || binding.doc, {\n      fields: formFieldNames(binding.form)\n    });\n  }\n  function currentFormLocalChangePlan(binding) {\n    syncFormData(binding);\n    const baseSnapshot = formBaseSnapshot(binding);\n    return documentMergePlan(baseSnapshot, baseSnapshot, binding.doc, {\n      fields: formFieldNames(binding.form)\n    });\n  }\n  function formBaseSnapshot(binding) {\n    return Object.assign(\n      {\n        version: binding.baseVersion,\n        data: binding.baseDoc\n      },\n      binding.baseDocstatus === void 0 ? {} : { docstatus: binding.baseDocstatus }\n    );\n  }\n  function applyMergeSaveResult(binding, result) {\n    binding.frm.last_merge_result = result;\n    const record = result;\n    if (record && record.plan) {\n      binding.remoteMergePlan = record.plan;\n      binding.frm.remote_merge_plan = record.plan;\n      binding.form.dataset.remoteMergeState = String(record.plan.status);\n    }\n    if (record && record.document) {\n      binding.remoteSnapshot = record.document;\n    }\n    if (record && (record.status === \"applied\" || record.status === \"noop\") && record.document) {\n      applyDocumentSnapshotToForm(binding, record.document);\n    }\n  }\n  function applyDocumentSnapshotToForm(binding, snapshot) {\n    const record = snapshot;\n    if (!record || !isPlainObject(record.data)) {\n      return;\n    }\n    binding.baseDoc = cloneMergeValue(record.data);\n    binding.baseDocstatus = record.docstatus;\n    if (typeof record.version === \"number\") {\n      binding.baseVersion = record.version;\n      binding.context.documentVersion = record.version;\n      binding.form.dataset.documentVersion = String(record.version);\n    }\n    binding.doc = cloneMergeValue(record.data);\n    binding.frm.doc = binding.doc;\n    writeDocumentToForm(binding, binding.doc);\n    applyConditionalFieldVisibility(binding);\n    binding.dirty = false;\n    delete binding.form.dataset.dirty;\n    delete binding.form.dataset.remoteUpdate;\n    delete binding.remoteSnapshot;\n    delete binding.remoteMergePlan;\n    delete binding.frm.remote_merge_plan;\n    binding.form.dataset.remoteMergeState = \"clean\";\n  }\n  function writeDocumentToForm(binding, data) {\n    binding.form.querySelectorAll(\"[name]\").forEach((element) => {\n      const field = element;\n      if (field.name === \"expectedVersion\") {\n        setControlValue(field, binding.baseVersion);\n        rememberLockedFieldValue(field);\n        return;\n      }\n      if (isInternalFormField(field.name)) {\n        return;\n      }\n      setControlValue(field, docValue(data, field.name));\n      rememberLockedFieldValue(field);\n    });\n  }\n  function applyConditionalFieldVisibility(binding) {\n    syncFormData(binding);\n    binding.form.querySelectorAll(\"[name]\").forEach((element) => {\n      const field = element;\n      const expressionSource = field.dataset && field.dataset.cfFrappeHiddenDependsOn;\n      if (!expressionSource) {\n        return;\n      }\n      const expression = parseJson(expressionSource);\n      if (!expression) {\n        return;\n      }\n      setFieldHidden(field, matchesFormPredicateExpression(binding, expression));\n    });\n  }\n  function parseJson(source) {\n    try {\n      return JSON.parse(source);\n    } catch (_error) {\n      return null;\n    }\n  }\n  function matchesFormPredicateExpression(binding, expression) {\n    if (!expression || typeof expression !== \"object\") {\n      return false;\n    }\n    const record = expression;\n    if (record.kind === \"group\") {\n      const predicates = Array.isArray(record.predicates) ? record.predicates : [];\n      return record.match === \"any\" ? predicates.some((predicate) => matchesFormPredicateExpression(binding, predicate)) : predicates.every((predicate) => matchesFormPredicateExpression(binding, predicate));\n    }\n    if (record.kind === \"not\") {\n      return !matchesFormPredicateExpression(binding, record.predicate);\n    }\n    if (record.kind !== \"compare\") {\n      return false;\n    }\n    return matchesFormPredicateComparison(binding, record);\n  }\n  function matchesFormPredicateComparison(binding, expression) {\n    const actual = formPredicateOperandValue(binding, expression.left);\n    const expected = formPredicateOperandValue(binding, expression.right);\n    const operator = expression.operator;\n    if (operator === \"eq\") {\n      return jsonConditionValuesEqual(actual, expected);\n    }\n    if (operator === \"ne\") {\n      return actual !== void 0 && actual !== null && !jsonConditionValuesEqual(actual, expected);\n    }\n    if (operator === \"in\") {\n      return actual !== void 0 && actual !== null && Array.isArray(expected) && expected.some((value) => jsonConditionValuesEqual(actual, value));\n    }\n    if (operator === \"not_in\") {\n      return actual !== void 0 && actual !== null && Array.isArray(expected) && !expected.some((value) => jsonConditionValuesEqual(actual, value));\n    }\n    if (operator === \"is\") {\n      return expected === \"set\" ? actual !== void 0 && actual !== null : expected === \"not set\" && (actual === void 0 || actual === null);\n    }\n    if (operator === \"contains\") {\n      return actual !== void 0 && actual !== null && expected !== void 0 && expected !== null && String(actual).toLowerCase().indexOf(String(expected).toLowerCase()) >= 0;\n    }\n    if (operator === \"like\" || operator === \"not_like\") {\n      if (actual === void 0 || actual === null || typeof expected !== \"string\") {\n        return false;\n      }\n      const matched = likePatternMatches(actual, expected);\n      return operator === \"like\" ? matched : !matched;\n    }\n    if (operator === \"gt\" || operator === \"gte\" || operator === \"lt\" || operator === \"lte\") {\n      if (!comparableConditionValues(actual, expected)) {\n        return false;\n      }\n      const compared = compareConditionValues(actual, expected);\n      return operator === \"gt\" ? compared > 0 : operator === \"gte\" ? compared >= 0 : operator === \"lt\" ? compared < 0 : compared <= 0;\n    }\n    if (operator === \"between\" || operator === \"not_between\") {\n      if (actual === void 0 || actual === null || !Array.isArray(expected) || expected.length !== 2 || !comparableConditionValues(actual, expected[0]) || !comparableConditionValues(actual, expected[1])) {\n        return false;\n      }\n      const between = compareConditionValues(actual, expected[0]) >= 0 && compareConditionValues(actual, expected[1]) <= 0;\n      return operator === \"between\" ? between : !between;\n    }\n    return false;\n  }\n  function formPredicateOperandValue(binding, operand) {\n    if (!operand || typeof operand !== \"object\") {\n      return void 0;\n    }\n    const record = operand;\n    if (record.kind === \"literal\") {\n      return record.value;\n    }\n    if (record.kind === \"field\" && record.scope === \"after\" && typeof record.field === \"string\") {\n      return formConditionValue(binding, record.field);\n    }\n    return void 0;\n  }\n  function formConditionValue(binding, fieldname) {\n    if (fieldname === \"system.name\") {\n      return binding.context.documentName;\n    }\n    if (fieldname === \"system.docstatus\") {\n      return binding.context.documentStatus;\n    }\n    if (fieldname === \"system.version\") {\n      return binding.context.documentVersion;\n    }\n    return docValue(binding.doc, fieldname);\n  }\n  function compareConditionValues(left, right) {\n    if (typeof left === \"number\" && typeof right === \"number\") {\n      return left - right;\n    }\n    return String(left).localeCompare(String(right));\n  }\n  function comparableConditionValues(left, right) {\n    return left !== void 0 && left !== null && right !== void 0 && right !== null && typeof left !== \"object\" && typeof right !== \"object\";\n  }\n  function jsonConditionValuesEqual(left, right) {\n    if (left === right) {\n      return true;\n    }\n    if (left === void 0 || right === void 0 || left === null || right === null) {\n      return false;\n    }\n    if (Array.isArray(left) || Array.isArray(right)) {\n      return Array.isArray(left) && Array.isArray(right) && left.length === right.length && left.every((value, index) => jsonConditionValuesEqual(value, right[index]));\n    }\n    if (typeof left !== \"object\" || typeof right !== \"object\") {\n      return false;\n    }\n    const leftRecord = left;\n    const rightRecord = right;\n    const leftKeys = Object.keys(leftRecord).sort();\n    const rightKeys = Object.keys(rightRecord).sort();\n    return leftKeys.length === rightKeys.length && leftKeys.every((key, index) => key === rightKeys[index] && jsonConditionValuesEqual(leftRecord[key], rightRecord[key]));\n  }\n  function likePatternMatches(actual, pattern) {\n    return new RegExp(`^${likePatternRegex(pattern)}$`, \"i\").test(String(actual));\n  }\n  function likePatternRegex(pattern) {\n    let regex = \"\";\n    for (let index = 0; index < pattern.length; index += 1) {\n      const character = pattern[index];\n      if (character === \"\\\\\") {\n        const next = pattern[index + 1];\n        if (next === void 0) {\n          regex += \"(?!)\";\n          continue;\n        }\n        regex += escapeRegex(next);\n        index += 1;\n      } else if (character === \"%\") {\n        regex += \"[\\\\s\\\\S]*\";\n      } else if (character === \"_\") {\n        regex += \"[\\\\s\\\\S]\";\n      } else {\n        regex += escapeRegex(character || \"\");\n      }\n    }\n    return regex;\n  }\n  function escapeRegex(value) {\n    return String(value).replace(/[\\\\^$.*+?()[\\]{}|]/g, \"\\\\$&\");\n  }\n  function isSaveSubmit(event) {\n    const submitter = event.submitter;\n    return !(submitter && typeof submitter.getAttribute === \"function\" && submitter.getAttribute(\"formaction\") !== null);\n  }\n  function triggerFormEvent(binding, eventName) {\n    let ok = true;\n    (formHandlers[binding.context.doctype] || []).forEach((handlers) => {\n      if (triggerFormHandler(binding, handlers, eventName) === false) {\n        ok = false;\n      }\n    });\n    return ok;\n  }\n  function triggerFormHandler(binding, handlers, eventName) {\n    const handler = handlers && handlers[eventName];\n    if (typeof handler !== \"function\") {\n      return void 0;\n    }\n    binding.validated = binding.frm.validated;\n    const result = handler(binding.frm);\n    binding.validated = binding.frm.validated;\n    return result;\n  }\n  var formNamespaceExtension = {\n    current() {\n      const binding = currentFormBinding();\n      return binding ? binding.frm : null;\n    },\n    on(doctype, handlers) {\n      return registerFormHandlers(doctype, handlers);\n    },\n    trigger(eventName) {\n      const binding = currentFormBinding();\n      return binding ? triggerFormEvent(binding, eventName) : void 0;\n    }\n  };\n  var formsNamespaceContribution = () => ({\n    form: formNamespaceExtension\n  });\n  var formsHydration = {\n    name: \"form-binding\",\n    hydrate: () => {\n      currentFormBinding();\n    }\n  };\n  function registerFormsModule() {\n    registerHydrator(formsHydration);\n    registerNamespaceContribution(formsNamespaceContribution);\n  }\n  registerFormsModule();\n\n  // src/adapters/desk/client-src/uploads.ts\n  function optionValue(options, camel, snake) {\n    if (!options) {\n      return void 0;\n    }\n    return options[camel] !== void 0 ? options[camel] : options[snake];\n  }\n  function fileUploadParams(options) {\n    const params = {};\n    fileAttachmentParams(params, options ?? {});\n    setParam(params, \"filename\", options?.filename);\n    setParam(params, \"is_private\", optionValue(options, \"isPrivate\", \"is_private\"));\n    return params;\n  }\n  function fileUploadHeaders(options) {\n    const headers = {};\n    const contentType = optionValue(options, \"contentType\", \"content_type\");\n    if (contentType !== void 0 && contentType !== null) {\n      headers[\"content-type\"] = contentType;\n    }\n    return headers;\n  }\n  function fileUploadLimit(options) {\n    const raw = optionValue(options, \"maxUploadBytes\", \"max_upload_bytes\");\n    const parsed = raw === void 0 ? Number.NaN : Number(raw);\n    return Number.isInteger(parsed) && parsed > 0 ? parsed : void 0;\n  }\n  function preflightKnownUploadSize(size, options) {\n    const maxUploadBytes = fileUploadLimit(options ?? {});\n    if (maxUploadBytes === void 0 || typeof size !== \"number\" || !Number.isFinite(size)) {\n      return;\n    }\n    if (size > maxUploadBytes) {\n      throw new Error(`File exceeds ${String(maxUploadBytes)} bytes`);\n    }\n  }\n  function uploadBodySize(body) {\n    const size = body?.size;\n    return typeof size === \"number\" && Number.isFinite(size) ? size : void 0;\n  }\n  function directUploadRequestBody(input) {\n    return withoutKeys(input ?? {}, [\"maxUploadBytes\", \"max_upload_bytes\"]);\n  }\n  var uploadReservationRequestBody = directUploadRequestBody;\n  var defaultMultipartChunkBytes = MIN_MULTIPART_FILE_PART_BYTES;\n  function fileBodySize(body) {\n    const size = uploadBodySize(body);\n    if (size === void 0) {\n      throw new Error(\"Multipart file body must expose a numeric size\");\n    }\n    return size;\n  }\n  function multipartFilename(body, options) {\n    const filename = options?.filename;\n    if (filename !== void 0 && filename !== null && String(filename) !== \"\") {\n      return filename;\n    }\n    const name = body?.name;\n    if (typeof name === \"string\" && name !== \"\") {\n      return name;\n    }\n    throw new Error(\"filename is required for multipart uploads\");\n  }\n  function multipartContentType(body, options) {\n    const contentType = optionValue(options, \"contentType\", \"content_type\");\n    if (contentType !== void 0 && contentType !== null && String(contentType) !== \"\") {\n      return contentType;\n    }\n    const type = body?.type;\n    return typeof type === \"string\" && type !== \"\" ? type : \"application/octet-stream\";\n  }\n  function multipartChunkSize(options) {\n    const chunkSize = options && options.chunkSize !== void 0 ? Number(options.chunkSize) : defaultMultipartChunkBytes;\n    if (!Number.isInteger(chunkSize) || chunkSize < 1) {\n      throw new Error(\"chunkSize must be a positive integer\");\n    }\n    return chunkSize;\n  }\n  function assertMultipartUploadPlan(size, chunkSize) {\n    const totalParts = Math.max(1, Math.ceil(size / chunkSize));\n    if (totalParts > 1 && chunkSize < MIN_MULTIPART_FILE_PART_BYTES) {\n      throw new Error(`chunkSize must be at least ${String(MIN_MULTIPART_FILE_PART_BYTES)} bytes for multi-part R2 uploads`);\n    }\n    if (totalParts > MAX_MULTIPART_FILE_PARTS) {\n      throw new Error(`Multipart upload cannot exceed ${String(MAX_MULTIPART_FILE_PARTS)} parts`);\n    }\n    return totalParts;\n  }\n  function multipartReservationBody(body, options) {\n    const input = {\n      filename: multipartFilename(body, options ?? {}),\n      size: fileBodySize(body),\n      contentType: multipartContentType(body, options ?? {})\n    };\n    fileAttachmentParams(input, options ?? {});\n    if (options && (options.isPrivate !== void 0 || options.is_private !== void 0)) {\n      input.isPrivate = options.isPrivate !== void 0 ? options.isPrivate : options.is_private;\n    }\n    if (options && options.expiresInSeconds !== void 0) {\n      input.expiresInSeconds = options.expiresInSeconds;\n    }\n    return input;\n  }\n  function directReservationBody(body, options) {\n    const input = multipartReservationBody(body, options ?? {});\n    const maxUploadBytes = optionValue(options, \"maxUploadBytes\", \"max_upload_bytes\");\n    if (maxUploadBytes !== void 0) {\n      input.maxUploadBytes = maxUploadBytes;\n    }\n    return input;\n  }\n  function multipartPartBody(body, start, end) {\n    const sliceable = body;\n    if (sliceable && typeof sliceable.slice === \"function\") {\n      return sliceable.slice(start, end);\n    }\n    throw new Error(\"Multipart file body must support slice(start, end)\");\n  }\n  function snapshotVersion(payload, fallback) {\n    const version = payload?.data?.version;\n    return version !== void 0 ? version : fallback;\n  }\n  function multipartProgress(callback, event) {\n    if (typeof callback === \"function\") {\n      callback(event);\n    }\n  }\n  async function prepareMultipartUpload(input) {\n    preflightKnownUploadSize(input?.size, input ?? {});\n    return request(\"/api/files/multipart-upload\", { method: \"POST\", body: uploadReservationRequestBody(input ?? {}) });\n  }\n  async function prepareDirectUpload(input) {\n    preflightKnownUploadSize(input?.size, input ?? {});\n    return request(\"/api/files/direct-upload\", { method: \"POST\", body: directUploadRequestBody(input ?? {}) });\n  }\n  function completeDirectUpload(name, options) {\n    return request(filePath(name, \"complete-upload\"), { method: \"POST\", body: versionBody(options) }).then(unwrapData);\n  }\n  async function uploadDirectFile(body, options) {\n    const prepared = await prepareDirectUpload(\n      directReservationBody(body, options ?? {})\n    );\n    const file = prepared?.data;\n    const upload = prepared?.upload;\n    if (!file || !file.name || !upload || !upload.url) {\n      throw new Error(\"Direct upload reservation did not return upload instructions\");\n    }\n    const uploadResponse = await fetch(upload.url, {\n      method: upload.method || \"PUT\",\n      headers: upload.headers || {},\n      body\n    });\n    if (!uploadResponse.ok) {\n      throwResponseError(uploadResponse, await readResponsePayload(uploadResponse));\n    }\n    return {\n      data: await completeDirectUpload(file.name, { expectedVersion: file.version }),\n      upload\n    };\n  }\n  function uploadMultipartPart(name, partNumber, body, options) {\n    const headers = {};\n    if (options && options.size !== void 0) {\n      headers[\"x-cf-frappe-part-size\"] = String(options.size);\n    }\n    return request(filePath(name, `multipart-parts/${encodePart(partNumber)}`), {\n      method: \"PUT\",\n      body,\n      headers\n    });\n  }\n  function completeMultipartUpload(name, parts, options) {\n    return request(filePath(name, \"complete-multipart-upload\"), {\n      method: \"POST\",\n      body: Object.assign({ parts }, versionBody(options))\n    }).then(unwrapData);\n  }\n  function abortMultipartUpload(name, options) {\n    return request(filePath(name, \"abort-multipart-upload\"), { method: \"POST\", body: versionBody(options) }).then(\n      unwrapData\n    );\n  }\n  async function uploadMultipartFile(body, options) {\n    const uploadOptions = options ?? {};\n    const input = multipartReservationBody(body, uploadOptions);\n    const chunkSize = multipartChunkSize(uploadOptions);\n    const size = input.size;\n    preflightKnownUploadSize(size, uploadOptions);\n    const totalParts = assertMultipartUploadPlan(size, chunkSize);\n    const prepared = await prepareMultipartUpload(input);\n    const fileName = prepared?.data?.name;\n    if (!fileName) {\n      throw new Error(\"Multipart upload reservation did not return a file name\");\n    }\n    let expectedVersion = snapshotVersion(prepared, void 0);\n    const parts = [];\n    let uploadedBytes = 0;\n    let canAbort = true;\n    try {\n      for (let partNumber = 1, start = 0; partNumber <= totalParts; partNumber += 1, start += chunkSize) {\n        const end = Math.min(start + chunkSize, size);\n        const chunk = multipartPartBody(body, start, end);\n        const uploaded = await uploadMultipartPart(fileName, partNumber, chunk, {\n          size: end - start\n        });\n        parts.push(uploaded?.part);\n        expectedVersion = snapshotVersion(uploaded, expectedVersion);\n        uploadedBytes += end - start;\n        multipartProgress(uploadOptions.onProgress, {\n          file: prepared?.data,\n          part: uploaded?.part,\n          partNumber,\n          totalParts,\n          uploadedBytes,\n          totalBytes: size\n        });\n      }\n      canAbort = false;\n      const completed = await completeMultipartUpload(fileName, parts, {\n        expectedVersion\n      });\n      return {\n        data: completed,\n        upload: prepared?.upload,\n        parts\n      };\n    } catch (error) {\n      if (canAbort && (!options || options.abortOnError !== false)) {\n        await abortMultipartUpload(fileName, { expectedVersion }).catch(() => {\n        });\n      }\n      throw error;\n    }\n  }\n  async function uploadFile(body, options) {\n    preflightKnownUploadSize(uploadBodySize(body), options ?? {});\n    return request(withQuery(\"/api/files\", fileUploadParams(options ?? {})), {\n      method: \"POST\",\n      body,\n      headers: fileUploadHeaders(options ?? {})\n    });\n  }\n  function hydrateFileUploadForms() {\n    const forms = document.querySelectorAll(\n      \"form.file-upload[data-max-file-bytes], form.attachment-upload[data-max-file-bytes]\"\n    );\n    forms.forEach((form) => {\n      hydrateFileUploadForm(form);\n    });\n  }\n  function hydrateFileUploadForm(form) {\n    if (form.__cfFrappeFileUploadHydrated) {\n      return;\n    }\n    form.__cfFrappeFileUploadHydrated = true;\n    form.addEventListener(\"submit\", (event) => {\n      void handleUploadFormSubmit(form, event);\n    });\n  }\n  async function handleUploadFormSubmit(form, event) {\n    const maxFileBytes = uploadFormMaxFileBytes(form);\n    const file = selectedUploadFile(form);\n    if (!file) {\n      clearUploadFileValidity(form);\n      return;\n    }\n    if (maxFileBytes !== void 0 && typeof file.size === \"number\" && Number.isFinite(file.size) && file.size > maxFileBytes) {\n      const message = `File exceeds ${String(maxFileBytes)} bytes`;\n      event.preventDefault();\n      setUploadFileValidity(form, message, true);\n      msgprint(message);\n      return;\n    }\n    clearUploadFileValidity(form);\n    if (form.dataset.uploadMode !== \"direct\") {\n      return;\n    }\n    event.preventDefault();\n    if (form.__cfFrappeFileUploadInFlight) {\n      return;\n    }\n    form.__cfFrappeFileUploadInFlight = true;\n    try {\n      await uploadDirectFile(file, uploadFormDirectOptions(form, file, maxFileBytes));\n      clearUploadFileValidity(form);\n      window.location.href = uploadFormSuccessUrl(form);\n    } catch (error) {\n      const withMessage = error;\n      const errorMessage = withMessage && withMessage.message ? String(withMessage.message) : String(error);\n      setUploadFileValidity(form, errorMessage, true);\n      msgprint(errorMessage);\n    } finally {\n      form.__cfFrappeFileUploadInFlight = false;\n    }\n  }\n  function uploadFormMaxFileBytes(form) {\n    const raw = form.dataset.maxFileBytes;\n    const parsed = raw === void 0 ? Number.NaN : Number(raw);\n    return Number.isInteger(parsed) && parsed > 0 ? parsed : void 0;\n  }\n  function uploadFileInput(form) {\n    return form.querySelector('input[type=\"file\"][name=\"file\"], input[type=\"file\"]');\n  }\n  function selectedUploadFile(form) {\n    const input = uploadFileInput(form);\n    if (!input || !input.files || input.files.length === 0) {\n      return void 0;\n    }\n    return input.files[0];\n  }\n  function uploadFormControl(form, name) {\n    return form.querySelector(`[name=\"${name}\"]`);\n  }\n  function uploadFormValue(form, name) {\n    const control = uploadFormControl(form, name);\n    if (!control || control.value === void 0 || control.value === null || String(control.value) === \"\") {\n      return void 0;\n    }\n    return control.value;\n  }\n  function uploadFormChecked(form, name) {\n    const control = uploadFormControl(form, name);\n    return control ? Boolean(control.checked) : void 0;\n  }\n  function uploadFormDirectOptions(form, file, maxFileBytes) {\n    const options = {\n      filename: file.name ? file.name : uploadFormValue(form, \"filename\"),\n      contentType: file.type ? file.type : void 0\n    };\n    const attachedToDoctype = form.dataset.attachedToDoctype ? form.dataset.attachedToDoctype : uploadFormValue(form, \"attached_to_doctype\");\n    const attachedToName = form.dataset.attachedToName ? form.dataset.attachedToName : uploadFormValue(form, \"attached_to_name\");\n    if (attachedToDoctype || attachedToName) {\n      options.attachedTo = { doctype: attachedToDoctype, name: attachedToName };\n    }\n    const isPrivate = uploadFormChecked(form, \"is_private\");\n    if (isPrivate !== void 0) {\n      options.isPrivate = isPrivate;\n    }\n    if (maxFileBytes !== void 0) {\n      options.maxUploadBytes = maxFileBytes;\n    }\n    return options;\n  }\n  function uploadFormSuccessUrl(form) {\n    return form.dataset.successUrl ? form.dataset.successUrl : window.location.href;\n  }\n  function setUploadFileValidity(form, message, report) {\n    const input = uploadFileInput(form);\n    if (input && typeof input.setCustomValidity === \"function\") {\n      input.setCustomValidity(message);\n    }\n    if (report && input && typeof input.reportValidity === \"function\") {\n      input.reportValidity();\n    }\n  }\n  function clearUploadFileValidity(form) {\n    setUploadFileValidity(form, \"\", false);\n  }\n  var filesUploadExtension = {\n    abortMultipartUpload,\n    completeDirectUpload,\n    completeMultipartUpload,\n    prepareDirectUpload,\n    prepareMultipartUpload,\n    upload: uploadFile,\n    uploadDirect: uploadDirectFile,\n    uploadMultipart: uploadMultipartFile,\n    uploadMultipartPart\n  };\n  function registerUploads() {\n    registerNamespaceContribution(() => ({ files: { ...filesUploadExtension } }));\n    registerHydrator({ name: \"file-upload-forms\", hydrate: hydrateFileUploadForms });\n  }\n  registerUploads();\n\n  // src/adapters/desk/client-src/filter-builder.ts\n  function asValueControl(element) {\n    return element;\n  }\n  function hydrateCompoundFilterBuilders() {\n    const builders = document.querySelectorAll(\n      \"[data-cf-frappe-compound-filter-builder]\"\n    );\n    builders.forEach((builder) => {\n      hydrateCompoundFilterBuilder(builder);\n    });\n  }\n  function hydrateCompoundFilterBuilder(builder) {\n    const form = builder.closest(\"form\");\n    if (!form || builder.__cfFrappeCompoundFilterHydrated) {\n      return;\n    }\n    builder.__cfFrappeCompoundFilterHydrated = true;\n    const expression = builder.querySelector('[name=\"filter_expression\"]');\n    if (expression) {\n      expression.addEventListener(\"input\", () => {\n        builder.__cfFrappeCompoundFilterSource = \"text\";\n      });\n      expression.addEventListener(\"change\", () => {\n        builder.__cfFrappeCompoundFilterSource = \"text\";\n      });\n    }\n    const root = builder.querySelector(\"[data-cf-frappe-filter-group]\");\n    if (root) {\n      hydrateCompoundFilterGroup(builder, root);\n    } else {\n      builder.querySelectorAll(\"[data-cf-frappe-filter-row]\").forEach((row) => {\n        hydrateCompoundFilterRow(builder, row);\n      });\n    }\n    form.addEventListener(\"submit\", () => {\n      syncCompoundFilterExpression(builder);\n    });\n  }\n  function hydrateCompoundFilterGroup(builder, group) {\n    if (group.__cfFrappeCompoundFilterGroupHydrated) {\n      return;\n    }\n    group.__cfFrappeCompoundFilterGroupHydrated = true;\n    const match = group.querySelector(\"[data-cf-frappe-filter-match]\");\n    if (match) {\n      match.addEventListener(\"change\", () => {\n        markCompoundFilterVisualDirty(builder);\n      });\n    }\n    const addButton = group.querySelector(\"[data-cf-frappe-add-filter]\");\n    if (addButton) {\n      addButton.addEventListener(\"click\", () => {\n        addCompoundFilterRow(builder, group);\n      });\n    }\n    const addGroupButton = group.querySelector(\"[data-cf-frappe-add-filter-group]\");\n    if (addGroupButton) {\n      addGroupButton.addEventListener(\"click\", () => {\n        addCompoundFilterGroup(builder, group);\n      });\n    }\n    const removeGroupButton = group.querySelector(\"[data-cf-frappe-remove-filter-group]\");\n    if (removeGroupButton) {\n      removeGroupButton.addEventListener(\"click\", () => {\n        markCompoundFilterVisualDirty(builder);\n        removeCompoundFilterGroup(builder, group);\n      });\n    }\n    group.querySelectorAll(\"[data-cf-frappe-filter-row]\").forEach((row) => {\n      hydrateCompoundFilterRow(builder, row);\n    });\n    group.querySelectorAll(\"[data-cf-frappe-filter-group]\").forEach((childGroup) => {\n      hydrateCompoundFilterGroup(builder, childGroup);\n    });\n  }\n  function hydrateCompoundFilterRow(builder, row) {\n    if (row.__cfFrappeCompoundFilterRowHydrated) {\n      return;\n    }\n    row.__cfFrappeCompoundFilterRowHydrated = true;\n    const field = row.querySelector(\"[data-cf-frappe-filter-field]\");\n    const operator = row.querySelector(\"[data-cf-frappe-filter-operator]\");\n    const remove = row.querySelector(\"[data-cf-frappe-remove-filter]\");\n    if (field) {\n      field.addEventListener(\"change\", () => {\n        markCompoundFilterVisualDirty(builder);\n        refreshCompoundFilterOperatorOptions(builder, row);\n        refreshCompoundFilterValueInputType(builder, row);\n      });\n    }\n    if (operator) {\n      operator.addEventListener(\"change\", () => {\n        markCompoundFilterVisualDirty(builder);\n        refreshCompoundFilterValueInputType(builder, row);\n      });\n    }\n    row.querySelectorAll(\"select, input\").forEach((control) => {\n      control.addEventListener(\"input\", () => {\n        markCompoundFilterVisualDirty(builder);\n      });\n      control.addEventListener(\"change\", () => {\n        markCompoundFilterVisualDirty(builder);\n      });\n    });\n    if (remove) {\n      remove.addEventListener(\"click\", () => {\n        markCompoundFilterVisualDirty(builder);\n        removeCompoundFilterRow(builder, row);\n      });\n    }\n  }\n  function markCompoundFilterVisualDirty(builder) {\n    builder.__cfFrappeCompoundFilterSource = \"visual\";\n  }\n  function addCompoundFilterRow(builder, group) {\n    const container = compoundFilterItemsContainer(group);\n    const row = cloneCompoundFilterTemplate(builder, \"[data-cf-frappe-filter-row-template]\");\n    if (!container || !row) {\n      return;\n    }\n    resetCompoundFilterRow(row);\n    container.appendChild(row);\n    refreshCompoundFilterOperatorOptions(builder, row);\n    refreshCompoundFilterValueInputType(builder, row);\n    markCompoundFilterVisualDirty(builder);\n    hydrateCompoundFilterRow(builder, row);\n  }\n  function addCompoundFilterGroup(builder, group) {\n    const container = compoundFilterItemsContainer(group);\n    const childGroup = cloneCompoundFilterTemplate(builder, \"[data-cf-frappe-filter-group-template]\");\n    if (!container || !childGroup) {\n      return;\n    }\n    resetCompoundFilterGroup(builder, childGroup);\n    container.appendChild(childGroup);\n    markCompoundFilterVisualDirty(builder);\n    hydrateCompoundFilterGroup(builder, childGroup);\n  }\n  function cloneCompoundFilterTemplate(builder, selector) {\n    const template = builder.querySelector(selector);\n    const content = template && template.content;\n    const element = content && content.firstElementChild;\n    if (!element) {\n      return null;\n    }\n    return element.cloneNode(true);\n  }\n  function resetCompoundFilterGroup(builder, group) {\n    const match = asValueControl(group.querySelector(\"[data-cf-frappe-filter-match]\"));\n    if (match) {\n      match.value = \"all\";\n    }\n    const container = compoundFilterItemsContainer(group);\n    const row = cloneCompoundFilterTemplate(builder, \"[data-cf-frappe-filter-row-template]\");\n    if (!container || !row) {\n      return;\n    }\n    while (container.firstChild) {\n      container.removeChild(container.firstChild);\n    }\n    resetCompoundFilterRow(row);\n    container.appendChild(row);\n  }\n  function resetCompoundFilterRow(row) {\n    row.querySelectorAll(\"select, input\").forEach((control) => {\n      control.value = \"\";\n    });\n    const operator = asValueControl(row.querySelector(\"[data-cf-frappe-filter-operator]\"));\n    if (operator) {\n      operator.value = \"eq\";\n    }\n  }\n  function removeCompoundFilterRow(builder, row) {\n    const group = compoundFilterClosestGroup(row);\n    const container = compoundFilterItemsContainer(group);\n    if (compoundFilterContainerChildren(container).length <= 1) {\n      resetCompoundFilterRow(row);\n      refreshCompoundFilterOperatorOptions(builder, row);\n      refreshCompoundFilterValueInputType(builder, row);\n      return;\n    }\n    row.remove();\n  }\n  function removeCompoundFilterGroup(builder, group) {\n    if (group === builder.querySelector(\"[data-cf-frappe-filter-group]\")) {\n      return;\n    }\n    const parent = compoundFilterParentGroup(group);\n    group.remove();\n    ensureCompoundFilterGroupHasItem(builder, parent);\n  }\n  function ensureCompoundFilterGroupHasItem(builder, group) {\n    const container = compoundFilterItemsContainer(group);\n    if (!container || compoundFilterContainerChildren(container).length > 0) {\n      return;\n    }\n    const row = cloneCompoundFilterTemplate(builder, \"[data-cf-frappe-filter-row-template]\");\n    if (!row) {\n      return;\n    }\n    resetCompoundFilterRow(row);\n    container.appendChild(row);\n    hydrateCompoundFilterRow(builder, row);\n  }\n  function syncCompoundFilterExpression(builder) {\n    const target = builder.querySelector('[name=\"filter_expression\"]');\n    if (!target) {\n      return;\n    }\n    if (builder.__cfFrappeCompoundFilterSource === \"visual\") {\n      const expression = compoundFilterExpressionFromBuilder(builder);\n      target.value = expression ? JSON.stringify(expression) : \"\";\n    }\n  }\n  function compoundFilterExpressionFromBuilder(builder) {\n    const root = builder.querySelector(\"[data-cf-frappe-filter-group]\");\n    if (root) {\n      return compoundFilterExpressionFromGroup(builder, root, true);\n    }\n    const filters = [];\n    builder.querySelectorAll(\"[data-cf-frappe-filter-row]\").forEach((row) => {\n      const filter = compoundFilterExpressionFromRow(builder, row);\n      if (filter) {\n        filters.push(filter);\n      }\n    });\n    if (filters.length === 0) {\n      return void 0;\n    }\n    if (filters.length === 1) {\n      return filters[0];\n    }\n    const match = controlValue(builder.querySelector(\"[data-cf-frappe-filter-match]\")) === \"any\" ? \"any\" : \"all\";\n    return {\n      kind: \"group\",\n      match,\n      filters\n    };\n  }\n  function compoundFilterExpressionFromGroup(builder, group, root) {\n    const filters = [];\n    const container = compoundFilterItemsContainer(group);\n    compoundFilterContainerChildren(container).forEach((item) => {\n      let filter = void 0;\n      if (item.matches(\"[data-cf-frappe-filter-row]\")) {\n        filter = compoundFilterExpressionFromRow(builder, item);\n      } else if (item.matches(\"[data-cf-frappe-filter-group]\")) {\n        filter = compoundFilterExpressionFromGroup(builder, item, false);\n      }\n      if (filter) {\n        filters.push(filter);\n      }\n    });\n    if (filters.length === 0) {\n      return void 0;\n    }\n    if (root && filters.length === 1) {\n      return filters[0];\n    }\n    const match = controlValue(group.querySelector(\"[data-cf-frappe-filter-match]\")) === \"any\" ? \"any\" : \"all\";\n    return {\n      kind: \"group\",\n      match,\n      filters\n    };\n  }\n  function compoundFilterItemsContainer(group) {\n    return group ? group.querySelector(\"[data-cf-frappe-filter-items]\") || group.querySelector(\"[data-cf-frappe-filter-rows]\") : null;\n  }\n  function compoundFilterContainerChildren(container) {\n    return container ? Array.prototype.slice.call(container.children) : [];\n  }\n  function compoundFilterClosestGroup(element) {\n    return element.closest(\"[data-cf-frappe-filter-group]\");\n  }\n  function compoundFilterParentGroup(group) {\n    const parent = group.parentElement;\n    return parent ? parent.closest(\"[data-cf-frappe-filter-group]\") : null;\n  }\n  function compoundFilterExpressionFromRow(builder, row) {\n    const field = controlValue(row.querySelector(\"[data-cf-frappe-filter-field]\"));\n    const value = controlValue(row.querySelector(\"[data-cf-frappe-filter-value]\"));\n    if (!field || value === \"\") {\n      return void 0;\n    }\n    if (compoundFilterExpressionKind(builder) === \"report\") {\n      return {\n        filter: field,\n        value\n      };\n    }\n    const operator = controlValue(row.querySelector(\"[data-cf-frappe-filter-operator]\")) || \"eq\";\n    return Object.assign(\n      {\n        field,\n        value: compoundFilterValue(value, operator)\n      },\n      operator === \"eq\" ? {} : { operator }\n    );\n  }\n  function compoundFilterValue(value, operator) {\n    if (operator === \"in\" || operator === \"not_in\" || operator === \"between\" || operator === \"not_between\") {\n      return value.split(\",\").map((item) => item.trim()).filter((item) => item !== \"\");\n    }\n    return value;\n  }\n  function refreshCompoundFilterOperatorOptions(builder, row) {\n    const field = controlValue(row.querySelector(\"[data-cf-frappe-filter-field]\"));\n    const operator = row.querySelector(\"[data-cf-frappe-filter-operator]\");\n    if (!operator) {\n      return;\n    }\n    const options = compoundFilterFieldOptions(builder, field);\n    const selected = operator.value;\n    while (operator.firstChild) {\n      operator.removeChild(operator.firstChild);\n    }\n    options.forEach((option) => {\n      const element = document.createElement(\"option\");\n      element.value = option.operator;\n      element.textContent = option.label;\n      if (option.operator === selected) {\n        element.selected = true;\n      }\n      operator.appendChild(element);\n    });\n    const first = options[0];\n    if (!options.some((option) => option.operator === operator.value) && first) {\n      operator.value = first.operator;\n    }\n  }\n  function refreshCompoundFilterValueInputType(builder, row) {\n    const value = row.querySelector(\"[data-cf-frappe-filter-value]\");\n    if (!value) {\n      return;\n    }\n    value.type = compoundFilterValueInputType(builder, row);\n  }\n  function compoundFilterValueInputType(builder, row) {\n    const operator = controlValue(row.querySelector(\"[data-cf-frappe-filter-operator]\")) || \"eq\";\n    if (operator === \"in\" || operator === \"not_in\" || operator === \"between\" || operator === \"not_between\") {\n      return \"text\";\n    }\n    const field = controlValue(row.querySelector(\"[data-cf-frappe-filter-field]\"));\n    const metadata = compoundFilterMetadata(builder).filter((item) => item && item.field === field)[0];\n    const inputType = metadata && metadata.inputType;\n    return inputType === \"number\" || inputType === \"date\" || inputType === \"datetime-local\" ? inputType : \"text\";\n  }\n  function compoundFilterFieldOptions(builder, field) {\n    const metadata = compoundFilterMetadata(builder);\n    const selected = metadata.filter((item) => item && item.field === field)[0];\n    if (selected && Array.isArray(selected.operators)) {\n      return selected.operators.slice();\n    }\n    return metadata.reduce((operators, item) => {\n      (item && Array.isArray(item.operators) ? item.operators : []).forEach((operator) => {\n        if (!operators.some((existing) => existing.operator === operator.operator)) {\n          operators.push(operator);\n        }\n      });\n      return operators;\n    }, []);\n  }\n  function compoundFilterMetadata(builder) {\n    if (builder.__cfFrappeFilterFields) {\n      return builder.__cfFrappeFilterFields;\n    }\n    try {\n      builder.__cfFrappeFilterFields = JSON.parse(\n        builder.dataset && builder.dataset.filterFields || \"[]\"\n      );\n    } catch (_error) {\n      builder.__cfFrappeFilterFields = [];\n    }\n    return builder.__cfFrappeFilterFields;\n  }\n  function compoundFilterExpressionKind(builder) {\n    return builder.dataset && builder.dataset.filterExpressionKind === \"report\" ? \"report\" : \"list\";\n  }\n  function controlValue(control) {\n    const value = asValueControl(control)?.value;\n    return value !== void 0 ? String(value).trim() : \"\";\n  }\n  function registerCompoundFilterBuilderHydrator() {\n    registerHydrator({ name: \"compound-filter-builder\", hydrate: hydrateCompoundFilterBuilders });\n  }\n  registerCompoundFilterBuilderHydrator();\n\n  // src/adapters/desk/client-src/formula-builder.ts\n  function createSelectControl() {\n    return document.createElement(\"select\");\n  }\n  function hydrateReportFormulaBuilders() {\n    const builders = document.querySelectorAll(\n      \"[data-cf-frappe-report-formula-builder]\"\n    );\n    builders.forEach((builder) => {\n      hydrateReportFormulaBuilder(builder);\n    });\n  }\n  function hydrateReportFormulaBuilder(builder) {\n    if (builder.__cfFrappeReportFormulaHydrated) {\n      return;\n    }\n    builder.__cfFrappeReportFormulaHydrated = true;\n    builder.querySelectorAll(\"[data-cf-frappe-formula-operand]\").forEach((operand) => {\n      hydrateReportFormulaOperand(builder, operand);\n    });\n  }\n  function hydrateReportFormulaOperand(builder, operand) {\n    if (operand.__cfFrappeReportFormulaOperandHydrated) {\n      return;\n    }\n    operand.__cfFrappeReportFormulaOperandHydrated = true;\n    const kind = operand.querySelector(\"[data-cf-frappe-formula-kind]\");\n    if (kind) {\n      kind.addEventListener(\"change\", () => {\n        syncReportFormulaNestedOperand(builder, operand);\n      });\n    }\n  }\n  function syncReportFormulaNestedOperand(builder, operand) {\n    const nested = operand.querySelector(\"[data-cf-frappe-formula-nested]\");\n    if (!nested) {\n      return;\n    }\n    const kind = controlValue2(operand.querySelector(\"[data-cf-frappe-formula-kind]\"));\n    if (kind !== \"nested\") {\n      clearReportFormulaNested(nested);\n      return;\n    }\n    if (nested.firstChild) {\n      return;\n    }\n    const group = createReportFormulaNestedGroup(builder, operand);\n    if (!group) {\n      return;\n    }\n    nested.appendChild(group);\n    group.querySelectorAll(\"[data-cf-frappe-formula-operand]\").forEach((childOperand) => {\n      hydrateReportFormulaOperand(builder, childOperand);\n    });\n  }\n  function clearReportFormulaNested(nested) {\n    while (nested.firstChild) {\n      nested.removeChild(nested.firstChild);\n    }\n  }\n  function createReportFormulaNestedGroup(builder, operand) {\n    const prefix = reportFormulaOperandPrefix(operand);\n    const label = reportFormulaOperandLabel(operand);\n    const depth = reportFormulaOperandDepth(operand);\n    if (!prefix || !label || depth > reportFormulaMaxDepth(builder)) {\n      return null;\n    }\n    const group = document.createElement(\"div\");\n    group.className = \"report-formula-nested-group\";\n    group.appendChild(createReportFormulaOperatorControl(prefix, label));\n    group.appendChild(createReportFormulaOperand(builder, `${prefix}Left`, `${label} Left`, depth + 1));\n    group.appendChild(createReportFormulaOperand(builder, `${prefix}Right`, `${label} Right`, depth + 1));\n    return group;\n  }\n  function createReportFormulaOperand(builder, prefix, label, depth) {\n    const operand = document.createElement(\"div\");\n    operand.className = \"report-formula-operand\";\n    operand.setAttribute(\"data-cf-frappe-formula-operand\", \"\");\n    operand.dataset.formulaPrefix = prefix;\n    operand.dataset.formulaLabel = label;\n    operand.dataset.formulaDepth = String(depth);\n    operand.appendChild(createReportFormulaKindControl(builder, prefix, label, depth));\n    operand.appendChild(createReportFormulaFieldControl(builder, prefix, label));\n    operand.appendChild(createReportFormulaLiteralControl(prefix, label));\n    const nested = document.createElement(\"div\");\n    nested.className = \"report-formula-nested\";\n    nested.setAttribute(\"data-cf-frappe-formula-nested\", \"\");\n    operand.appendChild(nested);\n    return operand;\n  }\n  function createReportFormulaKindControl(builder, prefix, label, depth) {\n    const select = createSelectControl();\n    setReportFormulaControlName(select, `${prefix}Kind`);\n    select.setAttribute(\"data-cf-frappe-formula-kind\", \"\");\n    appendReportFormulaOption(select, \"field\", \"Field\");\n    appendReportFormulaOption(select, \"literal\", \"Number\");\n    if (depth <= reportFormulaMaxDepth(builder)) {\n      appendReportFormulaOption(select, \"nested\", \"Nested formula\");\n    }\n    return reportFormulaField(`${label} Type`, select);\n  }\n  function createReportFormulaFieldControl(builder, prefix, label) {\n    const select = createSelectControl();\n    setReportFormulaControlName(select, prefix);\n    appendReportFormulaOption(select, \"\", \"\");\n    reportFormulaFields(builder).forEach((field) => {\n      appendReportFormulaOption(select, field.name, field.label || field.name);\n    });\n    return reportFormulaField(label, select);\n  }\n  function createReportFormulaLiteralControl(prefix, label) {\n    const input = document.createElement(\"input\");\n    setReportFormulaControlName(input, `${prefix}Literal`);\n    input.type = \"number\";\n    input.step = \"any\";\n    return reportFormulaField(`${label} Number`, input);\n  }\n  function createReportFormulaOperatorControl(prefix, label) {\n    const select = createSelectControl();\n    setReportFormulaControlName(select, `${prefix}Operator`);\n    appendReportFormulaOption(select, \"\", \"\");\n    appendReportFormulaOption(select, \"add\", \"Add\");\n    appendReportFormulaOption(select, \"subtract\", \"Subtract\");\n    appendReportFormulaOption(select, \"multiply\", \"Multiply\");\n    appendReportFormulaOption(select, \"divide\", \"Divide\");\n    return reportFormulaField(`${label} Operator`, select);\n  }\n  function reportFormulaField(label, control) {\n    const field = document.createElement(\"label\");\n    field.className = \"field\";\n    const span = document.createElement(\"span\");\n    span.textContent = label;\n    field.appendChild(span);\n    field.appendChild(control);\n    return field;\n  }\n  function setReportFormulaControlName(control, name) {\n    control.name = name;\n    control.setAttribute(\"name\", name);\n  }\n  function appendReportFormulaOption(select, value, label) {\n    const option = document.createElement(\"option\");\n    option.value = value;\n    option.textContent = label;\n    select.appendChild(option);\n  }\n  function reportFormulaFields(builder) {\n    if (builder.__cfFrappeReportFormulaFields) {\n      return builder.__cfFrappeReportFormulaFields;\n    }\n    try {\n      const parsed = JSON.parse(builder.dataset && builder.dataset.formulaFields || \"[]\");\n      builder.__cfFrappeReportFormulaFields = Array.isArray(parsed) ? parsed.filter(\n        (field) => !!field && typeof field.name === \"string\"\n      ).map((field) => ({\n        name: field.name,\n        label: typeof field.label === \"string\" ? field.label : field.name\n      })) : [];\n    } catch (_error) {\n      builder.__cfFrappeReportFormulaFields = [];\n    }\n    return builder.__cfFrappeReportFormulaFields;\n  }\n  function reportFormulaMaxDepth(builder) {\n    const value = Number(builder.dataset && builder.dataset.formulaMaxDepth);\n    return Number.isFinite(value) && value > 0 ? value : 16;\n  }\n  function reportFormulaOperandPrefix(operand) {\n    return operand.dataset && operand.dataset.formulaPrefix ? String(operand.dataset.formulaPrefix) : \"\";\n  }\n  function reportFormulaOperandLabel(operand) {\n    return operand.dataset && operand.dataset.formulaLabel ? String(operand.dataset.formulaLabel) : \"\";\n  }\n  function reportFormulaOperandDepth(operand) {\n    const value = Number(operand.dataset && operand.dataset.formulaDepth);\n    return Number.isFinite(value) && value > 0 ? value : 1;\n  }\n  function controlValue2(control) {\n    const value = control?.value;\n    return value !== void 0 ? String(value).trim() : \"\";\n  }\n  function registerReportFormulaBuilderHydrator() {\n    registerHydrator({ name: \"report-formula-builder\", hydrate: hydrateReportFormulaBuilders });\n  }\n  registerReportFormulaBuilderHydrator();\n\n  // src/adapters/desk/client-src/realtime.ts\n  function connectOptions(options) {\n    return options;\n  }\n  function realtimeRouteFromOptions(options) {\n    let route = connectOptions(options)?.realtimeRoute || pageContext().realtimeRoute || \"/api/realtime\";\n    if (route.charAt(0) !== \"/\") {\n      route = `/${route}`;\n    }\n    return route.length > 1 ? route.replace(/\\/$/, \"\") : route;\n  }\n  function realtimeUrl(topic, options) {\n    const opts = connectOptions(options);\n    const url = new URL(realtimeRouteFromOptions(options), window.location.href);\n    url.searchParams.set(\"topic\", topic);\n    if (opts && opts.replayAfter !== void 0 && opts.replayAfter !== null) {\n      url.searchParams.set(\"replayAfter\", String(opts.replayAfter));\n    }\n    if (opts && opts.replayLimit !== void 0 && opts.replayLimit !== null) {\n      url.searchParams.set(\"replayLimit\", String(opts.replayLimit));\n    }\n    url.protocol = url.protocol === \"https:\" ? \"wss:\" : \"ws:\";\n    return url;\n  }\n  function realtimePresenceUrl(topic, options) {\n    const route = realtimeRouteFromOptions(options);\n    return withQuery(`${route === \"/\" ? \"\" : route}/presence`, { topic });\n  }\n  function realtimePresence(topic, options) {\n    return request(realtimePresenceUrl(topic, options)).then(unwrapData);\n  }\n  function realtimePresenceDocument(doctype, name, options) {\n    return realtimePresence(documentTopicFromOptions(doctype, name, options), options);\n  }\n  function addSocketListener(socket, type, listener) {\n    if (typeof socket.addEventListener === \"function\") {\n      socket.addEventListener(type, listener);\n      return;\n    }\n    socket[`on${type}`] = listener;\n  }\n  function callRealtimeHandler(handler, args) {\n    if (typeof handler === \"function\") {\n      handler.apply(null, args);\n    }\n  }\n  function openSocket(url, options) {\n    return new WebSocket(url, connectOptions(options)?.protocols);\n  }\n  function realtimeSubscribe(topic, handlers, options) {\n    const url = realtimeUrl(topic, options).toString();\n    const socket = openSocket(url, options);\n    const subscription = {\n      close(code, reason) {\n        socket.close(code, reason);\n      },\n      send(message) {\n        socket.send(typeof message === \"string\" ? message : JSON.stringify(message));\n        return message;\n      },\n      sendFieldEdit(field, input) {\n        return realtimeSendFieldEdit(socket, field, input);\n      },\n      sendSharedDraft(input) {\n        return realtimeSendSharedDraft(socket, input);\n      },\n      socket,\n      topic,\n      url\n    };\n    const callbacks = handlers || {};\n    addSocketListener(socket, \"message\", (message) => {\n      handleRealtimeMessage(message, subscription, callbacks);\n    });\n    addSocketListener(socket, \"open\", (event) => {\n      callRealtimeHandler(callbacks.open, [event, subscription]);\n    });\n    addSocketListener(socket, \"close\", (event) => {\n      callRealtimeHandler(callbacks.close, [event, subscription]);\n    });\n    addSocketListener(socket, \"error\", (event) => {\n      callRealtimeHandler(callbacks.error, [event, subscription]);\n    });\n    return subscription;\n  }\n  function handleRealtimeMessage(rawMessage, subscription, callbacks) {\n    const raw = rawMessage?.data;\n    let parsed;\n    try {\n      parsed = typeof raw === \"string\" ? JSON.parse(raw) : raw;\n    } catch (error) {\n      callRealtimeHandler(callbacks.malformed, [error, raw, rawMessage, subscription]);\n      return;\n    }\n    callRealtimeHandler(callbacks.message, [parsed, rawMessage, subscription]);\n    if (!parsed || typeof parsed !== \"object\") {\n      return;\n    }\n    const message = parsed;\n    if (message.type === \"cf-frappe.realtime.connected\") {\n      callRealtimeHandler(callbacks.connected, [message, subscription]);\n      return;\n    }\n    if (message.type === \"cf-frappe.realtime.event\" && message.event) {\n      dispatchRealtimeEvent(message.event, message, subscription, callbacks);\n      return;\n    }\n    if (message.type === REALTIME_COLLABORATION_MESSAGE_TYPE && message.event) {\n      dispatchRealtimeCollaborationEvent(message.event, message, subscription, callbacks);\n      return;\n    }\n    if (message.type === \"cf-frappe.realtime.replay\" && message.replay) {\n      const replay = message.replay;\n      callRealtimeHandler(callbacks.replay, [replay, message, subscription]);\n      const events = Array.isArray(replay.events) ? replay.events : [];\n      events.forEach((entry) => {\n        const replayEntry = entry;\n        if (replayEntry && replayEntry.event) {\n          dispatchRealtimeEvent(\n            replayEntry.event,\n            Object.assign({}, message, {\n              type: \"cf-frappe.realtime.event\",\n              cursor: replayEntry.cursor,\n              event: replayEntry.event\n            }),\n            subscription,\n            callbacks\n          );\n        }\n      });\n      return;\n    }\n    if (message.type === \"cf-frappe.realtime.presence\" && message.presence) {\n      callRealtimeHandler(callbacks.presence, [message.presence, message, subscription]);\n    }\n  }\n  function dispatchRealtimeEvent(event, message, subscription, callbacks) {\n    callRealtimeHandler(callbacks.event, [event, message, subscription]);\n    const payload = event?.payload;\n    if (payload && payload.kind === \"DocumentUserNotification\") {\n      callRealtimeHandler(callbacks.notification, [payload, event, message, subscription]);\n    }\n  }\n  function dispatchRealtimeCollaborationEvent(event, message, subscription, callbacks) {\n    callRealtimeHandler(callbacks.collaboration, [event, message, subscription]);\n    const payload = event?.payload;\n    if (payload && payload.kind === \"DocumentFieldEditIntent\") {\n      callRealtimeHandler(callbacks.fieldEdit, [payload, event, message, subscription]);\n    }\n    if (payload && payload.kind === \"DocumentSharedDraftPatch\") {\n      callRealtimeHandler(callbacks.sharedDraft, [payload, event, message, subscription]);\n    }\n  }\n  function realtimeFieldEditMessage(field, input) {\n    const options = isPlainObject(input) ? input : input === void 0 ? {} : { value: input };\n    const message = {\n      type: FIELD_EDIT_MESSAGE_TYPE,\n      field: String(field || \"\").trim(),\n      editing: options.editing === false ? false : true\n    };\n    if (Object.prototype.hasOwnProperty.call(options, \"value\")) {\n      message.value = options.value;\n    }\n    return message;\n  }\n  function realtimeSendFieldEdit(socket, field, input) {\n    const message = realtimeFieldEditMessage(field, input);\n    socket.send(JSON.stringify(message));\n    return message;\n  }\n  function realtimeSharedDraftMessage(input) {\n    const options = isPlainObject(input) ? input : {};\n    const message = {\n      type: SHARED_DRAFT_MESSAGE_TYPE\n    };\n    if (Number.isInteger(options.baseVersion) && options.baseVersion >= 0) {\n      message.baseVersion = options.baseVersion;\n    }\n    if (isPlainObject(options.patch)) {\n      message.patch = options.patch;\n    }\n    if (Array.isArray(options.unset)) {\n      message.unset = options.unset;\n    }\n    return message;\n  }\n  function realtimeSendSharedDraft(socket, input) {\n    const message = realtimeSharedDraftMessage(input);\n    socket.send(JSON.stringify(message));\n    return message;\n  }\n  function realtimeNamespaceExtension() {\n    return {\n      connect: (topic, options) => openSocket(realtimeUrl(topic, options), options),\n      doctype: (doctype, options) => openSocket(realtimeUrl(doctypeTopicFromOptions(doctype, options), options), options),\n      doctypeUrl: (doctype, options) => realtimeUrl(doctypeTopicFromOptions(doctype, options), options).toString(),\n      document: (doctype, name, options) => openSocket(realtimeUrl(documentTopicFromOptions(doctype, name, options), options), options),\n      documentUrl: (doctype, name, options) => realtimeUrl(documentTopicFromOptions(doctype, name, options), options).toString(),\n      tenant: (options) => openSocket(realtimeUrl(tenantTopicFromOptions(options), options), options),\n      tenantUrl: (options) => realtimeUrl(tenantTopicFromOptions(options), options).toString(),\n      user: (userId, options) => openSocket(realtimeUrl(userTopicFromOptions(userId, options), options), options),\n      userUrl: (userId, options) => realtimeUrl(userTopicFromOptions(userId, options), options).toString(),\n      presence: realtimePresence,\n      presenceDoctype: (doctype, options) => realtimePresence(doctypeTopicFromOptions(doctype, options), options),\n      presenceDocument: (doctype, name, options) => realtimePresenceDocument(doctype, name, options),\n      presenceTenant: (options) => realtimePresence(tenantTopicFromOptions(options), options),\n      presenceUrl: realtimePresenceUrl,\n      presenceUser: (userId, options) => realtimePresence(userTopicFromOptions(userId, options), options),\n      subscribe: (topic, handlers, options) => realtimeSubscribe(topic, handlers, options),\n      subscribeDoctype: (doctype, handlers, options) => realtimeSubscribe(doctypeTopicFromOptions(doctype, options), handlers, options),\n      subscribeDocument: (doctype, name, handlers, options) => realtimeSubscribe(documentTopicFromOptions(doctype, name, options), handlers, options),\n      subscribeTenant: (handlers, options) => realtimeSubscribe(tenantTopicFromOptions(options), handlers, options),\n      subscribeUser: (userId, handlers, options) => realtimeSubscribe(userTopicFromOptions(userId, options), handlers, options),\n      url: (topic, options) => realtimeUrl(topic, options).toString()\n    };\n  }\n  var collaborationMessageApi = {\n    fieldEditMessage: realtimeFieldEditMessage,\n    sendFieldEdit: (subscription, field, input) => {\n      const target = subscription;\n      if (!target || typeof target.sendFieldEdit !== \"function\") {\n        return realtimeFieldEditMessage(field, input);\n      }\n      return target.sendFieldEdit(field, input);\n    },\n    sendSharedDraft: (subscription, input) => {\n      const target = subscription;\n      if (!target || typeof target.sendSharedDraft !== \"function\") {\n        return realtimeSharedDraftMessage(input);\n      }\n      return target.sendSharedDraft(input);\n    },\n    sharedDraftMessage: realtimeSharedDraftMessage\n  };\n  function registerRealtimeNamespace() {\n    registerNamespaceContribution(() => ({ realtime: realtimeNamespaceExtension() }));\n  }\n  registerRealtimeNamespace();\n\n  // src/adapters/desk/client-src/presence.ts\n  function currentFormTarget() {\n    const namespace = window.cfFrappe;\n    const current = namespace?.form?.current;\n    if (typeof current !== \"function\") {\n      return null;\n    }\n    const frm = current();\n    if (!frm || typeof frm !== \"object\") {\n      return null;\n    }\n    const form = document.querySelector(\"form.form\");\n    if (!form) {\n      return null;\n    }\n    return { frm, form };\n  }\n  function matchingFormTarget(doctype, documentName) {\n    const target = currentFormTarget();\n    if (!target || target.frm.doctype !== doctype || target.frm.docname !== documentName) {\n      return null;\n    }\n    return target;\n  }\n  function childFieldPath2(fieldname) {\n    const match = /^([^.[\\]]+)\\[(\\d+)\\]\\.(.+)$/.exec(fieldname);\n    if (!match) {\n      return null;\n    }\n    return {\n      field: match[3],\n      index: Number(match[2]),\n      table: match[1]\n    };\n  }\n  function isInternalFormField2(fieldname) {\n    const child = childFieldPath2(fieldname);\n    return fieldname === \"expectedVersion\" || fieldname === CHILD_TABLE_ROW_INDEX_FIELD || child !== null && child.field === CHILD_TABLE_ROW_INDEX_FIELD;\n  }\n  function cloneMergeValue2(value) {\n    if (value === null || typeof value !== \"object\") {\n      return value;\n    }\n    if (Array.isArray(value)) {\n      return value.map(cloneMergeValue2);\n    }\n    const clone = {};\n    Object.keys(value).forEach((key) => {\n      clone[key] = cloneMergeValue2(value[key]);\n    });\n    return clone;\n  }\n  function setDocValue2(doc, fieldname, value) {\n    const child = childFieldPath2(fieldname);\n    if (child) {\n      const rows = Array.isArray(doc[child.table]) ? doc[child.table] : [];\n      rows[child.index] = Object.assign({}, rows[child.index] || {}, {\n        [child.field]: value\n      });\n      doc[child.table] = rows;\n      return;\n    }\n    doc[fieldname] = value;\n  }\n  function docValue2(doc, fieldname) {\n    if (Object.prototype.hasOwnProperty.call(doc, fieldname)) {\n      return doc[fieldname];\n    }\n    const child = childFieldPath2(fieldname);\n    if (!child || !Array.isArray(doc[child.table])) {\n      return void 0;\n    }\n    const row = doc[child.table][child.index];\n    return row && row[child.field];\n  }\n  function unsetDocValue(doc, fieldname) {\n    const child = childFieldPath2(fieldname);\n    if (child) {\n      const rows = Array.isArray(doc[child.table]) ? doc[child.table] : [];\n      const row = rows[child.index];\n      if (row) {\n        delete row[child.field];\n      }\n      doc[child.table] = rows;\n      return;\n    }\n    delete doc[fieldname];\n  }\n  function formFields(form) {\n    return Array.prototype.slice.call(form.querySelectorAll(\"[name]\"));\n  }\n  function fieldValue2(field) {\n    const fieldType = field.dataset && field.dataset.cfFrappeFieldType;\n    if (field.type === \"checkbox\") {\n      return Boolean(field.checked);\n    }\n    if (fieldType && field.value === \"\" && !field.required) {\n      return void 0;\n    }\n    if (fieldType === \"integer\") {\n      const integerValue = Number(field.value);\n      return Number.isInteger(integerValue) ? integerValue : field.value;\n    }\n    if (fieldType === \"number\") {\n      const numberValue = Number(field.value);\n      return Number.isFinite(numberValue) ? numberValue : field.value;\n    }\n    if (fieldType === \"boolean\") {\n      return field.value === \"on\" || field.value === \"true\";\n    }\n    if (fieldType === \"json\") {\n      try {\n        return JSON.parse(field.value);\n      } catch (_error) {\n        return field.value;\n      }\n    }\n    return field.value;\n  }\n  function setControlValue2(field, value) {\n    if (field.type === \"checkbox\") {\n      field.checked = Boolean(value);\n    } else if (field.dataset && field.dataset.cfFrappeFieldType === \"json\" && value !== null && typeof value === \"object\") {\n      field.value = JSON.stringify(value);\n    } else {\n      field.value = value == null ? \"\" : String(value);\n    }\n  }\n  function fieldInteractionLocked2(field) {\n    const expando2 = field;\n    return Boolean(expando2[READ_ONLY_PROPERTY] || expando2[SOFT_DISABLED_PROPERTY]);\n  }\n  function rememberLockedFieldValue2(field) {\n    if (fieldInteractionLocked2(field)) {\n      field[LOCKED_VALUE_PROPERTY] = fieldValue2(field);\n    }\n  }\n  function readFormData2(form) {\n    const doc = {};\n    formFields(form).forEach((field) => {\n      if (!isInternalFormField2(field.name)) {\n        setDocValue2(doc, field.name, fieldValue2(field));\n      }\n    });\n    return doc;\n  }\n  function formExpectedVersion2(form) {\n    const fields = formFields(form).filter((field) => field.name === \"expectedVersion\");\n    const first = fields[0];\n    if (!first) {\n      return 0;\n    }\n    const value = Number(first.value);\n    return Number.isInteger(value) && value >= 0 ? value : 0;\n  }\n  function formBaseVersion(form) {\n    const stamped = form.dataset.documentVersion;\n    if (stamped !== void 0) {\n      const stampedVersion = Number(stamped);\n      if (Number.isInteger(stampedVersion) && stampedVersion >= 0) {\n        return stampedVersion;\n      }\n    }\n    const contextVersion = pageContext().documentVersion;\n    return contextVersion === void 0 ? formExpectedVersion2(form) : contextVersion;\n  }\n  function writeDocumentToForm2(form, data) {\n    const baseVersion = formBaseVersion(form);\n    formFields(form).forEach((field) => {\n      if (field.name === \"expectedVersion\") {\n        setControlValue2(field, baseVersion);\n        rememberLockedFieldValue2(field);\n        return;\n      }\n      if (isInternalFormField2(field.name)) {\n        return;\n      }\n      setControlValue2(field, docValue2(data, field.name));\n      rememberLockedFieldValue2(field);\n    });\n  }\n  function hydratePresencePanels() {\n    const panels = document.querySelectorAll('[data-cf-frappe-presence=\"document\"]');\n    if (!panels || panels.length === 0) {\n      return;\n    }\n    Array.prototype.forEach.call(panels, (panel) => {\n      hydratePresencePanel(panel);\n    });\n  }\n  function hydratePresencePanel(panel) {\n    const context = pageContext();\n    const dataset = panel.dataset || {};\n    const doctype = dataset.doctype || context.doctype;\n    const documentName = dataset.documentName || context.documentName;\n    const realtimeRoute = dataset.realtimeRoute || context.realtimeRoute;\n    const tenantId = dataset.tenantId || context.tenantId;\n    if (!doctype || !documentName || !tenantId) {\n      return;\n    }\n    const realtimeOptions = Object.assign(\n      { tenantId },\n      realtimeRoute ? { realtimeRoute } : {}\n    );\n    setPresencePanelState(panel, \"loading\", \"Checking active collaborators.\", \"Checking active collaborators.\");\n    setPanelText(panel, \"[data-cf-frappe-document-update]\", \"Viewing latest saved version.\");\n    setPanelText(panel, \"[data-cf-frappe-shared-draft]\", \"No shared draft proposals.\");\n    attachPresencePanelMergeSave(panel, doctype, documentName);\n    attachPresencePanelSharedDraftApply(panel, doctype, documentName);\n    setPresencePanelMergeAction(panel, false, false);\n    setPresencePanelSharedDraftAction(panel, false, false);\n    realtimePresenceDocument(doctype, documentName, realtimeOptions).then((snapshot) => {\n      setPresencePanelConnections(panel, \"ready\", snapshot?.connections);\n      subscribePresencePanel(panel, doctype, documentName, realtimeOptions);\n    }).catch((error) => {\n      const message = error?.message;\n      setPresencePanelState(\n        panel,\n        \"error\",\n        \"Presence unavailable\",\n        message ? String(message) : \"Unable to load document presence.\"\n      );\n    });\n  }\n  function subscribePresencePanel(panel, doctype, documentName, options) {\n    if (panel.__cfFrappePresenceSubscription) {\n      return;\n    }\n    try {\n      panel.__cfFrappePresenceSubscription = realtimeSubscribe(\n        documentTopicFromOptions(doctype, documentName, options),\n        {\n          event: (event) => {\n            markPresencePanelDocumentEvent(panel, event, doctype, documentName);\n          },\n          presence: (presence) => {\n            setPresencePanelConnections(panel, \"live\", presence?.connections);\n          },\n          fieldEdit: (payload) => {\n            setPresencePanelFieldEdit(panel, payload);\n          },\n          sharedDraft: (payload) => {\n            setPresencePanelSharedDraft(panel, payload, doctype, documentName);\n          }\n        },\n        options\n      );\n    } catch (_error) {\n      panel.__cfFrappePresenceSubscription = void 0;\n    }\n  }\n  function markPresencePanelDocumentEvent(panel, event, doctype, documentName) {\n    const payload = event?.payload;\n    const snapshot = payload?.snapshot;\n    const remoteVersion = snapshot?.version;\n    const localVersion = panel.dataset && panel.dataset.documentVersion ? Number(panel.dataset.documentVersion) : NaN;\n    if (typeof remoteVersion !== \"number\" || Number.isNaN(localVersion) || remoteVersion <= localVersion) {\n      return;\n    }\n    if (panel.dataset) {\n      panel.dataset.documentState = \"stale\";\n      panel.dataset.remoteVersion = String(remoteVersion);\n    }\n    setPanelText(\n      panel,\n      \"[data-cf-frappe-document-update]\",\n      `Document updated to v${String(remoteVersion)}. Refresh to review latest changes.`\n    );\n    const target = markCurrentFormRemoteUpdate(doctype, documentName, snapshot);\n    setPresencePanelMergeAction(panel, Boolean(target), false, \"Merge saved changes\");\n  }\n  function markCurrentFormRemoteUpdate(doctype, documentName, snapshot) {\n    const target = matchingFormTarget(doctype, documentName);\n    if (!target) {\n      return null;\n    }\n    target.form.dataset.remoteUpdate = \"1\";\n    if (snapshot && isPlainObject(snapshot.data) && typeof target.frm.mergePlan === \"function\") {\n      const plan = target.frm.mergePlan(snapshot);\n      target.frm.remote_merge_plan = plan;\n      if (plan && plan.status !== void 0) {\n        target.form.dataset.remoteMergeState = String(plan.status);\n      }\n    }\n    return target;\n  }\n  function attachPresencePanelMergeSave(panel, doctype, documentName) {\n    if (panel.__cfFrappeMergeSaveAttached) {\n      return;\n    }\n    const button = panel.querySelector(\"[data-cf-frappe-merge-save]\");\n    if (!button || typeof button.addEventListener !== \"function\") {\n      return;\n    }\n    panel.__cfFrappeMergeSaveAttached = true;\n    button.addEventListener(\"click\", () => {\n      const target = matchingFormTarget(doctype, documentName);\n      if (!target) {\n        return;\n      }\n      setPresencePanelMergeAction(panel, true, true, \"Merging...\");\n      setPanelText(panel, \"[data-cf-frappe-document-update]\", \"Merging saved changes.\");\n      target.frm.merge_save().then((result) => {\n        if (result === false) {\n          if (panel.dataset) {\n            panel.dataset.documentState = \"validation-blocked\";\n          }\n          setPanelText(panel, \"[data-cf-frappe-document-update]\", \"Fix validation errors before merging saved changes.\");\n          setPresencePanelMergeAction(panel, true, false, \"Try merge again\");\n          return;\n        }\n        updatePresencePanelMergeResult(panel, result);\n      }).catch((error) => {\n        if (panel.dataset) {\n          panel.dataset.documentState = \"merge-error\";\n        }\n        const message = error?.message;\n        setPanelText(\n          panel,\n          \"[data-cf-frappe-document-update]\",\n          message ? String(message) : \"Unable to merge saved changes.\"\n        );\n        setPresencePanelMergeAction(panel, true, false, \"Try merge again\");\n      });\n    });\n  }\n  function updatePresencePanelMergeResult(panel, result) {\n    const mergeResult = result;\n    const mergedDocument = mergeResult?.document;\n    const version = mergedDocument?.version;\n    if (mergeResult && (mergeResult.status === \"applied\" || mergeResult.status === \"noop\")) {\n      if (panel.dataset) {\n        panel.dataset.documentState = \"merged\";\n        if (typeof version === \"number\") {\n          panel.dataset.documentVersion = String(version);\n          panel.dataset.remoteVersion = String(version);\n        }\n      }\n      setPanelText(\n        panel,\n        \"[data-cf-frappe-document-update]\",\n        mergeResult.status === \"applied\" ? `Merged saved changes${typeof version === \"number\" ? ` at v${String(version)}` : \"\"}.` : `Already up to date${typeof version === \"number\" ? ` at v${String(version)}` : \"\"}.`\n      );\n      setPresencePanelMergeAction(panel, false, false);\n      return;\n    }\n    if (panel.dataset) {\n      panel.dataset.documentState = \"conflict\";\n    }\n    setPanelText(panel, \"[data-cf-frappe-document-update]\", \"Merge conflict. Review local changes before saving.\");\n    setPresencePanelMergeAction(panel, true, false, \"Try merge again\");\n  }\n  function setPresencePanelMergeAction(panel, visible, disabled, label) {\n    const button = panel.querySelector(\"[data-cf-frappe-merge-save]\");\n    if (!button) {\n      return;\n    }\n    button.hidden = !visible;\n    button.disabled = Boolean(disabled);\n    if (label !== void 0) {\n      button.textContent = label;\n    }\n  }\n  function attachPresencePanelSharedDraftApply(panel, doctype, documentName) {\n    if (panel.__cfFrappeSharedDraftApplyAttached) {\n      return;\n    }\n    const button = panel.querySelector(\"[data-cf-frappe-apply-shared-draft]\");\n    if (!button || typeof button.addEventListener !== \"function\") {\n      return;\n    }\n    panel.__cfFrappeSharedDraftApplyAttached = true;\n    button.addEventListener(\"click\", () => {\n      const target = matchingFormTarget(doctype, documentName);\n      const draft = panel.__cfFrappeSharedDraft;\n      if (!target || !draft) {\n        return;\n      }\n      if (typeof draft.payload.baseVersion === \"number\" && formBaseVersion(target.form) !== draft.payload.baseVersion) {\n        if (panel.dataset) {\n          panel.dataset.sharedDraftState = \"stale\";\n        }\n        setPresencePanelSharedDraftAction(panel, false, false);\n        return;\n      }\n      setPresencePanelSharedDraftAction(panel, true, true, \"Applying...\");\n      const fields = applySharedDraftToForm(target, draft.payload);\n      if (fields.length === 0) {\n        if (panel.dataset) {\n          panel.dataset.sharedDraftState = \"noop\";\n        }\n        setPanelText(panel, \"[data-cf-frappe-shared-draft]\", \"No applicable shared draft changes.\");\n        setPresencePanelSharedDraftAction(panel, false, false);\n        return;\n      }\n      if (panel.dataset) {\n        panel.dataset.sharedDraftState = \"applied\";\n      }\n      setPanelText(\n        panel,\n        \"[data-cf-frappe-shared-draft]\",\n        `Applied shared draft from ${draft.actor}: ${presencePanelFieldSummary(fields)}.`\n      );\n      setPresencePanelSharedDraftAction(panel, false, false);\n    });\n  }\n  function setPresencePanelSharedDraft(panel, payload, doctype, documentName) {\n    const draftPayload = payload;\n    if (!draftPayload || draftPayload.doctype !== doctype || draftPayload.name !== documentName) {\n      return;\n    }\n    const fields = sharedDraftFields(draftPayload);\n    if (fields.length === 0) {\n      return;\n    }\n    const actor = String(draftPayload.actorId || draftPayload.connectionId || \"A collaborator\");\n    panel.__cfFrappeSharedDraft = {\n      actor,\n      payload: {\n        baseVersion: draftPayload.baseVersion,\n        patch: isPlainObject(draftPayload.patch) ? cloneMergeValue2(draftPayload.patch) : {},\n        unset: Array.isArray(draftPayload.unset) ? draftPayload.unset.slice() : []\n      }\n    };\n    if (panel.dataset) {\n      panel.dataset.sharedDraftState = \"available\";\n      if (typeof draftPayload.baseVersion === \"number\") {\n        panel.dataset.sharedDraftBaseVersion = String(draftPayload.baseVersion);\n      }\n    }\n    setPanelText(\n      panel,\n      \"[data-cf-frappe-shared-draft]\",\n      `${actor} shared draft changes: ${presencePanelFieldSummary(fields)}.`\n    );\n    const target = matchingFormTarget(doctype, documentName);\n    if (target && typeof draftPayload.baseVersion === \"number\" && formBaseVersion(target.form) !== draftPayload.baseVersion) {\n      if (panel.dataset) {\n        panel.dataset.sharedDraftState = \"stale\";\n      }\n      setPanelText(\n        panel,\n        \"[data-cf-frappe-shared-draft]\",\n        `${actor} shared draft changes for v${String(draftPayload.baseVersion)}; current form is v${String(formBaseVersion(target.form))}.`\n      );\n      setPresencePanelSharedDraftAction(panel, false, false);\n      return;\n    }\n    setPresencePanelSharedDraftAction(panel, Boolean(target), false, \"Apply shared draft\");\n  }\n  function applySharedDraftToForm(target, payload) {\n    const draft = cloneMergeValue2(readFormData2(target.form));\n    const changed = [];\n    const patch = isPlainObject(payload.patch) ? payload.patch : {};\n    Object.keys(patch).forEach((field) => {\n      const fieldname = String(field || \"\").trim();\n      if (!fieldname || isInternalFormField2(fieldname)) {\n        return;\n      }\n      setDocValue2(draft, fieldname, cloneMergeValue2(patch[field]));\n      changed.push(fieldname);\n    });\n    (Array.isArray(payload.unset) ? payload.unset : []).forEach((field) => {\n      const fieldname = String(field || \"\").trim();\n      if (!fieldname || isInternalFormField2(fieldname) || changed.indexOf(fieldname) >= 0) {\n        return;\n      }\n      unsetDocValue(draft, fieldname);\n      changed.push(fieldname);\n    });\n    if (changed.length === 0) {\n      return changed;\n    }\n    target.frm.doc = draft;\n    writeDocumentToForm2(target.form, draft);\n    target.frm.dirty();\n    changed.forEach((field) => {\n      target.frm.trigger(field);\n    });\n    return changed;\n  }\n  function sharedDraftFields(payload) {\n    const seen = {};\n    const fields = [];\n    const patch = isPlainObject(payload.patch) ? payload.patch : {};\n    Object.keys(patch).forEach((field) => {\n      addSharedDraftField(fields, seen, field);\n    });\n    (Array.isArray(payload.unset) ? payload.unset : []).forEach((field) => {\n      addSharedDraftField(fields, seen, field);\n    });\n    return fields;\n  }\n  function addSharedDraftField(fields, seen, field) {\n    const fieldname = String(field || \"\").trim();\n    if (!fieldname || seen[fieldname]) {\n      return;\n    }\n    seen[fieldname] = true;\n    fields.push(fieldname);\n  }\n  function presencePanelFieldSummary(fields) {\n    const visible = fields.slice(0, 5);\n    const suffix = fields.length > visible.length ? ` +${String(fields.length - visible.length)} more` : \"\";\n    return visible.join(\", \") + suffix;\n  }\n  function setPresencePanelSharedDraftAction(panel, visible, disabled, label) {\n    const button = panel.querySelector(\"[data-cf-frappe-apply-shared-draft]\");\n    if (!button) {\n      return;\n    }\n    button.hidden = !visible;\n    button.disabled = Boolean(disabled);\n    if (label !== void 0) {\n      button.textContent = label;\n    }\n  }\n  function setPresencePanelConnections(panel, state, connections) {\n    prunePresencePanelFieldEdits(panel, connections);\n    const labels = presenceConnectionLabels(connections);\n    const count = labels.length;\n    setPresencePanelState(\n      panel,\n      state,\n      count === 1 ? \"1 active collaborator\" : `${String(count)} active collaborators`,\n      count === 0 ? \"No active collaborators are viewing this document.\" : labels.join(\", \")\n    );\n  }\n  function presenceConnectionLabels(connections) {\n    const seen = {};\n    const labels = [];\n    (Array.isArray(connections) ? connections : []).forEach((connection) => {\n      const record = connection;\n      const label = record && (record.userId || record.connectionId);\n      if (!label || seen[String(label)]) {\n        return;\n      }\n      seen[String(label)] = true;\n      labels.push(String(label));\n    });\n    return labels;\n  }\n  function setPresencePanelFieldEdit(panel, payload) {\n    const editPayload = payload;\n    if (!editPayload || !editPayload.field) {\n      return;\n    }\n    const edits = panel.__cfFrappeFieldEdits || {};\n    const key = `${String(editPayload.connectionId || \"\")}:${String(editPayload.field)}`;\n    if (editPayload.editing === false) {\n      delete edits[key];\n    } else {\n      edits[key] = {\n        actor: editPayload.actorId || editPayload.connectionId || \"A collaborator\",\n        connectionId: editPayload.connectionId,\n        field: editPayload.field\n      };\n    }\n    panel.__cfFrappeFieldEdits = edits;\n    renderPresencePanelFieldEdits(panel);\n  }\n  function prunePresencePanelFieldEdits(panel, connections) {\n    const edits = panel.__cfFrappeFieldEdits;\n    if (!edits || !Array.isArray(connections)) {\n      return;\n    }\n    const active = {};\n    connections.forEach((connection) => {\n      const record = connection;\n      if (record && record.connectionId) {\n        active[String(record.connectionId)] = true;\n      }\n    });\n    let changed = false;\n    Object.keys(edits).forEach((editKey) => {\n      const edit = edits[editKey];\n      const connectionId = edit && edit.connectionId;\n      if (!connectionId || !active[String(connectionId)]) {\n        delete edits[editKey];\n        changed = true;\n      }\n    });\n    if (changed) {\n      renderPresencePanelFieldEdits(panel);\n    }\n  }\n  function renderPresencePanelFieldEdits(panel) {\n    const edits = panel.__cfFrappeFieldEdits || {};\n    const labels = Object.keys(edits).sort().map((editKey) => {\n      const edit = edits[editKey];\n      return `${String(edit.actor)} editing ${String(edit.field)}`;\n    });\n    setPanelText(panel, \"[data-cf-frappe-field-edits]\", labels.length === 0 ? \"No live field edits.\" : labels.join(\", \"));\n  }\n  function setPresencePanelState(panel, state, countText, listText) {\n    if (panel.dataset) {\n      panel.dataset.presenceState = state;\n    }\n    setPanelText(panel, \"[data-cf-frappe-presence-count]\", countText);\n    setPanelText(panel, \"[data-cf-frappe-presence-list]\", listText);\n  }\n  function setPanelText(panel, selector, value) {\n    const panelTarget = typeof panel.querySelector === \"function\" ? panel.querySelector(selector) : null;\n    if (panelTarget) {\n      panelTarget.textContent = value;\n    }\n  }\n  function registerPresencePanels() {\n    registerHydrator({ name: \"presence-panels\", hydrate: hydratePresencePanels });\n  }\n  registerPresencePanels();\n\n  // src/adapters/desk/client-src/hydrators.ts\n  registerNamespaceContribution(() => ({\n    collaboration: {\n      ...collaborationMessageApi,\n      mergePlan: documentMergePlan\n    }\n  }));\n\n  // src/adapters/desk/client-src/main.ts\n  boot();\n})();\n";
