/**
 * GENERATED IMPORT LIST — behavior modules self-register here.
 *
 * Each ported behavior module (uploads, filter builder, formula builder, form binding,
 * realtime/presence) is imported below for its registration side effects
 * (`registerHydrator` / `registerNamespaceContribution` from `./boot.js`).
 *
 * The Flip agent finalizes this list once all behavior modules are ported. `main.ts`
 * does NOT need edits when modules land — add the import here instead, e.g.:
 *
 *   import "./uploads.js";
 *   import "./filter-builder.js";
 *   import "./formula-builder.js";
 *   import "./form.js";
 *   import "./realtime.js";
 *   import "./presence.js";
 */

export {};
